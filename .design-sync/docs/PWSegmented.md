---
category: Controls
---
The preferences segmented control: `options` (string labels) with `selected` as an index; the active segment gets a raised background.

```jsx
<PWSegmented options={["Menu bar", "Dock", "Both"]} selected={0} />
```
