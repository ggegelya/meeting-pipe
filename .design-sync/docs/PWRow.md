---
category: Structure
---
The inline preference row: `label` (+ optional `sublabel`) grows on the left, compact controls hug the right; `first` drops the top hairline, `alignTop` for tall controls.
For full-width controls (sliders, long fields) stack them under the label instead of forcing them into the right slot.
