---
category: Structure
---
One extracted action item: circle marker + `task` text, with tinted mini-chips below for `owner`, `due`, and `confidence` (chip appears only when confidence is "high").

```jsx
<SLActionItem task="Draft the migration runbook" owner="Georgy" due="Jul 10" confidence="high" />
```
