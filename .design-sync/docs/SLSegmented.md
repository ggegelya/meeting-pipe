---
category: Controls
---
The Library's smaller segmented control (text-xs): `options` strings with `selected` index; used for Read / Edit and channel switches.

```jsx
<SLSegmented options={["Read", "Edit"]} selected={0} />
```
