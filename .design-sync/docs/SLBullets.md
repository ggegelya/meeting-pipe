---
category: Structure
---
The summary bullet list: `items` render with faint bullet markers, or numbered in mono when `numbered` is set. Realistic items are single-sentence meeting takeaways.

```jsx
<SLBullets items={["Q3 scope agreed: ship the roster editor first.", "Pricing review moves to Thursday."]} />
```
