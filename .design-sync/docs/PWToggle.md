---
category: Controls
---
The macOS-style switch (34x20): signal-600 when `on`, ink-300 when off. Static visual, no callbacks.
Usually composed inside a PWRow so the label carries the meaning.

```jsx
<PWRow label="Launch at login" first><PWToggle on /></PWRow>
```
