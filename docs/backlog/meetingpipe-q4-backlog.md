# MeetingPipe Q4 backlog

This is the source of truth for TECH-* items. Q3 archived to `q3-final.md`; the Q2 archives were removed in TECH-DOC4 (git history preserves them). Q4 carries forward everything Q3 left open (not started, "done but ..." with an unmet bar, and items closed at low confidence), plus the findings from the Q4 full-scale review (architecture, security, performance, design) and the workflow-editor review, plus the mic-capture and mute-detection rearchitecture from the Q4 mute-capture audit (the `TECH-MIC` band).

## How to pick up a task (delegation)

Mechanics are codified in `/tech-task <ID>` (read the task here, read the orientation docs, implement, verify, one commit on `main`, do not push). This section is only the soft context that command does not carry:

- User locale: 99% English / Ukrainian. Do not default examples to German. `uk` is in `MuteLabels.toml`; verify any new `uk` labels with the user.
- The user is vibe-coding: high-level explanations, does not read code. Lean on the ARCHITECTURE.md diagrams.
- No em-dashes in any output (CI fails on a U+2014 in an added line; a whole-tree ban under `daemon/Sources` and `daemon/Resources`). Use hyphens, commas, or rewrite. Do not hardcode a personal name or email; commit with the repository's configured git identity. Do not push without permission.
- MicGate runtime knobs live in Preferences > Recording > Microphone and Preferences > Prompt > Stop conditions.
- Binding constraint (from CLAUDE.md): the primary user is the author, selling is tertiary. Technical excellence in core functionality comes first. The one promotion this quarter: GitHub repo presence and the app's visual identity are in scope (TECH-REPO1/REPO2), for contributor visibility. The full launch BRAND band stays deferred.

## Priority bands

- **P0**: correctness or a broken core promise; do first.
- **P1**: meaningful, not blocking.
- **P2**: polish and power-user payoff.
- **P3**: deferred; listed so the trail stays readable.

## Table of contents

| ID | Task | Category | Status | Description |
|---|---|---|---|---|
| TECH-SEC2 | Clamp sinks under global regulated mode | Security | DONE (was P0) | Global regulated mode forces the local LLM but does not strip the Notion sink, so the publish path still transmits meeting content; clamp sinks the way NDA does. |
| TECH-CONC1 | Move MicGate emit + lock off the render thread | Concurrency | DONE (was P1) | Every mic gate flip takes an NSLock and writes events.jsonl synchronously on the audio render thread; defer both and add a CI assertion. |
| TECH-SEC3 | Process-wide egress firewall under regulated/NDA | Security | DONE (was P1) | Replace seven scattered flag checks with one httpx transport that hard-fails any non-loopback request when regulated or NDA is active. |
| TECH-SEC4 | Gate or remove the daemon's direct Notion call | Security | DONE (was P1) | The daemon POSTs to api.notion.com for the DB picker, outside all egress enforcement and against its own "no Notion from the daemon" rule. |
| TECH-ARCH1 | Single effective_config() chokepoint | Architecture | DONE (was P1) | The regulated/NDA force-local rule is copied across four-plus sites; compute it once so a new code path cannot forget the clamp. |
| TECH-PERF1 | Stream diarize.py per segment | Performance | DONE (was P1) | The channel-aware fallback loads the whole stereo WAV plus two float copies (~2 GB at 3 h); read per-segment instead. |
| TECH-PERF2 | FluidAudio mono read + free input buffer (was A13 RAM half) | Performance | DONE (was P1 carry) | readMonoFloat32 holds the full clip plus a mono copy across ASR and diarization; halve the peak and free the input buffer early. |
| TECH-WF1 | Workflow backend unification + inherit | Workflow | DONE (was P1) | The per-workflow picker omits Apple Intelligence and always overrides the global backend; add it plus a "use global default" option. |
| TECH-DSN1 | Preferences IA pass | Design | DONE (was P1) | Seven panes, ~44 controls; collapse the local-MLX cluster behind a disclosure, cut cosmetic toggles, move regulated mode out of Prompt. |
| TECH-DSN2 | Detail-pane tabs + one republish path | Design | DONE (was P1) | Five tabs (two are debug surfaces) and 5+ overlapping republish/reprocess controls; reduce to three tabs and one canonical republish. |
| TECH-REPO1 | GitHub repo presence | Identity | DONE (was P1) | README hero, LICENSE, CONTRIBUTING, repo description / topics / social preview, for contributor visibility. |
| TECH-REPO2 | App visual identity | Identity | DONE (was P1) | A signal color that is unmistakably MeetingPipe (not generic system blue) plus icon polish; the MacPaw-grade identity. |
| TECH-H1-FINISH | Coordinator under 600 lines | Architecture | DONE (was P1) | Still 1371 lines; only three extractions landed. See TECH-ARCH2. |
| TECH-C6-FINISH | Real detection-trace corpus | Detection | DONE: scaffolding (real traces owed) | Still nine synthetic seeds; need 20+ real dogfood traces so detection cannot silently regress. |
| TECH-VALID1 | On-device acceptance for A15/A16/DIAR1/SUM1-APPLE/UX4 | Validation | DONE: scaffolding (runtime acceptance owed) | Five Q3 tasks shipped code but their quality / latency / zero-egress / degraded-banner bars were never validated on a real Mac. |
| TECH-CONC2 | Strict-concurrency island for MeetingPipeCore | Concurrency | DONE (was P2) | Turn on Swift 6 strict concurrency for the core module and the recorder's shared fields, where the real cross-thread risk lives. |
| TECH-PERF3 | Vectorize render-thread RMS (vDSP) | Performance | DONE (was P2) | Two scalar sum-of-squares passes per mic buffer on the hot path; use vDSP_svesq once. |
| TECH-PERF4 | Replace the 60 fps dismiss-ring timer | Performance | DONE (was P2) | DismissProgressView wakes the main thread 60x/sec for a cosmetic ring; use TimelineView(.animation) or a CA keyframe. |
| TECH-ARCH2 | Extract MeetingSessionController | Architecture | DONE (was P2) | Lift per-meeting verdict plumbing and sidecar writing out of Coordinator; finishes H1-FINISH by construction. |
| TECH-ARCH3 | Collapse PipelineLauncher scaffolding onto runMP | Architecture | DONE (was P2) | Four near-identical ~90-line Process/watchdog blocks; the runMP helper already factors the pattern. |
| TECH-SEC5 | Fail-closed subprocess env on local/NDA | Security | DONE (was P2) | Drop ANTHROPIC_API_KEY and NOTION_TOKEN from the child env on local/NDA runs so an enforcement bug fails closed, not open. |
| TECH-SEC6 | Untrusted-transcript boundary + field scrub | Security | DONE (was P2) | Wrap transcript text as untrusted in all summarizers and scrub owner/attendee fields before they reach sinks and the correction corpus. |
| TECH-SEC7 | Obsidian YAML title injection | Security | DONE (was P2) | A title with a newline injects YAML keys; route the title through _yaml_str and strip control chars at the Swift extraction boundary. |
| TECH-SEC8 | Tokens in Keychain (extends SEC1) | Security | P2 new | Move NOTION_TOKEN / ANTHROPIC_API_KEY out of secrets.env into the macOS Keychain; closes SEC1 properly. |
| TECH-SEC1 | secrets.env read-permission check | Security | DONE (was P2 carry) | Neither reader refuses a 0644 secrets.env; warn or refuse on a too-open mode. Folds into SEC8 if Keychain lands. |
| TECH-WF2 | Workflow emoji picker | Workflow | DONE (was P2) | The emoji field is a bare 80pt text box that accepts arbitrary text; use a real emoji picker constrained to one grapheme. |
| TECH-WF3 | Workflow color picker | Workflow | DONE (was P2) | Replace the #RRGGBB hex field with a native ColorPicker or token-aligned swatches; keep hex as an advanced fallback. |
| TECH-WF4 | Workflow identity-section layout | Workflow | DONE (was P2) | Name / Color / Emoji rows are raggedly aligned (a stray Spacer, mismatched widths); make the section coherent. |
| TECH-WF5 | Workflow discoverability | Workflow | DONE (was P2) | Editing a workflow is buried behind selecting a Library scope and an unlabeled pencil; give it a findable home. |
| TECH-DSN3 | Token-enforcement pass | Design | DONE: colors + guard (font-size sweep deferred) | 126 raw .system fonts and 22 raw colors bypass the token system; enforce tokens, unify to one button language, add a CI guard. |
| TECH-DSN4 | Identity color | Design | DONE (was P2) | Signal blue reads as generic macOS blue; pick a distinct hue that survives the dark-mode auto-flip. Pairs with REPO2. |
| TECH-DSN5 | Motion + haptic + opt-in tone | Design | DONE (was P2) | Animate exactly three moments; Stop haptic for the consequential action; one opt-in post-call tone; never an in-call chime. |
| TECH-DSN6 | Persisted NDA flag + row badge | Design | DONE (was P2) | The Library infers NDA heuristically, so a row can read "Local only" without the user setting it; persist and read the resolved flag. |
| TECH-DSN7 | Dead elapsed placeholder + menu-bar title | Design | DONE (was P2) | The recording pill shows a permanent "-:-"; the menu-bar title can stack four clauses. Fix or remove. |
| TECH-FEAT1 | Local-network (LAN) sink | Feature | DONE (was P2) | A reachability-aware filesystem publisher for a mounted SMB/NFS share (on-prem, no cloud cost); deepens the regulated story. |
| TECH-FEAT2 | Local semantic search ("ask my meetings") | Feature | DONE: lexical MVP (vector RAG deferred) | On-device RAG over the transcript library using the MLX model already run; the biggest "why this over cloud" answer. |
| TECH-FEAT5 | Auto/anthropic fallback on 429/500 | Feature | DONE (was P2) | The auto backend only falls back on connection/auth errors; a sustained rate-limit fails the run instead of going local. |
| TECH-DIST1 | Bundle runtime for a drag-n-drop installer | Distribution | P2 new | Embed a standalone Python plus ffmpeg and notarize, so a clean Mac (no Homebrew, no Python) can install by dragging the app. |
| TECH-DOC1 | Merge GLOSSARY into ARCHITECTURE | Docs | DONE (was P2) | Fold the glossary into an ARCHITECTURE "## Glossary" section, rewire the five references, delete GLOSSARY.md. |
| TECH-DOC2 | Merge SPEC into README + ADRs | Docs | DONE (was P2) | Move surviving rationale to a README "Why it is shaped this way" section, dedupe the schema tables into CONVENTIONS, rewire six refs, delete SPEC.md. |
| TECH-DOC3 | Trim the signal-fusion doc | Docs | DONE (was P2) | Fix the stale file tree (actor wording, deleted signals, per-vendor adapters, InternalSpeechProbe, Locale path, Thresholds.swift); keep the durable "why". |
| TECH-UI-X1 | Extract MeetingDetailView per tab | Architecture | DONE (was P2) | MeetingDetailView is ~1206 lines; split per tab (target under 250). |
| TECH-UI-X2 | Extract PreferencesView per section | Architecture | DONE (was P2) | PreferencesView is ~1177 lines; split per section (target under 200). Pairs with DSN1. |
| TECH-T2 | Snapshot tests for three SwiftUI views | Tests | DONE (was P2) | No snapshot harness yet; add swift-snapshot-testing gated by Appearance. |
| TECH-I6 | Partial-publish visibility | Observability | DONE (was P2) | Scoped down: fanout already returns per-sink results; only a sidecar publish_state field and a per-row indicator remain. |
| TECH-W2 | Workflow precedence test | Tests | DONE (was P2) | Near-noop: the acceptance bar is already met by WorkflowMatcherTests.test_ties_break_by_order_ascending; move or close. |
| TECH-E4-FINISH | Dogfood analysis script | Tooling | DONE (was P2) | The events.jsonl acceptance-bar report (scripts/dogfood-report) was never built; pipeline/src/mp/dogfood.py is a different A/B harness. |
| TECH-ARCH4 | Golden-vector fixture for the dual chunkers | Architecture | DONE (was P3) | The Swift and Python chunkers must stay identical; pin parity with one shared input/expected fixture across both suites. |
| TECH-PERF5 | Adaptive backoff on poll timers | Performance | DONE: polls (engine-tick gating deferred) | The 1 Hz HAL/AX polls and 4 Hz engine tick run all meeting; back off when the listener is delivering. |
| TECH-SEC9 | Drop unused AppleEvents entitlement | Security | DONE (was P3) | NSAppleEventsUsageDescription is declared but browser detection uses Accessibility; remove the capability or document a use site. Done: dropped the key from install.sh's Info.plist and removed AppleEvents from the tccutil reset loops in install.sh + uninstall.sh (no AppleEvents API is used in Swift). |
| TECH-FEAT3 | Speaker enrollment | Feature | DONE: MVP (voiceprint follow-up filed) | Label your own voice once so "me vs them" is reliable; leans on the diarization-cleanup work. |
| TECH-FEAT3-VOICEPRINT | Full speaker enrollment (voiceprint) | Feature | P3 new (FEAT3 follow-up) | Promote the FEAT3 heuristic MVP to a real voiceprint match so "me" holds on mono/merged audio and when the user is not the dominant speaker. |
| TECH-FEAT4 | Cross-meeting action tracking | Feature | DONE (was P3) | Extract open action items across meetings and surface the unresolved ones. |
| TECH-DIST2 | Spike: Swift port of the pipeline | Distribution | DONE (was P3) | Evaluate removing Python entirely (gated on MLX-Swift maturity for the local summarizer); would moot DIST1. Overlaps TECH-I7. |
| TECH-DOC4 | Delete Q2 archives + fix design READMEs | Docs | DONE (was P3) | Remove the two all-DONE Q2 archives (git preserves them) and trim the deleted-architecture prose in design/README.md and the ui_kits README. |
| TECH-DOC5 | Superseding ADRs | Docs | DONE (was P3) | Record that ADR 0001 (HAL tap) is superseded by ScreenCaptureKit and ADR 0002 is partially superseded by ADR 0007. |
| TECH-DSN8 | Summary-tab reading polish | Design | DONE (was P3) | Make the summary the app's "paper" moment: measured line length, the type ramp, generous rhythm. |
| TECH-DSN9 | Preferences layout polish | Design | DONE: audit + token-align (pixel retune deferred) | Loose vertical spacing and inconsistent element alignment across the General/Recording/Prompt/Pipeline panes; Integrations/Permissions/Advanced already read clean. A per-pane consistency pass, not a rebuild. |
| TECH-BRAND1..9 | Launch readiness band | Brand | Deferred | Domains, trademark, landing page, demo GIF, screenshots, OG card, compliance pages. Selling is tertiary; do not pull forward without an explicit launch decision. |
| TECH-I7 | Drop Python entirely | Deferred | P3 | Promotion trigger: Apple Intelligence proves out and the local LLM swap is Swift-native. See DIST2. |
| TECH-I8 | Live transcription during recording | Deferred | P3 | A tar pit; promotion trigger is a Q4 streaming-summarization design that proves the floor. |
| TECH-G1 | Personal two-Mac hub | Deferred | P3 | All P0/P1 dogfood bars met first. |
| TECH-D8 | Developer ID + notarization in CI | Deferred | P3 | Partly overlapped by DIST1; full notarization-in-CI stays parked until a second user. |
| TECH-CAP1 | Mic/system end-of-call skew | Deferred | P3 | Monitor only; promotion trigger is the few-second skew reappearing across multiple recordings. Spec retained in q3-final.md. |
| TECH-MIC1 | Capture-first architecture ADR (amends 0008/0009) | Architecture | DONE (P0) | Ratify: never destructively gate capture on a real-time mute read; default privacy is retention-based (Notion/Granola model); mute becomes optional per-tool metadata. Decision gate for MIC4/5/6. |
| TECH-MIC2 | Serialize MicGate.state (close the core race CONC2 left) | Concurrency | P1 new | Four signal sources funnel into one unsynchronized read-modify-write on `state`; a fresh `.unmuted` can be clobbered by a stale `.muted`. CONC2 fixed the recorder side only. |
| TECH-MIC3 | Mono-downmix the mic before the gate | Capture | P1 new | Gate zeroes channel 0 only while RMS averages all channels and the merge downmixes to mono; on a multichannel mic this both dilutes the gate (over-gates) and leaks muted channel-1 voice. |
| TECH-MIC4 | Lossless capture + retention-based privacy (the cure) | Capture | P0 new | Stop zeroing the mic at capture; capture losslessly like every major notetaker, manage privacy by retention. Fixes the data-loss bug across all tools with no mute-oracle dependency. |
| TECH-MIC5 | Optional offline mute-redaction layer | Privacy | P2 new | For the strict/regulated differentiator: redact muted spans from the consumed artifact offline from the recorded timeline, recoverable, ADR-0009 frame-parity preserving. |
| TECH-MIC6 | Futureproof + repair the native mute oracle | Detection | P1 new | Read the button's stable state attribute not its localized label; re-arm the dead element; fix the blind-clear that fired 0 times in 19 days. The verified proximate cause; load-bearing for regulated mode. |
| TECH-MIC7 | Browser/PWA mute oracle via a stable platform signal | Detection | P2 new | Browsers have no AX oracle; read the platform's own mute state (extension / DOM aria-state), Google Meet first. Only affects browser redaction accuracy; MIC4 already removes the loss. |
| TECH-MIC8 | Spike: UI-independent native mute signals (logs/SDK) | Detection | P3 new | Investigate whether Zoom/Teams local logs or SDKs expose mute transitions that survive every UI redesign, the strongest futureproofing; supersedes a tool's AX read if found. |

---

## Task specs

### Mic capture and mute detection (rearchitecture, Q4 mute-capture audit)

Origin: a two-auditor review of the recurring "talking unmuted, mic recorded nothing" failure, cross-checked against the live `events.jsonl` (21,252 micgate events over 19 days, every headline count re-verified) and the source, then pressure-tested by an adversarial panel. Both audits' diagnosis is confirmed: the mic gate is fail-closed (`passesLiveAudio` is true only for `.hot`, [MicGateVerdict.swift:31](../../daemon/Sources/MeetingPipeCore/MicGate/MicGateVerdict.swift)), reaching `.hot` requires not-muted AND voice-energy with no confidently-unmuted floor ([MicGate.swift:165](../../daemon/Sources/MeetingPipeCore/MicGate/MicGate.swift)), and a stale AX `.muted` latches for the whole call because the probe never re-arms its element ([AXMuteButtonProbe.swift:162](../../daemon/Sources/MeetingPipeCore/MicGate/Probes/AXMuteButtonProbe.swift)) and the blind-clear rescue is structurally unreachable (it fired 0 times in 19 days, [MeetingAXWindowWatcher.swift:167](../../daemon/Sources/MeetingPipe/MeetingAXWindowWatcher.swift)). The decisive finding the audits only half-stated, and a macOS-API sweep then confirmed: there is no general, cross-tool, real-time API to read whether another app's mic is muted (Core Audio process objects expose running-input but no mute selector; AUVoiceIO muted-talker detection is first-party only; ScreenCaptureKit cannot read mute; CallKit only sees your own app's calls; AX label-scraping is the only per-app oracle and it is absent for every browser meeting). So a real-time destructive capture gate that depends on the mute read can never be reliable across tools, and that dependency, not any single AX bug, is the architectural root cause.

Industry check (how the mature notetakers do it): Notion AI Meeting Notes and Granola both capture mic + system audio at the OS layer and do no per-app mute detection at all; cross-tool generality comes precisely from not depending on per-app mute state. Notion detects a meeting by "a process actively using the microphone (e.g. Zoom)" plus a calendar match and does not read mic content, gates on an explicit consent click, captures system+mic on desktop, stores a temporary local audio copy deleted after successful processing or within 24 h (persistent local storage is off by default, with an Enterprise auto-deletion schedule), and sends audio to transcription sub-processors that do not store it. Granola captures device audio, transcribes in real time, deletes the audio immediately, and keeps only the transcript, across Zoom / Meet / Teams / Slack / Webex via the one generic capture path. Neither honours in-app mute; privacy is managed by retention (ephemeral audio, transcript-only), not by gating. meeting-pipe's capture and detection layers are already industry-aligned (ScreenCaptureKit system audio + mic; process-audio + window detection); its only divergence is the real-time destructive MicGate, which no competitor has and which is the bug source.

Direction (ratified in TECH-MIC1): keep lossless OS-level capture as the load-bearing layer and never gate capture on a real-time mute read; manage default-mode privacy by retention the way Notion/Granola do; demote the mute signal to an optional, per-tool, stable-signal layer that drives an offline redaction for the strict/regulated differentiator (MIC5) and, where no audio at rest is required, a hardened real-time gate (MIC6). The user has approved tool-scoped approaches (Teams, Zoom, Slack, Google, ...) provided they are stable and futureproof against UI/UX changes, so the oracle tasks favour durable signals (a button's state attribute over its localized label, platform DOM/extension state, local logs) over the current localized-title scrape the incident proved fragile. Sequencing: MIC1 decides; MIC2/MIC3 are correctness fixes that are right under any architecture (do them regardless); MIC4 is the cure; MIC5/MIC6/MIC7/MIC8 are the optional privacy layer and the per-tool oracles.

**[DONE] TECH-MIC1 (P0 new): capture-first architecture ADR (amends ADR 0008/0009).** Write the MADR ADR in `docs/decisions/` that records the direction above and supersedes the load-bearing parts of 0008 (verdict-fusion as the capture gate) and 0009 (zero-at-capture rationale). Decisions to ratify: (a) lossless capture is load-bearing and never destructively gated on a real-time mute read; (b) the default-mode privacy model, choose one and document why, between keep-audio + redact muted spans (MIC5 on by default), ephemeral-audio + no redact (Granola model), or keep-audio + no redact (simplest, muted side-comments transcribed); (c) the mute signal is optional metadata feeding MIC5 and/or the MIC6 gate; (d) per-tool oracles must use stable, futureproof signals. The ADR must own the constraints the adversarial panel surfaced: any retained/working audio lives outside the Library-scanned tree and out of `RawFilesTab`'s reach ([RawFilesTab.swift:151](../../daemon/Sources/MeetingPipe/RawFilesTab.swift) enumerates every stem-prefixed file with one-click Reveal/Open/Copy-path), mode `0600`, Time-Machine/iCloud-excluded, reaped after processing; `captureMode` is a required, fail-closed argument (today `recorder.start` has no mode param and `regulated_mode` is a mutable global read only at sidecar-write time); the artifact every consumer reads (the daemon's FluidAudio at [SinkDispatcher.swift:81](../../daemon/Sources/MeetingPipe/Coordination/SinkDispatcher.swift) AND the Python pipeline) is the redacted/policy artifact, never a raw sibling; redaction is a zero-fill preserving ADR 0009 L/R frame parity; and the regulated/NDA path that keeps a real-time gate carries a residual data-loss risk by design (its only lever is MIC6). P0 as the decision gate: MIC4/MIC5/MIC6 implement whatever it ratifies. Acceptance: the user signs off on the default-mode behaviour change before MIC4 lands. Done: wrote [ADR 0016](../decisions/0016-capture-first-mute-redaction.md) (Status Accepted); the project owner chose the keep-full-recording + redact-muted-spans default on 2026-06-07 (over keep-everything-no-redact and ephemeral-Granola), recorded as the central decision with the three options weighed; the verdict-fusion machinery of 0008 is retained as the redaction-timeline source, and 0009's frame-parity invariant is preserved and re-homed to redaction time; added superseding notes to ADR 0008 (mic-gate-at-capture disposition superseded) and ADR 0009 (zero-at-capture mechanism amended). No code shipped (MIC4 carries lossless capture, MIC5 the redaction); the README "Why it is shaped this way" pointer is owed when MIC4 lands and behaviour actually changes.

**[DONE] TECH-MIC2 (P1 new): serialize MicGate.state (close the core-side race CONC2 scoped out).** All four signal sources (the AX notification probe via `NativeMuteAdapter`, the 1 Hz window-watcher via `injectAxMuteEvent`, the RMS gate on the render thread, the HAL probes on their queue) funnel into `MicGate.update` ([MicGate.swift:191](../../daemon/Sources/MeetingPipeCore/MicGate/MicGate.swift)), which does an unsynchronized read-modify-write on `state` (`var newState = state`, mutate, write back) off multiple threads; only `lastVerdict` in `publish` is lock-guarded. A fresh window-watcher `.unmuted` can be clobbered by a stale probe `.muted` or a concurrent RMS write, stranding a stale verdict. The `injectAxMuteEvent` doc comment (MicGate.swift:138) claims "update serialises internally," which is false. TECH-CONC2 fixed the recorder-side `GateAccess` but explicitly left the core-side `state` RMW for a later pass; this is that pass. Fix: guard the `state` RMW with the existing `NSLock` (or an `OSAllocatedUnfairLock`), correct the comment, add a test that interleaves a stale `.muted` and a fresh `.unmuted` and asserts the fresh write wins / `decide` sees a consistent snapshot. Right under any architecture; rework-proof. Done: wrapped the `update` read-modify-write in `lock.withLock`, taking the `decide` snapshot under the lock so the verdict always reflects a consistent state, and guarded the `stop()` reset of `state` too. The `injectAxMuteEvent` comment now accurately says `update` serialises under the lock. Added `MicGateConcurrencyTests`: one hammers three independent fields from 4,000 concurrent writers and asserts none is clobbered; one reproduces the incident shape (a fresh `.unmuted` racing a write storm never reverts to the stale `.muted`). Reused the existing `NSLock` rather than introducing `OSAllocatedUnfairLock` (no new type; publish's async dedupe takes the same lock without nesting, so no deadlock). Build clean; MicGate suite 40/0.

**[DONE] TECH-MIC3 (P1 new; privacy when multichannel): downmix the mic to mono before the gate.** Verified by the panel: the gate/zeroing touches channel 0 only (`UnsafeMutableBufferPointer(start: data[0], ...)`, [MeetingRecorder.swift:428](../../daemon/Sources/MeetingPipe/MeetingRecorder.swift)) while `sumOfSquares` averages energy across all channels (MeetingRecorder.swift:847-852, `samples = frameLen * channels`) and the stop-time ffmpeg merge forces mic to mono (`aformat=channel_layouts=mono`, :936). The mic tap takes whatever `inputNode.outputFormat(forBus:0)` reports, with no mono coercion on the capture path (:215/:242/:377), so on a multichannel input device (USB interface, aggregate device, mic array) two defects are live: (a) RMS energy is diluted ~3 dB, biasing the gate toward `silentByRMS` and feeding the over-gating data-loss family; (b) on a mute verdict channel 0 is zeroed but channels 1+ keep live voice, which the mono downmix folds back into the published WAV, a real leak of muted audio. Fix: downmix (or take the max-energy channel) to one mono mic buffer at capture, before RMS and before the gate, so there is exactly one channel to measure and to gate; the mic is published mono anyway, so nothing is lost. Acceptance: a synthetic 2-channel buffer with voice only on channel 1 both reads the correct dBFS and is fully silenced on a mute verdict. The leak half is a privacy defect; promote to P0 if you record on a multichannel input device. Rework-proof. Done: the mic WAV is opened in a mono Float32 format (`monoCaptureFormat`, same sample rate) and `processMicBuffer` collapses each tap buffer to one channel via `collapseToMono` (copies the highest-energy channel) before the RMS read and before the gate. Picking the loudest channel keeps the speaking channel's true level (the old all-channel average read low) and leaves exactly one channel for `MicGateWriter` to zero, so muted voice on channels 1+ can no longer survive into the stop-time mono merge. The device-change path downmixes through the existing `AVAudioConverter` for free now that the file format is mono, so `CaptureRecoveryPlanner` was untouched. Removed the now-unused `AVAudioPCMBuffer.deepCopy` extension and its three tests (the mono collapse is the mic path's only owning copy). Added three `collapseToMono` tests: correct dBFS on a voice-on-channel-1 buffer (-6.02 dBFS, not the diluted average), the single-channel anti-leak invariant, and mono passthrough is owned. Build clean; recorder suite 16/0.

**[DONE] TECH-MIC4 (P0 new): lossless capture + retention-based privacy (the cure).** The fix for the broken core promise (your spoken voice is lost). Stop destructively zeroing the mic at capture in the default path: capture the mic ungated into the recording, exactly as Notion and Granola do, so a wrong or missing mute read can never delete your voice on any tool. This is the cure for the cross-tool goal precisely because it needs no mute oracle at all. Then manage default-mode privacy by the retention policy MIC1 ratifies rather than by gating (today meeting-pipe keeps the WAV indefinitely and the Library Audio tab plays it, so making audio ephemeral / opt-in-retained is a behaviour decision, not a given). Thread `captureMode` as a required, fail-closed argument from `beginRecording` ([MeetingSessionController.swift:521](../../daemon/Sources/MeetingPipe/MeetingSessionController.swift)) through `recorder.start` with no defaulted value, so every call site (manual recordings, `OrphanRecordingRecovery`) must choose and an ambiguous mode takes the safe path. Depends on MIC1. Acceptance: across Teams native, Zoom, a Google Meet browser tab, and a manual recording, speaking with the meeting app muted or unmuted never produces a silent mic span in the transcript; regulated/NDA still produces no audio at rest (it takes the MIC6 gated path, not lossless capture). Done: added `CaptureMode` (`.captureFirst` / `.regulatedGate`) with a pure `resolve(regulated:nda:)`, threaded as a required no-default argument from `beginRecording` (resolved from the global regulated flag plus the workflow NDA flag) through `recorder.start`. Under `.captureFirst` the mic is never zeroed: `processMicBuffer` skips `MicGateWriter.apply` and instead records the muted spans (`verdict.indicatesMute`, so quiet-but-unmuted speech is never marked muted) into a `MuteTimeline`, written at stop as `<stem>.mute-timeline.json` (`MuteTimelineFile`, daemon-internal, for the MIC5 redactor). Under `.regulatedGate` the existing real-time zeroing stands (no audio at rest, no timeline). The HUD meter shows the live level under capture-first and the gated level under the regulated path. The mic-file frame clock that timestamps the timeline is advanced across device-change silence pads so it stays aligned. `OrphanRecordingRecovery` is unaffected (it post-processes existing intermediates, never calls `recorder.start`); an orphan recovered without a stop has no timeline and is consumed losslessly. Added `MicGateVerdict.indicatesMute`. Tests: CaptureMode resolution (5), MuteTimeline coalescing/finalize/sidecar round-trip and snake_case keys (9), indicatesMute (1). Updated the README "Why it is shaped this way" with the capture-first bullet pointing at ADR 0016. Build clean; full suite 763/0. Note: the redaction that consumes the timeline is TECH-MIC5; between MIC4 and MIC5 the default is lossless-without-redaction (the Granola model), so muted side-comments are transcribed until MIC5 lands in this same series.

**TECH-MIC5 (P2 new): optional offline mute-redaction layer (the privacy differentiator).** For users who want "my muted side-comments never reach the summary" without the real-time gate's data-loss risk: at processing time, redact the muted spans from the consumed artifact using the recorded mute timeline (MIC6/MIC7), as a zero-fill of sample ranges on the mic channel preserving the ADR 0009 L/R parity, applied to the canonical WAV before any consumer runs (the daemon FluidAudio at SinkDispatcher.swift:81 and the Python pipeline; any raw working copy carries a distinct suffix no glob in `orchestrate.py` / `publish_obsidian.py` / FluidAudio dispatch resolves, and is reaped). Add the confidently-unmuted rule here and in any gate path: a confident `axMute == .unmuted` keeps audio regardless of RMS (today `decide` has no such floor, MicGate.swift:173, so a quiet-but-unmuted moment can be dropped). Note the `diarize.py` channel-aware fallback compares absolute int16 RMS tuned to gated audio (`dominance_ratio`, `min_other_rms`), so it must read the redacted artifact, never a full-amplitude raw copy, or it flips muted-user windows to USER_SPEAKER. Offline and recoverable, so a wrong timeline mis-redacts rather than destroys. Opt-in; default-on under regulated/NDA only if the user accepts transient audio-at-rest during processing (otherwise regulated keeps the MIC6 gate).

**TECH-MIC6 (P1 new): futureproof and repair the native (Teams/Zoom/Webex/Slack) mute oracle.** The verified proximate cause, and the signal MIC5's redaction and the regulated real-time gate depend on. Four changes: (1) Futureproof the read, the user's explicit ask: the probe today scrapes the localized button title (`defaultProbe` reads kAXTitle/kAXHelp/kAXDescription recognized against `MuteLabels.toml`), which breaks on locale and on any UI text change; prefer the button's stable state attribute (AXValue / pressed state / mark-char) where the app exposes it, so the read survives UI redesigns and locale. Capture an AX dump of the current Teams/Zoom controls first (the `daemon/scripts` ax-dump tool) to choose the durable attribute. (2) Re-arm the dead element: `AXMuteButtonProbe` caches one element and never re-resolves it, so when Teams 2 re-renders or shows "Mic is not available" the read latches `.unknown` forever (11,007 `ax_mute_button_state_kept` over 19 days); on N consecutive `.unknown`, re-resolve via a fresh tree walk rather than fall through, to preserve the deliberate ~90 s call-setup anti-leak latch the comment at AXMuteButtonProbe.swift:155-162 documents. (3) Fix the dead blind-clear: it fired 0 times because it is gated on the watcher's own `lastEmitted == .muted`, which is only set from a confident fresh read, so when the live control is unmatchable the rescue is structurally unreachable (MeetingAXWindowWatcher.swift:142-167); decouple it from `lastEmitted` and treat a stale or all-windows-fallback `.muted` as non-confident. (4) Verify the `uk` Leave/Mute labels while here (the user runs en/uk). Under MIC4 this is no longer load-bearing for default-mode data loss, but it is the fix for the regulated/NDA path (which keeps the destructive gate) and for MIC5's redaction accuracy.

**TECH-MIC7 (P2 new): browser/PWA mute oracle via a stable platform signal (Google Meet first).** Browser meetings have no AX mute oracle (`NoOpMuteAdapter` emits nothing), so today every browser meeting is energy-gated only and MIC5's redaction would degrade to an RMS guess there. The futureproof, tool-scoped approach the user blessed: read the meeting platform's own mute state from the page (a small browser extension or DOM probe reading the platform's `aria-pressed` / data-attribute / exposed JS state for the mute control), which is more stable across UI changes than scraping a native AX tree and is the only real mute signal a browser surface offers. Scope per platform, Google Meet first (largest browser-meeting share), then Teams-web / Webex-web. Only affects browser redaction accuracy; MIC4 already removes the data-loss failure mode for browsers. Net-new surface (an extension is a new artifact and a new permission); gate it behind the same consent/regulated rules. Spike the Meet DOM signal's stability before committing.

**TECH-MIC8 (P3 new): spike UI-independent native mute signals (logs / SDK).** The most futureproof native oracle would not touch the UI at all. Investigate whether Zoom's local client logs or its SDK, or a Teams local signal, expose mute-state transitions that survive every UI redesign (the user's strongest futureproofing bar). Research only: confirm availability, latency, and stability per app; if a durable signal exists for a tool, it supersedes that tool's AX read in MIC6. Promotion trigger: MIC6's AX approach proving fragile again after a vendor UI change.

### Security and privacy

**[DONE] TECH-SEC2 (P0): close the regulated-mode Notion egress.** Under global regulated mode with the default `notion` sink, `orchestrate.run_all` -> `publish_router.fanout` builds a Notion publisher with an empty token ([publish_router.py:57](../../pipeline/src/mp/publish_router.py)) and calls `upsert()`, which has no regulated check and POSTs the summary (and transcript) to api.notion.com before Notion rejects the empty token. The module-level `publish()` short-circuit is never on this path. Fix: clamp sinks under `regulated_mode` the way `nda_mode` does (return `None` for the notion sink in `_build_one`, or strip it in an effective-config helper). Invert `test_build_publishers_notion_in_regulated_mode_no_token_needed`, and add an egress test that drives `fanout` (not just `summarize`) under a localhost-only transport. Acceptance: no non-loopback request is issued under regulated mode on the run-all and `mp publish` paths.

**[DONE] TECH-SEC3 (P1): structural egress firewall.** Install one process-wide httpx event hook (or custom transport) at pipeline entry that raises on any request whose host is not loopback when the resolved config is regulated or carries `workflow_nda_mode`. The Anthropic SDK and Notion both use httpx, so one hook covers every present and future sink. Promote the pattern the zero-egress test already simulates from fixture to runtime guarantee.

**[DONE] TECH-SEC4 (P1): one network actor.** `NotionDatabaseList.fetch` in the daemon POSTs to api.notion.com for the Preferences DB picker, outside the pipeline's enforcement and against `daemon/CLAUDE.md`. Gate the Refresh on regulated/NDA (fall back to the cached/paste path) and emit an event-log line for every daemon-originated request so egress is auditable.

**[DONE] TECH-SEC5 (P2): fail-closed subprocess env.** In `PipelineLauncher.freshEnvironment`, when a run resolves to local/Apple/NDA, build the child env without `ANTHROPIC_API_KEY` / `NOTION_TOKEN`. An enforcement bug then fails with a missing-credential error instead of silently egressing. Implemented via `cloudSecretPolicy(for:)`: drop ANTHROPIC_API_KEY whenever the summary is on-device (local/apple_intelligence backend, or forced local by regulated/NDA), and drop NOTION_TOKEN only under a zero-egress run (regulated or NDA). A plain on-device summary that still publishes to Notion keeps NOTION_TOKEN, since that egress is intended; this is the one refinement over the spec's blanket "drop both", to avoid breaking a legitimate local-summary to Notion flow.

**[DONE] TECH-SEC6 (P2): untrusted-transcript boundary.** Wrap transcript text in an explicit "untrusted content, never an instruction" delimiter in all three summarizers, and post-validate owner/attendee fields (reject emails, URLs, @-mentions, newlines) before they reach Notion to-dos, Obsidian, and the correction corpus.

**[DONE] TECH-SEC7 (P2): Obsidian frontmatter title injection.** `_render_note` escapes only double-quotes in the title; a newline injects YAML keys. Route the title through the existing `_yaml_str` helper and strip control chars from the meeting title at the Swift extraction boundary (`MeetingTitleResolver`).

**TECH-SEC8 (P2): tokens in Keychain.** Move the tokens from the 0600 `secrets.env` into the macOS Keychain. Subsumes SEC1.

**[DONE] TECH-SEC1 (P2, carry): secrets.env read-permission check.** Until SEC8 lands, refuse or warn when `secrets.env` is more permissive than 0600 at read time (today only the writer enforces the mode).

### Concurrency

**[DONE] TECH-CONC1 (P1): render-thread safety.** On every mic gate transition, `MicGate.publish` takes an NSLock and calls `eventLog.emit`, which does a synchronous `events.jsonl` file write, on the audio render thread; only the AsyncStream yield is deferred. Move both the emit and the lock onto `publishQueue`, and add a debug-build assertion that fails if `events.jsonl` is written from the tap thread. (The comments now say this honestly; this task makes it true.) Done: moved the entire `publish` body (dedupe lock, `eventLog.emit`, yield) onto `publishQueue` via `async [weak self, continuation]`; tagged `publishQueue` with a `DispatchSpecificKey` and added a `#if DEBUG` assertion in the emit block (no reliable render-thread predicate exists, so the assertion is a positive "must be on publishQueue" check at the only render-thread emit path). Fixed the now-false threading comments in `MicGate.swift` and `Coordinator.swift`. Updated the emit test to wait for the async drain and added `test_emit_runs_off_the_calling_thread`.

**[DONE] TECH-CONC2 (P2): strict-concurrency island.** Enable Swift 6 strict concurrency for `MeetingPipeCore` (already nearly Sendable-clean) and the recorder's shared verdict/level fields, rather than a wholesale migration. Converts the riskiest prose invariants into checked ones. Done, with one deviation named up front: the spec assumed the core was "nearly Sendable-clean," but `complete` strict concurrency surfaced 40-60+ warnings across the whole DI-seam pattern (every signal/adapter's immutable `static let` config + probe/scheduler closures = `#MutableGlobalVariable` / `#SendableClosureCaptures`). Driving those to zero IS the wholesale migration the task scopes out, so instead I set the core target to **`targeted`** strict concurrency (`-strict-concurrency=targeted` via `swiftSettings`), which checks Sendable across the real concurrency boundaries (the `MicGate`/lifecycle verdict `AsyncStream`s, Task captures) and is **clean (0 warnings)**, without flagging the benign immutable globals. Then made the actual cross-thread invariant explicit + checked: `MicGateVerdict` (and its nested `Reason`) now conform to `Sendable`, since that enum crosses the verdict stream and is latched into the recorder. For the recorder's shared fields (the executable side, outside the island), replaced the lock-free `currentMicGateVerdict` + `latestMicLevelDb` with one `OSAllocatedUnfairLock`-guarded `GateAccess` struct: the render thread reads the verdict + writes the HUD level under a single uncontended acquisition, closing a real torn-read hazard (a non-`.hot` verdict carries `String` payloads, so a torn read mid-write is use-after-free, not a benign stale sample). `complete`-mode adoption for the whole core stays a separate, larger pass (it needs Sendable conformances + `@Sendable` DI-seam typealiases that ripple into the test target). Used `.unsafeFlags` for the mode (acceptable: `MeetingPipeCore` is a local target, never consumed as a published dependency). Build clean, full suite 711/0 (the MicGate integration tests drive `setMicGateVerdict`/`debugCurrentMicGateVerdict` through the new lock).

### Performance

**[DONE] TECH-PERF1 (P1): stream diarize.py.** Replace the single `sf.read` plus two `astype(float32)` copies with per-segment `sf.read(start=, stop=)`, computing RMS per window. Cuts the ~2 GB peak on a 3-hour stereo file to a few MB, exactly in the degraded fallback path you least want to also OOM. Done: rewrote `assign_speakers_by_channel` to open a `soundfile.SoundFile`, scan the right channel block-wise for the silence gate, then `seek`/`read` per segment. Kept `dtype="int16"` with a local `.astype(float32)` on each bounded slice so per-segment RMS is identical and `min_other_rms`/`dominance_ratio` keep their int16-magnitude meaning. Added a clamp-path test; the existing channel-aware suite stays green.

**[DONE] TECH-PERF2 (P1, carry of A13 RAM half): FluidAudio memory.** The final WAV is already 16 kHz mono; read it into a mono buffer directly for the common case and scope the input PCM buffer so it is freed before `asr.transcribe` / `runDiarization`. Halves the readMonoFloat32 peak. (The waveform half of A13 shipped; this is the deferred RAM half.) Done: scoped the full-clip `AVAudioPCMBuffer` inside `readMonoFloat32` to a closure so it is released the moment the mono `[Float]` exists, before `resampleMono` allocates or the function returns. The `samples` array itself legitimately spans ASR + diarization (both consume it sequentially), so halving that would need a re-read and is out of scope; this is the input-buffer half the spec calls out. Added end-to-end `readMonoFloat32` round-trip tests (mono passthrough + stereo mix), which had no direct coverage before.

**[DONE] TECH-PERF3 (P2): vectorize render-thread RMS.** Use `vDSP_svesq` for sum-of-squares once per mic buffer and feed both the gate dBFS and the ~1 Hz accumulator from it, removing the second scalar pass. Keep it allocation-free. Done: added a static `MeetingRecorder.sumOfSquares(_:)` that runs one `vDSP_svesq` per channel (allocation-free, `Double` accumulation across channels so the ~1 s average keeps precision), `import Accelerate`. `processMicBuffer` now computes the buffer's sum-of-squares once, derives the gate dBFS + the HUD VU level from it, and passes the SAME `(sumSq, samples)` into `accumulateAndEmit`; that method lost its inner per-sample loop and just folds the precomputed delta. The system tap routes through the same helper (it was the system path's only pass, now vectorized too). Added three `sumOfSquares` tests (scalar-reference parity, multi-channel summation, the -6.02 dBFS half-amplitude check). Suite 711/0.

**[DONE] TECH-PERF4 (P2): dismiss-ring timer.** Replace the 60 fps `Timer` in `DismissProgressView` with `TimelineView(.animation)` or a single CA keyframe so the CPU idles between frames. Done: `DismissProgressView` is an AppKit `NSView` (CALayer hairline), so the CA-keyframe path fits, not SwiftUI's `TimelineView`. Replaced the 1/60 s `Timer` + `tick()` with one `CABasicAnimation` on the fill layer's left-anchored `transform.scale.x` (1 -> 0 over the timeout, linear, `fillMode = .forwards`), driven on the render server so the main thread no longer wakes per frame. Pause/resume (panel hover) now uses the standard CALayer `speed`/`timeOffset` freeze instead of recomputing elapsed each tick, and dims to 30% as before. Dropped the timer + elapsed-tracking state; the public `start`/`stop`/`setPaused`/`isPaused` surface is unchanged, so `MeetingPromptWindow` is untouched. Cosmetic, no test; build green.

**[DONE: polls] TECH-PERF5 (P3): poll backoff.** Back the 1 Hz HAL/AX poll fallbacks off to ~0.2 Hz once the listener has delivered recently, and gate the 4 Hz engine tick on a pending debounce. Done (polls): added `AdaptivePollCadence` (clock-free; tracks whether the listener fired since the last poll, returns slow=5s while it is delivering, fast=1s once it goes quiet) and wired it into the three fixed-1 Hz fallbacks, `ProcessAudioSignal`, `AXLeaveButtonSignal`, and `AXMuteButtonProbe`, mirroring `ShareableContentSignal`'s self-rearming `startPoll` pattern. Thread-safety: the listener path only flips one Bool (`noteListener()`, same single-writer discipline the signals already use for `lastValue`); the interval decision and the timer re-arm happen on the poll callback's thread alone, so no timer is ever scheduled from the off-main HAL listener thread. Safe by design because the leading end signal (`ShareableContentSignal`, window-gone) keeps its own fast 0.5/1 Hz cadence and is untouched, so end-detection never depends solely on a backed-off poller. Unit-tested (`AdaptivePollCadenceTests`, 5 cases); full Swift suite 743/0. **Deferred, named:** the 4 Hz engine-tick gating. It needs `PromotionEngine` to expose pending-deadline state and the coordinator to arm/disarm the tick on engine-state changes, which happen on `engineQueue` while the timer lives on main, i.e. a cross-thread timer-lifecycle change to the detection hot path, exactly the unverifiable risk the binding constraint rules out for a vibe-coding user. The poll backoff (three 1 Hz pollers -> 0.2 Hz) is the larger wakeup win; the engine-tick gating is a separate, carefully-scoped follow-up.

### Architecture

**[DONE] TECH-ARCH1 (P1): effective_config chokepoint.** Add one `effective_config(cfg)` (or `effective_backend` / `effective_sinks`) in `config.py` that applies the regulated/NDA forcing once, call it at the single entry of orchestrate/summarize/diarize_cleanup/publish, and delete the four-plus inline copies. Preserve the intentional apple_intelligence/auto differences per call site. This is the change that most reduces future egress drift and is the natural home for the SEC2 sink clamp. Done: added `effective_backend` + `effective_sinks` to `config.py` (keyed on `regulated_mode or workflow_nda_mode`), routed `summarize._select_backend`, `orchestrate` (deleted its local `_effective_backend`), `diarize_cleanup._select_cleanup_backend`, and `publish_router.build_publishers` through them; deleted the inline copies and the SEC2 notion clamp from `_build_one`. Left the overlay-time NDA policy in `workflow.py` and the legacy `publish_notion.publish` / `publish_from_paste` guards as-is (each does something the helpers do not). Added `effective_backend`/`effective_sinks` unit tests and a `workflow_nda_mode` drop-notion variant.

**[DONE] TECH-ARCH2 (P2): MeetingSessionController.** Extract the two verdict-consumer Tasks, engage/disengage, silence-detector arming, and meta-sidecar writing into a session controller that owns one meeting's lifetime. Coordinator drops to wiring plus the three UI-delegate conformances, under ~700 lines. Closes H1-FINISH. Done: added `MeetingSessionController` (638 lines) owning the per-meeting state (`activeWorkflow`, `pendingWorkflowOverride`, `silenceDetector`, `axWindowWatcher`, `latestSystemLevelDb`), the two verdict-consumer Tasks (`startConsumers`/`shutdownConsumers`), the begin/stop/prompt-timeout/silence/MicGate-engage/lifecycle methods, and the detection-to-record handlers; it reaches subsystems through an `unowned coordinator` backref, so Coordinator stays the subsystem + UI-wiring owner (the lowest-risk faithful extraction: bodies moved verbatim, only subsystem references reprefixed, so the compiler validates the rebinding). `Coordinator.swift` 542 -> 482; wiring + the three delegate conformances total ~641, with menu forwarders (147) and the live-config readers (52) the rest of Coordinator's surface; the meeting lifetime is now its own type. No behaviour change; build green, full suite 708/0. This is the real decomposition TECH-H1-FINISH deferred (it hit the line-count bar with a safe file-split and left ARCH2 open); the residual hot-path risk the user accepted is mitigated by the verbatim move + the full suite (the Coordinator wiring itself has no integration test, by design).

**[DONE] TECH-ARCH3 (P2): PipelineLauncher dedup.** Collapse the runAll/publish/summarize subprocess blocks onto `runMP` (lowest-risk first: `summarize`, which differs only in timeout). Removes ~270 lines of duplicated scaffolding. Done: generalized the private `runMP` into the single spawn primitive by adding `onProgress` (the stdout heartbeat scan), `retainForCancel` (the `setActiveRunProcess` retention for TECH-UX5 Cancel), and `extraEnv` (the `MP_FORCE_BYO` overlay); folded all three public methods onto it, each keeping only its own success-path sidecar read. Extracted a shared `readPageURL(from:)` for the `<stem>.notion.json` page-url read used by run-all, publish, and the Apple-Intelligence publish hand-off. `PipelineLauncher.swift` went 831 -> 601 lines (230 removed). The watchdog SIGTERM->SIGKILL grace is now a uniform 30s (publish was 15s; immaterial on the timeout path). Static helpers (`freshEnvironment` / `cloudSecretPolicy` / `parseProgress` / `secretsFileIsTooOpen`) are untouched, so the PipelineLauncherTests stay green; full suite 708/0.

**[DONE] TECH-ARCH4 (P3): chunker golden vectors.** A checked-in input/expected-windows fixture exercised by both the Swift `TranscriptChunker` and Python `chunked_windows` suites, so the "must stay identical" comment is enforced by CI. Done: `daemon/Tests/MeetingPipeTests/Fixtures/chunking-golden.json` (generated from the Python primitive so it is authoritative; three cases: ASCII overlap, single-window, and a Ukrainian/Cyrillic overlap case to pin precomposed-codepoint parity). The Swift `TranscriptChunkerTests.test_golden_vector_parity_with_python` loads it via `Bundle.module`; the Python `test_chunking.py::test_golden_vector_parity_with_swift` reads the same file across the tree and asserts identical windows. Both green (Swift 7/0 for the suite incl. the Cyrillic case; Python 12/0). Regenerate from `chunking.py` if the algorithm ever changes.

**[DONE] TECH-H1-FINISH (P1, carry):** the under-600-line bar is unmet (Coordinator is 1371). Subsumed by ARCH2; track here until that lands. Done: hit the bar with a **pure file-split** (chosen over the riskier ARCH2 type-extraction). `Coordinator.swift` is now 542 lines: it keeps the stored properties, `init`, `wireSubsystems`, `start` (the two verdict-consumer Tasks), `shutdown`, the startup helpers, and the lazy stores. The methods moved verbatim into three same-type extension files, `Coordinator+Session.swift` (recording lifecycle: begin/stop, sidecar, prompt-timeout, silence, mic-gate, lifecycle, meeting-start/end handlers, live-config readers), `Coordinator+MenuActions.swift` (menu actions + MeetingLibraryService forwarders), and `Coordinator+Delegates.swift` (the three UI-delegate conformances). The only logic-adjacent edits were mechanical: `private` -> internal on the members touched across files (stored props, the lazy stores, `LogEventAdapter`, the moved funcs). Zero behavior change; build green and the full 699-test suite passes. **TECH-ARCH2 stays open** as the real decomposition (extract a `MeetingSessionController` type that owns the meeting lifetime); the binding constraint (no unverifiable hot-path risk for a vibe-coding user) made the file-split the right call for the line-count bar.

### Workflow editor

**[DONE] TECH-WF1 (P1): backend unification + inherit.** The per-workflow backend enum is `{anthropic, local, auto}` and the sidecar always stamps it, so a matched workflow always overrides the global backend, and global "Apple" is unreachable for any normal meeting. The Python overlay already allowlists `apple_intelligence`. Add `appleIntelligence` to `WorkflowBackend` and the editor picker (with the availability gating Preferences uses), add a "use global default" case, and only write `workflow_backend` when the workflow pins a non-default backend. Fixes the unreachable-Apple bug and the silent override, and corrects the "mirrors summarization.backend" docstring. Done: added `appleIntelligence = "apple_intelligence"`; made `Workflow.backend` optional with `nil` = inherit (new-workflow default, and the migrator now seeds the fallback "General" as inherit so global Apple is reachable out of the box); switched the editor picker to `.menu` with a "Use global default" row and the Apple row plus the `AppleIntelligenceSummarizer.availabilityReason` caption Preferences uses; sidecar/TOML omit `workflow_backend` when inheriting (NDA still forces local); `WorkflowInspector` shows "Global default" for nil. Python read side already accepted apple + a missing key, so only the docstring + CONVENTIONS row changed. Swift suite 699/0; added inherit/Apple cases to the sidecar, store, and overlay tests.

**[DONE] TECH-WF2 (P2): emoji picker.** Replace the free `TextField("optional")` (width 80, accepts any text) with a real emoji picker (system Character palette via `orderFrontCharacterPalette`, or a curated grid) constrained to one grapheme. Done: the Emoji row now pairs a "face.smiling" button that opens the system emoji & symbols palette (`NSApp.orderFrontCharacterPalette`) with the field, and an `onChange` runs `constrainToOneEmoji` so the field holds exactly one grapheme (keeps the last typed/picked, so a fresh pick replaces the old; a ZWJ family or flag counts as one cluster). Added `WorkflowEmojiConstraintTests` (empty, single, last-of-several, grapheme-cluster, whitespace-trim). Suite 716/0.

**[DONE] TECH-WF3 (P2): color picker.** Replace the `#RRGGBB` hex field with SwiftUI's native `ColorPicker` or a token-aligned swatch palette; keep hex as an advanced fallback. Done: the Color row now leads with a native `ColorPicker` (its built-in swatch replaces the WF4 placeholder ring), bound via a `colorBinding` that parses the model's hex for the swatch and writes an sRGB `#RRGGBB` back on pick. Added the inverse helper `HexColor.hexString(from:)` (sRGB components -> upper-case hex) next to the existing `parse`. The monospaced hex field stays inline as the advanced fallback for paste / power users. Build green.

**[DONE] TECH-WF4 (P2): identity-section layout.** Fix the ragged alignment in the Identity section (the color row ends in a Spacer, emoji is width-80, name is full-width); give the rows a consistent field width. Done: dropped the stray `Spacer`, gave the Color and Emoji controls a shared `identityFieldWidth` (150) so they line up, and moved the colour preview to a `colorSwatch` left of the hex field (a muted placeholder ring when the hex is empty/unparseable, so the row height never jumps). Name stays full-width as the primary field. Build green.

**[DONE] TECH-WF5 (P2): discoverability.** Workflow editing is only reachable by selecting a workflow filter-scope in the Library rail and clicking an unlabeled pencil. Add a labeled "Manage Workflows ..." entry (menu and/or a dedicated surface). Overlaps DSN1. Done: added a "Manage Workflows ..." item to the status-bar menu (after Quick Find), wired through a new `Coordinator.menuManageWorkflows` to a `LibraryWindowModel.pendingOpenNewWorkflow` flag the `LibraryWindow` observes (mirroring the existing `pendingSelection` pattern): it opens the Library and presents the workflow editor sheet, with the rail behind it listing the existing workflows for edit. The status-bar menu is the app's primary menu (LSUIElement, no `NSApp.mainMenu`), so that is the findable top-level home. Note on the spec's premise: the rail pencil is in fact already labeled "Edit workflow"; the real gap was the absence of any top-level entry, which this closes. Build green, suite 716/0.

### Design and UX

**[DONE] TECH-DSN1 (P1): Preferences IA.** Seven panes, ~44 controls. Collapse the local-MLX cluster (model id, endpoint, active model, preload, custom preset) behind a "Configure local model ..." disclosure or into Advanced; cut the two cosmetic toggles; move regulated mode out of the Prompt pane (it is a privacy/publishing concept). Pairs with UI-X2. Done: added a `SettingsDisclosure` primitive and collapsed the five local-MLX controls behind a "Configure local model" disclosure (backend picker stays visible); cut the two cosmetic toggles (the menu-bar icon outline/filled segmented control in General and the "Show menu-bar lock" toggle); moved the Regulated-mode group from the Prompt pane into Permissions (the privacy/egress surface). All seven panes remain, so PreferencesSelectionStateTests stays green.

**[DONE] TECH-DSN2 (P1): detail-pane tabs + republish.** Reduce the five detail tabs to three (drop "Raw files", demote "Corrections" to the "..." menu). Define two verbs only (Republish, Reprocess) and expose each once per altitude; remove the inline row buttons and the "Save & Republish" compound. Done: tabs reduced to Summary / Transcript / Audio; Corrections moved to a sheet off the "..." menu (CorrectionsTab now takes an `onReedit` closure instead of the tab binding); Raw files dropped (RawFilesTab/Lister kept on disk + tested). The detail "..." menu now carries the two canonical verbs Republish + Reprocess (plus "Corrections..."); the editor's "Save & Republish" compound and its republish-feedback infra were removed, so republish is the single "..." menu / row affordance (the row needs-republish badge lights up after a Save). Row context-menu labels renamed to "Republish" / "Reprocess" for consistency. **Deviation:** kept the row "Regenerate" verb (inline button + context item). It is the BYO `manualPasteReady` re-summarize affordance backed by a distinct, tested service operation (`MeetingLibraryService.regenerateMeeting`, re-summarize without re-transcribe) rather than a redundant alias of Reprocess (full `run-all`); collapsing it would entangle the BYO flow and a deep tested chain, and is better done in a focused, visually-verified pass. Swift suite 699/0.

**[DONE: colors + guard] TECH-DSN3 (P2): token enforcement.** Replace raw `.green/.orange/.red`, `.accentColor`, and the 126 `.system(size:)` literals with token equivalents; unify to one button language; add a CI grep guard (like the em-dash guard) for raw colors and bare `.font(.headline)` outside Tokens.swift. Done: migrated every raw semantic color literal (`.red`/`.orange`/`.green`/`.accentColor`, ~24 sites across 9 views) to new `.mpDanger`/`.mpWarning`/`.mpSuccess`/`.mpSignal` tokens (defined on both `Color` and `ShapeStyle where Self == Color`, so `.foregroundStyle(.mpDanger)` resolves); tokenised the TranscriptTab speaker hues into `MPColors.speakerPalette` (same colours, same order, so assignments don't shift), as the spec's exempted multi-hue set; added the `design-tokens` CI job mirroring the em-dash guard: a **whole-file** color guard (zero now, blocks reintroduction) plus a **diff-based** font guard (blocks new `.system(size:)`). Build green, suite 718/0 (DesignTokensTests included). **Deviation, named:** the bulk rewrite of the 128 existing `.system(size:)` literals is **deferred**, not done. `MPType` is a sparse CGFloat ramp (11/12/13/15/17/22/28...), so off-ramp sizes (14/16/18/20) have no exact token; many bare semantic fonts (`.caption`/`.headline`) are Dynamic-Type-aware and a fixed `MPType` size would silently downgrade accessibility; and a 128-site blind rewrite is exactly the unverifiable sub-pixel visual churn the binding constraint rules out for a vibe-coding user. The diff-based guard stops new font drift; a focused, visually-verified font pass (and the "one button language" unification, which also needs eyes on it) is the right follow-up. Colors, the speaker palette, and the guard, the durable enforcement, all landed.

**[DONE] TECH-DSN4 (P2): identity color.** Choose a signal hue that is unmistakably MeetingPipe and survives the dark-mode auto-flip. Pairs with REPO2. Done as part of TECH-REPO2: teal `#0E8C82` (ramp 700 `#0A6F67` / 600 `#0E8C82` / 500 `#14A89B` / 400 `#4FC7BC` / 100 `#DCF1EF`), the same value in light and dark with the `signal-100` tint overridden to `rgba(14, 140, 130, 0.18)`; contrast-checked on the light (`#FBFAF7`) and dark (`#1A1B1E`) canvases and kept clear of the success/warning/danger/pulse hues.

**[DONE] TECH-DSN5 (P2): motion, haptic, sound.** Animate exactly three moments (HUD degraded grow/shrink, prompt fade-in, Library row-selection settle); add a Stop-button trackpad haptic; add one opt-in, default-off, post-call completion tone. Never an in-call chime, no shimmer, no skeletons, no bounce. Done: the first two animations already existed; added the third as a `.easeOut(MPMotion.durFast)` settle keyed on the Library `selection` so the highlight eases rather than snaps. The HUD `StopButton.mouseDown` now fires `NSHapticFeedbackManager.defaultPerformer.perform(.generic, .now)` (a firm detent for the consequential action; a no-op without a Force Touch trackpad). Added one opt-in, default-off completion tone: a new `UISettings.playCompletionTone` (UserDefaults-backed) gates an `NSSound(named: "Glass")` played from the pipeline `onDone` wiring (the summary is ready), never during a call, and separate from the done notification's own sound (which Focus can suppress); exposed it as a "Sound" group toggle in Preferences. Build green, suite 718/0.

**[DONE] TECH-DSN6 (P2): persisted NDA badge.** Persist the resolved NDA/regulated flag to the sidecar and drive the Library row badge from it; delete the heuristic guess in MeetingRow. A privacy badge must never be inferred. Done: the sidecar already carried `workflow_nda_mode` (the workflow NDA), but not the global half of the resolved flag, so `MeetingMetaSidecar.build` now also writes `regulated_mode` (the global flag at record time, passed from `configStore.regulatedMode`, written top-level so even a manual workflow-less recording badges). The `Meeting` model reads both into `workflowNDAMode` / `regulatedMode` with an `isZeroEgress` accessor, and `MeetingRow.isNDA` reads that instead of the old `backend == "local" && workflow set` heuristic (deleted, with its stale "sidecar doesn't yet persist" comment). Per the single Swift<->Python sidecar contract, the Python reader (`apply_overrides`) folds the sidecar's `regulated_mode` in fail-closed (a reprocess of a regulated meeting stays on-device + filesystem-only even if the global flag later flips), with the forcing block now keyed on `zero_egress = nda or regulated`. Documented the key in `CONVENTIONS.md`; added Swift sidecar tests (top-level write, omitted-when-false) and a Python overlay test (regulated forces local+filesystem). Swift 718/0, Python 263/0, ruff clean.

**[DONE] TECH-DSN7 (P2): dead placeholder + menu-bar title.** Show the real elapsed time in the recording pill (the HUD already tickers it) or drop the slot; collapse the multi-clause menu-bar title to one clause. Done: dropped the dead `"-:-"` slot from the Library recording pill (the separator + the placeholder Text + the `elapsedString` property), so the pill now reads dot + "Recording" + the workflow chip; live elapsed already tickers in the HUD, and wiring a 1 Hz counter into the toolbar would re-render the whole rail for marginal value (Simplicity First). For the menu bar, `applyTitle` now shows exactly one clause instead of stacking up to four: the base state is the clause, and only while genuinely idle (new `isIdleState` flag, false during recording/prompting/stopping) does background work take its place, by priority (a model download, then the processing queue), since both have their own surfaces (the download menu row, the Library). The regulated lock stays as a single trailing glyph, not a clause. Build green, suite 716/0.

**[DONE] TECH-DSN8 (P3): summary reading polish.** Treat the Summary tab as the app's "paper" moment: measured line length, the MP type ramp, generous vertical rhythm. Done in `SummaryRenderedView`: capped the prose to a readable measure (`readingMeasure` 640pt, left-aligned in the pane via the outer infinity frame) so summary lines no longer run the full pane width, the single biggest reading win; and put the vertical rhythm on the `MPSpace` ramp (`s6` between sections, `s5` page padding, `s3` title-to-content, `s2` between bullets / action rows, `s1` action-row internals), snapping the off-ramp 10/6 gaps generous. **Deviation, named:** I did *not* convert the fonts to fixed `MPType.system(size:)` sizes. TECH-DSN3 deliberately kept Dynamic-Type-aware semantic fonts over fixed sizes (accessibility + sub-pixel risk) and shipped a CI guard that blocks new `.system(size:)`, so the "MP type ramp" here is delivered as the existing coherent Dynamic-Type semantic ramp (headline section titles over body prose over caption chips), not fixed sizes. Build green; layout-only, no test assertions on spacing (snapshot coverage arrives with TECH-T2).

**TECH-DSN9 (P2): Preferences layout polish.** Surfaced in a live smoke test of the Q4 build: the Preferences panes (General, Recording, Prompt, Pipeline) read unfinished, with loose vertical spacing between groups and inconsistent element alignment (mismatched control widths, label/control baselines that do not line up). Integrations, Permissions, and Advanced already look clean, so this is a per-pane consistency pass, not a rebuild. Tighten the inter-group spacing to one consistent rhythm and align the trailing controls to a shared column using the existing `SettingsGroup` / `SettingsRow` / `SettingsSegmented` / `SettingsSlider` primitives (do not add parallel one-offs; extend the primitives if a gap exists). Not a regression from the batch (the DSN5 Sound group already matches the pattern). Natural to do alongside TECH-UI-X2 (the PreferencesView per-section split). Worth a Claude-Design review of one pane to set the target spacing/alignment before sweeping the rest. Done (audit + token-align; pixel retune deferred): audited all four "loose" panes after the UI-X2 split, every one already routes its controls through the shared `SettingsRow`/`SettingsToggleRow`/`SettingsGroup` primitives (no raw top-level controls, no stray Spacers), so the two concrete goals, one consistent inter-group rhythm and trailing controls on a shared column, are already satisfied structurally: the 168pt label column and the 22pt inter-group gap come from the primitives uniformly. The 22/14/168 spacing is also deliberate, `PreferencesControls.swift` documents it as mirroring the design prototype (`primitives.jsx`), so blind-tightening it would deviate from the design source of truth. Concrete change made: aligned the primitives' on-ramp spacing to the `MPSpace` token ramp where the value matches exactly (`SettingsSectionHeader` 16/4 -> `s4`/`s1`, `SettingsGroup` 8 -> `s2`, `SettingsRow` vertical 12 -> `s3`), zero visual change, so the primitives now reference the shared ramp instead of magic numbers. **Deferred, named:** the subjective pixel-level "feels loose" retune. It needs a Claude-Design review against rendered pixels (and a paired `primitives.jsx` revision) to set a new target, which the task itself flags ("worth a Claude-Design review before sweeping") and which the binding constraint rules out doing blind for a vibe-coding user, the same reasoning that deferred the DSN3 font sweep. Build green.

### Features

**[DONE] TECH-FEAT1 (P2): local-network sink.** A first-class LAN publisher: write summaries (and optionally audio) to a mounted SMB/NFS share with reachability checks, atomic writes, and credential/host config, instead of pointing the plain filesystem sink at a network mount. On-prem, no cloud metering; strengthens the regulated story. Additive (publishers are already a Protocol with fan-out). Done: added `LanPublisher` (`publish_lan.py`) implementing the `MeetingPublisher` protocol, registered as the `"lan"` sink in `publish_router._build_one`, plus a `LanCfg` (`mount_path`, optional `host`) on the config. It mirrors the filesystem sink's three files (reusing its renderer so they never drift) with two share-specific behaviours: a reachability check that refuses to write unless the target or its immediate parent already exists and is writable (it never creates the mount root, so a down share raises `LanUnreachableError` for `fanout` to record rather than silently writing to local disk), and atomic writes (sibling temp file + `os.replace`) so a watcher never reads a half-flushed file. Allowed under regulated mode (it is on-prem; `effective_sinks` clamps only the cloud Notion sink) and excluded under NDA (which forces filesystem-only). Audio attach left out of scope (the spec's "optionally"), matching the filesystem sink. Added `test_publish_lan.py` (write, idempotency, unreachable-raises + nothing-created-locally, no-temp-leftovers, router wiring, survives-regulated) and a README config entry. Ruff clean, suite 271/0.

**[DONE: lexical MVP] TECH-FEAT2 (P2): local semantic search.** On-device RAG over the transcript library using the MLX model already in-process: "ask my meetings" with no new egress surface. The single biggest reason to use this over a cloud tool. Done as the user-approved **zero-dependency lexical MVP** (a faithful vector RAG needs an embeddings dependency + index, deferred): added `mp ask <query>` (`ask.py`, registered in `__main__.py` + USAGE), a stdlib-only TF-IDF ranker (`re` tokenize, `collections.Counter`, smoothed `math.log` idf) over each meeting's `<stem>.summary.md` + `<stem>.md` transcript, printing the top-N meetings with a best-matching-line snippet (`--top`, `--dir`, `--json`). No new dependency, no new egress. Added `test_ask.py` (discover, skip-empty, ranking, no-match, snippet, empty-library, JSON). The true on-device semantic search (embeddings over the same library, reusing the MLX model, with a persisted vector index) is the named follow-up this scaffolds; documented `mp ask` + that follow-up in the README. Ruff clean, suite 278/0.

**[DONE] TECH-FEAT5 (P2): auto fallback on rate-limit.** `_AutoFallbackClient` catches only connection/timeout/auth errors; extend it to fall back to local on 429/500 so a busy Anthropic does not fail the whole run. Note the default backend is `anthropic` (no fallback at all), so consider defaulting installs to `auto`. Done: added `anthropic.RateLimitError` (429) and `anthropic.InternalServerError` (5xx) to the `_AutoFallbackClient.summarize` except clause, so once `_create_message`'s own tenacity retries are exhausted, a sustained busy-Anthropic drops to the local backend instead of raising. Added a parametrized test (both exceptions -> `last_used_backend == "local"`) with fake Anthropic/local clients. Left the default backend as `anthropic` deliberately: flipping the sole user's install to `auto` is a silent behaviour change better made as an explicit opt-in, so it is noted here, not done. Ruff clean, suite 265/0.

**[DONE: MVP] TECH-FEAT3 (P3): speaker enrollment.** Label the user's own voice once for reliable "me vs them"; builds on diarization cleanup. Done as the user-approved tested MVP slice: a one-time enrollment is just a display name in config (`summarization.user_label`); at finalization `diarize.label_me_speaker` stamps it on the user's own speaker, picked structurally without a voiceprint, the channel-assigned mic speaker (`speaker_user`) when present (the reliable stereo path, the mic channel is always the user, and it wins even when the user is not the most talkative), else the dominant speaker by spoken time as a best-effort fallback for FluidAudio's merged-audio labels or a mono recording. Wired into `orchestrate._finalize_streamed_transcript` (both the FluidAudio-labelled and channel-aware branches), so it always runs, no extra LLM call. `test_diarize.py` covers channel precedence, the dominant fallback, and the no-op cases (empty label, no usable speaker, already-labelled). Ruff clean, pipeline 301/0. The full voiceprint build (embedding capture + cross-meeting match) is filed as **TECH-FEAT3-VOICEPRINT** below.

**TECH-FEAT3-VOICEPRINT (P3 new, FEAT3 follow-up): full speaker enrollment.** Promote the FEAT3 MVP's channel/dominant heuristic to a real voiceprint: capture the user's speaker embedding once (FluidAudio's diarizer embeddings if its Swift API exposes them, else a dedicated enrollment pass over the mic channel) and persist it, then match it against each meeting's diarized speakers so "me" is identified by voice even on a mono / merged recording or when the user is not the dominant speaker. Open question carried from the FEAT3 scout: confirm whether FluidAudio surfaces per-speaker embeddings before committing to the embedding path.

**[DONE] TECH-FEAT4 (P3): cross-meeting action tracking.** Surface unresolved action items across meetings. Done as the stdlib-only `mp actions` MVP (`actions.py`, registered in `__main__.py` + USAGE), mirroring `mp ask`: scans every `<stem>.summary.json` for the `MeetingSummary.actions` the summarizer already extracts (ADR 0014), aggregates them across the library, filters by `--owner` / `--due-before` / `--min-confidence`, sorts soonest-deadline-first, and prints text or `--json`. No new dependency, no new egress. `test_actions.py` (discover, malformed-skip, filters, JSON sort, empty). Since the schema has no resolved/done flag, every extracted action counts as open; a done flag + the UI to set it is the named follow-up. README documents it next to `mp ask`.

### Distribution

**TECH-DIST1 (P2): bundle a runtime.** There is no drag-n-drop installer today; install needs Homebrew plus uv plus ffmpeg, and a clean Mac has no usable Python 3 (Apple removed it; the CLT shim is 3.9, below the required 3.11+). Bundle a relocatable Python (python-build-standalone) plus the pipeline wheels and a static ffmpeg into the app, and notarize. This is also what locked-down regulated Macs need, since they often cannot install Homebrew.

**[DONE] TECH-DIST2 (P3): Swift-port spike.** Evaluate porting the pipeline (now summarize + publish only) to Swift to drop Python entirely. The hard blocker is the local MLX summarizer (mlx_lm is Python; MLX-Swift LM tooling is less mature). Would moot DIST1. Overlaps TECH-I7. Done: wrote the evaluation at `docs/spikes/dist2-swift-port-of-the-pipeline.md` (a spike, no decision). Component-by-component, everything except the local MLX backend is a low-risk Swift rewrite over HTTP + file IO + the already-mirrored chunker; the local backend is the blocker (it would mean adopting and maintaining the immature MLX-Swift LM stack, putting the privacy differentiator at risk). Recommendation: defer the port, pursue DIST1 (bundle the runtime) near-term; reopen when MLX-Swift LM tooling is production-ready or Apple Intelligence proves out as the sole on-device summarizer (the TECH-I7 trigger). ADR 0007 stands until then.

### Distribution note on Python

The pipeline is already "summarize + publish only" (ADR 0007). The realistic options are bundle-the-runtime (DIST1, near-term, unblocks distribution) or port-to-Swift (DIST2, the clean endgame). Relying on system Python is not viable on macOS.

### Docs (finish the consolidation started in Q4)

**[DONE] TECH-DOC1 (P2): GLOSSARY into ARCHITECTURE.** Append the glossary as an ARCHITECTURE "## Glossary" section, repoint the five references (ARCHITECTURE self-link, CLAUDE.md, CONVENTIONS.md, daemon/CLAUDE.md, pipeline/CLAUDE.md, tech-task.md) to `ARCHITECTURE.md#glossary`, delete GLOSSARY.md. Done: appended the 33-term glossary as ARCHITECTURE.md `## Glossary` (em-dashes converted to hyphens to satisfy the added-line em-dash guard), repointed all six refs (ARCHITECTURE self-link to `#glossary`; CONVENTIONS.md, root + daemon + pipeline CLAUDE.md, and tech-task.md to `ARCHITECTURE.md#glossary`), deleted GLOSSARY.md.

**[DONE] TECH-DOC2 (P2): SPEC into README + ADRs.** Fold the surviving user-relevant rationale into a concise README "Why it is shaped this way" section, confirm the architectural why is covered by ADRs 0007/0008/0009, move the duplicated event-log and sidecar schema tables to CONVENTIONS as the single owner (replace the copies in ARCHITECTURE and SPEC with links), rewire the six SPEC references, then delete SPEC.md. Also fix SPEC's own stale content if any survives the merge (it documents three backends, the Whisper/sherpa stack, and a stale repo layout). Done: added the README "Why it is shaped this way" section (five load-bearing decisions, each pointing at ADR 0007/0008/0009; non-goals folded in); CONVENTIONS stays the sole owner of the event-log + sidecar schema tables and ARCHITECTURE's inline category dup was trimmed to a pointer; rewired all SPEC references (13 sites, not 6: ARCHITECTURE, README, root CLAUDE.md, design READMEs + onboarding JSX, and the stale source-comment section numbers in schemas.py/corrections.py/CorrectionWindow/CorrectionStore/Logger/meeting_apps.toml, repointed to ADR 0014/0015 + CONVENTIONS); deleted SPEC.md. The stale SPEC content (three backends, Whisper/sherpa, old layout) left with the file.

**[DONE] TECH-DOC3 (P2): trim the signal-fusion doc.** Reduce `docs/architecture/signal-fusion-and-mic-gating.md` to the durable "why" (signal classification, Webex ultrasound, Sequoia AX dropouts, zero-frame writer). Fix the stale file tree: the "actor" wording (both types are final class plus NSLock), the deleted InputDeviceSignal/CalendarContextSignal rows, the per-vendor adapter filenames (consolidated into NativeMuteAdapter/NoOpMuteAdapter and NativeLifecycleAdapter), the never-built InternalSpeechProbe, the Locale/ vs Resources/ path, the nonexistent Thresholds.swift, and the os_unfair_lock atomics description. Done: rewrote both file trees against the live code (final class + NSLock, PromotionEngine added, signals/adapters consolidated, Infra hoisted to the shared `MeetingPipeCore/Infra/`, TOML under `Resources/`), corrected the RMS/threshold/render-thread prose (init params on `RMSGateProbe`, allocation-free tap, lock + emit deferred to `MicGate.publishQueue` per CONC1), dropped the never-built InternalSpeechProbe paragraph, and removed the done-task "Backlog updates" section that carried the stale filenames and a nonexistent module. 245 -> 200 lines.

**[DONE] TECH-DOC4 (P3): delete Q2 archives, fix design READMEs.** Remove q2-final.md and q2-ui-addendum-final.md (git preserves them; the addendum is also written against a path layout that never existed). Trim the deleted-architecture prose (two-signal AND, WhisperX, three-tab Preferences) from design/README.md and design/ui_kits/macos_app/README.md. Done: git-rm'd both Q2 archives and repointed the active backlog header; rewrote design/README.md "Architecture in one breath" (verdict fusion not two-signal AND, stereo WAV not mono, on-device FluidAudio not Python WhisperX, plus the local backend + multi-sink) and the Preferences surface (seven panes), and fixed the ui_kits README Preferences row (split view, seven panes).

**[DONE] TECH-DOC5 (P3): superseding ADRs.** Record that ADR 0001 (CoreAudio HAL tap) is superseded by ScreenCaptureKit/SCStream, and note ADR 0002 is partially superseded by ADR 0007. Done: set ADR 0001 Status to "Superseded by ScreenCaptureKit (SCStream)" and ADR 0002 Status to "Accepted; partially superseded by ADR 0007", each with a short superseding-note blockquote after the property table (0001: the live capture path is SCStream; 0002: the FluidAudio engine choice stands but transcription moved in-process per ADR 0007).

### Identity and repo presence (promoted)

**[DONE] TECH-REPO1 (P1): GitHub repo presence.** README hero, a LICENSE file, CONTRIBUTING, and repo metadata (description, topics, social-preview image). This is the in-scope slice of branding, justified by contributor visibility, distinct from the deferred launch band. Done: added a centered README hero (tagline + CI / platform / MIT-license badges; used a text hero rather than the `currentColor` logo SVG, which is invisible on GitHub's dark theme) and fixed the clone placeholder to `ggegelya/meeting-pipe`; wrote `CONTRIBUTING.md` (setup, verify table, the Swift/Python sidecar contract, no-em-dash rule, commit/PR conventions, scope). The MIT `LICENSE` already exists with a generic "meeting-pipe contributors" copyright, so no change. Owed by the user (outward-facing GitHub mutations, not run from here): `gh repo edit ggegelya/meeting-pipe --description "..." --add-topic ...` and the manual social-preview image upload.

**[DONE] TECH-REPO2 (P1): app visual identity.** Land the distinct signal color (DSN4) and polish the app icon / menu-bar glyph so the app reads as a crafted niche tool, not a default Mac app. The taste already exists in the HUD and tokens; this is consistent application plus a real identity mark. Done (DSN4 folded in): replaced the generic-blue signal ramp with a distinct **teal** (primary `#0E8C82`), contrast-checked on both the light and dark canvases. Propagated the full ramp across every site, the four the scout named (`Tokens.swift`, `colors_and_type.css` incl. the dark `signal-100` override, `scripts/gen-icon.swift`, `design/assets/app-icon.svg`) plus the ones a re-sweep caught: the **shipped** fallback app-glyph (`daemon/.../Resources/AppGlyphs/_fallback.svg` and its `design/assets` mirror), the README license badge, and the design-system swatch docs (`design/README.md`, `design/preview/*.html`). Regenerated `AppIcon.icns` via `gen-icon.swift` and eyeballed it (teal center bar reads cleanly on paper). Per scope, did not reshape the icon/glyph art (already crafted) and left the coral recording dot and the template-tinted menu-bar idle glyph untouched. Build green, 699/0; no stale signal hex remains outside this backlog trail.

### Carried-over Q3 items (unchanged specs)

- **TECH-C6-FINISH (P1):** 20+ real detection traces; the corpus has nine synthetic seeds. Add the `detection-corpus/README.md` redaction note required by the stop-and-ask before real traces land. **Scaffolding done:** wrote the `detection-corpus/README.md` redaction note (the named precondition: strip/placeholder titles, drop real pids, keep only the closed signal vocabulary, no free text, never commit raw `events.jsonl`, en/uk locale) plus the trace-format reference, and `scripts/redact_detection_trace.py`, a stdlib helper that rewrites pids and title fields, validates the trace shape + promotion signal kinds, and fails closed on any residual PII (email / URL / @-mention). The 20+ real dogfood traces themselves remain **owed by the user** (runtime data captured from live meetings, redacted through the helper before commit). `DetectionCorpusTests` still green; the README is ignored by the INDEX-driven loader.
- **TECH-VALID1 (P0):** run the owed on-device validations for A15 (local cold-start within 10%), A16 (re-run quality/latency), DIAR1 (DER and under-10s), SUM1-APPLE (quality vs local, latency within 2x, zero egress via Little Snitch), UX4 (live degraded banner and `recording.degraded` event on a real failed SCStream). These are runtime acceptance, not code. **Scaffolding done:** wrote `docs/validation/valid1-acceptance-runbook.md` (per-bar command + threshold + where to read the result + a results table) and `scripts/valid1_check.py`, a stdlib-only helper that reads `events.jsonl` / `pipeline_events.jsonl` to make UX4 a hard pass/fail and to print run/stage timings for the A15/A16/DIAR1 reads. The five measurements themselves remain **owed by the user** on a real Apple-Silicon Mac (cold-start, DER, Apple-Intelligence quality, Little-Snitch zero-egress, a real failed SCStream).
- **[DONE] TECH-UI-X1 (P2):** split MeetingDetailView per tab (target under 250 lines). Pairs with DSN2. Done: the 1146-line file is now four, `MeetingDetailView.swift` (217, the tab container + PublishURLs, under the target), `MeetingDetailView+Header.swift` (297, the header rendering + title-edit/toolbar actions as a same-type extension), `SummaryTab.swift` (415, the Summary tab), and `SummaryRenderedView.swift` (231, the typed-summary render + ActionItemRow/AttendeeChips). TranscriptTab/AudioTab were already separate. Pure mechanical move (verbatim, only `private` -> internal on the members the extension reads, which the compiler validates); full Swift suite 743/0. SummaryTab at 415 stays cohesive, in line with the sibling TranscriptTab (476) / AudioTab (394) files; the container, the actual target, is under 250.
- **[DONE] TECH-UI-X2 (P2):** split PreferencesView per section (target under 200 lines). Pairs with DSN1. Done: the 1187-line file is now the nav core `PreferencesView.swift` (177, under target: selection state + item enum + the split view + sidebar + doctor sheet) plus one file per pane, `PreferencesView+General.swift` (99), `+Recording.swift` (199, carries the WrappingHStack/FlowLayout it alone uses), `+Prompt.swift` (88), `+Pipeline.swift` (292, carries SummarizationPromptPreview), `+Integrations.swift` (60), `+Permissions.swift` (233, carries PermissionsCardRow), `+Advanced.swift` (53). Each section struct just dropped `private` so the nav switch still constructs it; doc comments travelled with their struct. Pipeline (292) and Permissions (233) stay over 200 because splitting those cohesive panes further would fragment them; the container, the real target, is under 200. Pure mechanical move; full Swift suite 743/0 (PreferencesSelectionStateTests included).
- **[DONE] TECH-T2 (P2):** snapshot tests for three SwiftUI views, gated by macOS Appearance. Done: added `pointfreeco/swift-snapshot-testing` (resolved 1.19.2) to the `MeetingPipeTests` target, and `SnapshotTests.swift` snapshots three stable SwiftUI views, `MPStatusPill`, `WorkflowChip`, and `SummaryRenderedView`, each rendered through an `NSHostingController` in both light (`.aqua`) and dark (`.darkAqua`) appearance (six references under `Tests/MeetingPipeTests/__Snapshots__/`). Comparison runs with `precision: 0.99, perceptualPrecision: 0.95` so cross-version anti-aliasing/font drift doesn't flap CI; references re-record with `record: .all` on a deliberate visual change. Full Swift suite 746/0. `Package.resolved` stays gitignored per the repo convention, so CI re-resolves within the `from: 1.17.0` bound. Caveat: snapshot references are environment-bound; if macos-14 CI rendering drifts past the perceptual tolerance, the references get a one-time re-record (standard snapshot maintenance).
- **[DONE] TECH-I6 (P2):** scoped down to the sidecar `publish_state` field plus a per-row indicator; the per-sink result map already exists in `fanout`. Done: `publish_router.publish_state(result)` classifies the fanout map as `full` / `partial` / `none`; `orchestrate.run_all` records it onto the run sidecar after fanout via `corrections.set_publish_state` (best-effort, atomic). The Swift `Meeting` reads `run.json["publish_state"]` and `MeetingRow` shows a `.warning` "Partial" / "Unpublished" pill on done rows (new `MPStatusPill.warning` -> `MPColors.warning600`, NDA "Local only" still takes priority). **Deviation, named:** publish_state lives on the pipeline-owned `<stem>.run.json` (already read by the Library), not the daemon-owned `<stem>.meta.json` whose contract is daemon->pipeline; a pipeline->Library signal belongs on a pipeline-owned sidecar, so `test_workflow_overlay.py` is not the relevant test. Covered by `test_publish_router.py` (classifier), `test_corrections.py` (set_publish_state merge + missing-noop), and `MeetingStoreTests.test_publish_state_read_from_run_sidecar`. Swift build + MeetingStoreTests 28/0; pipeline 293/0.
- **[DONE] TECH-W2 (P2):** near-noop; `WorkflowMatcherTests.test_ties_break_by_order_ascending` already pins order-ascending precedence. Move the assertion into the W2-blessed file or close W2. Done: the assertion already lives in the right file (`WorkflowMatcherTests`), and the default-fallback ascending path is also already covered (`test_no_default_falls_back_to_lowest_ordered`), so rather than a redundant copy I added one crisp acceptance pin, `test_w2_ties_break_by_order_independent_of_array_position`: three same-score matches with the lowest `order` deliberately in the middle of the array, proving the tie-break is order-based, not insertion-order-based. WorkflowMatcherTests 10/0. W2 closed.
- **[DONE] TECH-E4-FINISH (P2):** the events.jsonl acceptance-bar report script. Done: `scripts/dogfood_report.py` (stdlib-only, runs on a clean Mac), reads `events.jsonl` + `pipeline_events.jsonl` and reports detection (lifecycle verdicts + ends by leading signal), mic-gate verdict transitions, and pipeline run/stage/sink outcomes with per-stage timing stats; `--since` / `--json` / `--out`. Distinct from the A/B `dogfood.py`. Pure `build_report`/`render_markdown` unit-tested in `test_dogfood_report.py` (4 cases); validated live against the real 125k-event log. Named with an underscore to match the repo's Python-script convention and to be importable by the test.
- **TECH-UX3/UX5/UX7/UX8:** if any Q3 P2 UX item is still open, re-confirm against current code before scheduling; UX5 and several others landed in Q3 with documented name drift only.

### Deferred (P3, unchanged trail)

TECH-I7 (drop Python; see DIST2), TECH-I8 (live transcription), TECH-G1 (two-Mac hub), TECH-D8 (notarization in CI; partly via DIST1), TECH-CAP1 (mic/system skew, monitor only; spec in q3-final.md), Group F compliance docs (partly activated by BRAND8). The full BRAND launch band (BRAND1-9) stays deferred until an explicit launch decision.
