# I7: Drop Python entirely

Band origin: architecture review 2026-07-10 (engineering-health band). Status and priority live in this task's ToC row in [meetingpipe-q6-backlog.md](../meetingpipe-q6-backlog.md).

**I7 (P3, deferred; trigger amended 2026-07-10).** Base spec in [`q4-final.md`](../q4-final.md). The Q4 trigger (Apple Intelligence proves out as the Swift-native summarizer) is the wrong gate: the realistic Swift-native local path is MLX-Swift (the same models mlx-lm serves, Apple-maintained Swift bindings), not the 4096-token system model. New trigger: MLX-Swift LLM inference matches the mlx-lm 7B on quality and latency for this workload, or Apple widens the system-model context. Context for when the trigger fires: DIST1 (bundling a relocatable Python), the de-CLI goal, and the dual-language maintenance burden all hang off the Python sidecar, so evaluate the consolidation deliberately rather than letting it drift. LOCAL12 (2026-07-12, assessment band) delivered the verdict: NO-GO now, keep the embedded-Python runtime as the end state, with the trigger operationalized as an owner-run bench that A/Bs an MLX-Swift server against mlx-lm when one exists; see [`docs/spikes/local12-mlx-swift-trigger-evaluation.md`](../../spikes/local12-mlx-swift-trigger-evaluation.md).
