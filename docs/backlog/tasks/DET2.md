# DET2: CoreAudio process-tap attribution

Band origin: assessment review 2026-07-12. Status and priority live in this task's ToC row in [meetingpipe-q6-backlog.md](../meetingpipe-q6-backlog.md).

**DET2 (P2): CoreAudio process-tap attribution (DET1 phase 2).** `ProcessAudioSignal` is dead in production: the PID-to-HAL translation never resolves without audio-capture authorization (0 reads in 19.8 days, 13,407 unresolved; `ProcessAudioSignal.swift`, `NativeLifecycleAdapter.swift:8-17`). Adopt a CoreAudio process tap (`AudioHardwareCreateProcessTap` / `CATapDescription`, macOS 14.2+, the repo's existing floor) so per-process input attribution resolves, reviving `processAudioActive` as real start/end evidence. Upgrades DET1's generic prompt to a named high-confidence one, gives native start detection a second evidence class (breaking the DET5 title single-gate for good), and gives end detection an independent leg. Cost: one more TCC consent, so sequence after DET1 proves the tier's value. Acceptance: the signal reports live per-process audio on a real call; the scorer treats it as an independent signal; DET1 prompts name the attributed app.

**Progress (2026-07-12): spike shipped; the empirical verdict is owner-owed.** The SDK confirms the tap is not a thin call (to *capture* audio it needs an aggregate device), but DET2 only needs a per-process boolean, and creating the tap, or merely holding the Screen Recording grant the daemon already has, may already resolve the read, so the mechanism is measured before any build (the owner's goal is a zero-setup sidecar with no device selection; the old shared-aggregate pattern that would need one is already rejected in ADR 0001). Deliverables, matching CAL1/MIC7: the analysis doc [`docs/spikes/det2-process-tap-attribution.md`](../../spikes/det2-process-tap-attribution.md) (the A/B/C mechanism decision tree, the private-vs-shared aggregate clarification, the `requiresCorroboration = true` safety rail for the end leg when built so a mute-induced input drop can never solo-confirm a false end, and a note that the two non-runtime acceptance clauses, scorer-treats-it-independent and prompt-names-the-app, are already satisfied by existing code once the read is real) + the owner-run read-only probe [`daemon/scripts/det2-process-tap-probe.swift`](../../../daemon/scripts/det2-process-tap-probe.swift) (typecheck-clean against the SDK, which resolved the `NS_REFINED_FOR_SWIFT` initializer spelling; it holds a private muted tap / private aggregate briefly and tears them down, never selecting a device). No production detection/recording code changed, so there is no regression risk. Owner-owed remainder, the spike's whole point: run `swift daemon/scripts/det2-process-tap-probe.swift` during a live meeting and read which mechanism (A grant-only / B bare tap / C aggregate / none) revives `kAudioProcessPropertyIsRunningInput`, then GO (flip `usesProcessAudio` with the corroboration rail, plus a `ProcessAudioTap` wrapper only if B, and update `LifecycleAdapterTests.test_process_audio_signal_is_disabled_for_every_provider`) or close the leg.
