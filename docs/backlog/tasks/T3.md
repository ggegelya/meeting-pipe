# T3: Shipped-untested tests + coverage harness

Band origin: assessment review 2026-07-12. Status and priority live in this task's ToC row in [meetingpipe-q6-backlog.md](../meetingpipe-q6-backlog.md).

**T3 (P2): shipped-untested tests + coverage harness.** The load-bearing untested residue: `SearchIndexer`'s reconcile/signature diff (UX16, shipped with zero test references), `StatusBarController` (731 lines, the always-visible surface, zero references), `Notifier`'s response-identifier-to-delegate routing (a consent surface), and `__main__.py`'s dispatch (the pipeline front door); and no coverage measurement exists in either tree (pytest-cov absent from dev extras, no `--enable-code-coverage` anywhere). Scope: pin reconcile (first-scan build, mtime-change reindex, dead-stem delete, no-change no-bump); extract a pure `StatusBarModel` derivation (title/icon/menu state from AppState + processing count + NDA + permissions + download state) and a pure Notifier routing map per the repo's `decide()` idiom, table-test both; a parametrized dispatch test over every registered command name; add pytest-cov to dev extras + `scripts/coverage.sh` (pytest --cov=mp; swift test --enable-code-coverage + llvm-cov report) and record a baseline here. Optional: `SystemAudioCapture.pcmBuffer(from:)` conversion. Acceptance: new tests green in both suites; `scripts/coverage.sh` prints per-module numbers for both trees; the baseline is written down.
