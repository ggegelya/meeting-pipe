# CAL2: Pre-meeting prep card

Band origin: feature review 2026-07 (owner approved 2026-07-05). Status and priority live in this task's ToC row in [meetingpipe-q6-backlog.md](../meetingpipe-q6-backlog.md).

**CAL2 (P3): pre-meeting prep card.** Before a recurring meeting, the relevant context (the matched workflow's last summary and its open actions) is a hunt through the Library. Add a user-invoked "Last time" affordance on the prompt panel (and `mp prep <workflow>` for the shell), quiet register, never a popup. The workflow-keyed version needs nothing new. The calendar-aware timing and title/attendee seeding that would have sequenced behind CAL1 are closed with it (CAL1 NO-GO 2026-07-17: the owner's calendar is not in macOS EventKit, see [`q6-final.md`](../q6-final.md)); re-open that extension only if the owner ever connects a macOS calendar and CAL1 is revisited GO.
