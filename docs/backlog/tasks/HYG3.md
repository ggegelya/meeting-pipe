# HYG3: Dead-code removal, 2026-07-23 sweep

Band origin: filed 2026-07-23 (owner-commissioned dead-code sweep: two parallel very-thorough passes over daemon and pipeline, every actionable claim re-verified in the filing session by repo-wide word-boundary greps). Status and priority live in this task's ToC row in [meetingpipe-q6-backlog.md](../meetingpipe-q6-backlog.md).

**HYG3 (P3, new): remove the seven verified dead or misplaced items the 2026-07-23 sweep found, and fix the one doc surface that claims infrastructure which never existed.** Headline for calibration: both trees are genuinely clean at the type, knob, dependency, event, branch, and menu-action level; this is a short list, not an excavation.

Pipeline items (each verified: repo-wide `grep -rnw` over pipeline, daemon, scripts, .github, docs returns only the definition and, where noted, tests). (1) `_numbered` (publish_notion.py:560) and (2) `_todo` (publish_notion.py:568): zero callers; the live code builds `numbered_list_item` and `to_do` blocks inline (publish_notion.py:616, :667, the multi-run rich-text and checked-state paths that superseded these single-run helpers). Remove both. (3) `reap_orphan` (local_server.py:150): zero production callers, three test call sites (test_local_server.py:55, :138, :154); production reaping is Swift `LocalServerReaper.reapIfOrphaned()` (direct kill(2), no mp shell-out) and Python `doctor` only reports orphans via `orphaned_server` (alive, doctor.py:419); the module docstring already assigns reaping to the daemon. Remove the function and its tests. (4) `icloud_desktop_sync_enabled` (cloudsync.py:109): zero production callers, test-only (test_cloudsync.py); never wired into `detect_sync_provider`, so the "last-resort corroborator" its docstring describes does not exist in the detector. Default disposition: remove the function, its tests, and the docstring's corroborator claim. Named alternative for the owner: wiring it into the detector is SEC12-adjacent feature work, file separately if wanted; do not leave the half-state.

Daemon items. (5) `MuteLabelsValidator` (MeetingPipeCore/MicGate/MuteLabelsValidator.swift:4): referenced only by its own tests; no CI job runs it (ci.yml runs truth_fences, swift build, swift test). The sharp edge is documentation asserting fiction: docs/architecture/signal-fusion-and-mic-gating.md:186 describes "a CI job (`MuteLabelsValidator`) runs nightly on a build VM with Teams, Zoom, Webex, and Slack pre-installed in each locale" walking AX trees, and MuteLabels.toml:17 repeats the claim; no such job has ever existed in this repo. Disposition: keep the validation logic only if it is made true, either by relocating the type into the test target as the explicit engine of `MuteLabelsValidatorTests` (pickup verifies those tests validate the BUNDLED TOML, not just fixtures, and points them at it if not) or by deleting it; in both branches rewrite the two doc surfaces to state the actual guard (unit tests on every CI push). The nightly-VM-farm paragraph does not survive this task. This is the 2026-07-12 assessment's "surfaces assert behavior the code no longer has" class, mute story again. (6) `RecordingEventLog` (MeetingPipeCore/Infra/EventLog.swift:15): a deliberate shared test double shipped in the production target; its own doc comment says "tests pass `RecordingEventLog`" while the executable wires `LogEventAdapter`. Relocate into the test tree if it can serve its consumers there cheaply; otherwise keep and extend the comment to say explicitly that it ships in Core as a cross-target test seam, so the next sweep does not re-flag it. Smallest truthful change wins. (7) `MenuBarIcon.svg` + `MenuBarIconRecording.svg` (Sources/MeetingPipe/Resources/): bundled but never loaded by any code path (the status-bar icon is drawn programmatically, StatusBarController.makeIdleIcon/makeRecordingIcon; exhaustive forResource/Bundle.module/NSImage(named:) search plus scripts/ came back empty); the design source of truth is design/assets/menubar-icon*.svg. Remove from Resources. (8) `CaptureError.bufferConversionFailed` (SystemAudioCapture.swift:227): declared and given an errorDescription arm (:232) but never thrown; the buffer-conversion failure path drops the frame and returns. Default: remove the case and its arm; if the pickup session reading that path concludes silent frame-drop is itself wrong, that is a behavior change to file separately, not to smuggle in here.

Kept-deliberately register, verified against their recorded decisions, so nobody re-files them: `ProcessAudioSignal` + `usesProcessAudio` + the `.processAudio` evidence class (DET2 NO-GO 2026-07-20, kept dormant by design, documented in the signal file and NativeLifecycleAdapter); `MeetingPipeAppIntents.swift` and its six intents (AUTO1, gated behind MP_APP_INTENTS pending a Developer ID); `services.NotionPublisher` alias (kept-importable comment at services.py:91-94); `summarize_local._largest_balanced_json_object` alias (documented test-compat comment, and the promoted `json_extract` original is production-live including diarize_cleanup.py:260).

Clean-sweep record, 2026-07-23, so the next sweep diffs instead of redoing: config knobs mechanically fenced (scripts/truth_fences.py both green, empty allowlists); no unused dependencies in pyproject main or dev extras (the DOC9 vcrpy precedent already resolved); no dead event categories; no provable dead branches (every pragma-no-cover is a defensive guard or main block); every @objc/menu action has a #selector wiring; all other bundled resources load and all 29 detection-corpus fixtures are INDEX.json-listed; no custom Notification.Name exists; all root and daemon scripts are referenced by CI, docs, or each other; single-use SwiftUI subviews and Core public probe/signal types are alive through their own files, the coordinator wiring, or the testability seam.

Explicitly not: no removal of anything in the kept-deliberately register; no behavior changes riding along (the frame-drop question in item 8 files separately if raised); no doc rewrites beyond the two MuteLabels surfaces; no chasing "public but only used in one file" Swift API into access-level churn.

Acceptance: each removed symbol greps to zero hits repo-wide afterwards; removed functions take their tests with them in the same commit; the two MuteLabels doc surfaces no longer claim a nightly job and name the real guard; `MuteLabelsValidatorTests` demonstrably validates the bundled MuteLabels.toml (or the validator is gone); both suites, ruff, pyright, and `scripts/truth_fences.py both` green; `swift build` clean. Pure deletion/relocation, so no new tests beyond the retargeted validator test.
