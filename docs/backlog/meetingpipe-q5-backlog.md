# MeetingPipe Q5 backlog

This is the active source of truth. Q4 is archived to [`q4-final.md`](./q4-final.md) (earlier quarters beside it; git history preserves the rest). Q5 carries forward three things:

1. **Everything Q4 left open** (the start/end-detection, capture-privacy, pipeline-honesty, local-model, security, design and UX bands the Q4 audits opened but never closed).
2. **The validation carries.** A full audit of every Q4 DONE item ([`docs/audits/2026-06-26-done-task-validation.md`](../audits/2026-06-26-done-task-validation.md)) confirmed 68 closed done-done but found 13 that were not fully wired: two CLI-only features with no UI (FEAT2, FEAT4), eight with an explicitly deferred remainder (PERF5, DSN3, DSN9, END2, END3, FEAT3, MIC6, and the END2/END3 residuals folded into END6/END7), two with owner-owed runtime validation (C6-FINISH, VALID1), and one that did not hold (T2). Nothing marked DONE was fabricated.
3. **The intelligence + data-view layer** from two deep-research reports: [local AI intelligence](../research/2026-06-26-local-ai-intelligence.md) and [meeting-level UX](../research/2026-06-26-meeting-level-ux.md). Both independently converged on the same two highest-leverage items: an action-item lifecycle (done flag + aging) and a cross-meeting facts view. These are promoted into P1 per the owner's call to interleave the intelligence layer up alongside the worst correctness bugs (rather than strictly correctness-debt-first).

## How to pick up a task (delegation)

Mechanics are codified in `/tech-task <ID>` (read the task here, read the orientation docs, implement, verify, one commit on `main`, do not push). Soft context that command does not carry:

- Task ids carry no `TECH-` prefix (e.g. `/tech-task START1`). Historical code comments, ADRs, and commits keep the old `TECH-` form as provenance.
- **Full specs for items carried from Q4 unchanged live in [`q4-final.md`](./q4-final.md)** (search the id); this file restates them in one line and only writes full specs for new or changed items, so the spec has a single owner.
- User locale: 99% English / Ukrainian. Do not default examples to German. Verify any new `uk` labels in `MuteLabels.toml` with the user.
- The user is vibe-coding: high-level explanations, does not read code. Lean on the ARCHITECTURE.md diagrams. Blind sub-pixel visual churn is out of bounds; visual changes need eyes (the binding constraint that deferred the DSN3 font sweep and DSN9 retune).
- No em-dashes in any output (CI fails on a U+2014 in an added line under `daemon/Sources` and `daemon/Resources`). Use hyphens, commas, or rewrite. Commit with the repo's git identity. Do not push without permission.
- Binding constraint (CLAUDE.md): the primary user is the author; selling is tertiary; technical excellence in core functionality comes first. The BRAND launch band stays deferred. The one standing promotion: GitHub repo presence + visual identity (closed in Q4 as REPO1/REPO2).
- New families this quarter: `AI*` (on-device intelligence) and `DV*` (the meeting-level data view). Every AI/DV capability must route through the local engine path so the `egress_guard` clamps it under regulated/NDA, honour `effective_backend()` (forced local under regulated/NDA), and add no new always-on egress.

## Priority bands

- **P0**: correctness or a broken core promise; do first.
- **P1**: meaningful, not blocking. This quarter the P1 band deliberately interleaves the worst open correctness bugs with the three promoted intelligence/data-view items (AI1, DV1, AI2).
- **P2**: polish and power-user payoff.
- **P3**: deferred; listed so the trail stays readable.

## Table of contents

| ID | Task | Category | Priority | Description |
|---|---|---|---|---|
| CI1 | Green CI honestly | Tests | DONE (was P0) | Snapshot refs OS-pinned to macOS 26 vs the macos-15 runner kept CI red since Jun 6, masking all signal; ffmpeg-gated safety tests silently skip. Fix the skips, install ffmpeg in CI, push. (AUD-8) Snapshot half already closed by de3bf35 (swift-snapshot-testing removed under T2); this change installs ffmpeg in the daemon CI job and makes the three MuteRedactor safety-test skips loud (fail, not skip, when ffmpeg is missing on CI). Push deferred per the no-push rule. |
| START1 | Un-wedge the prompt/suppression lifecycle | Detection | DONE (was P0) | A skipped/timed-out prompt plus one stale AX end event leaves the engine terminally `.ended`; detection is off for every app until restart. (AUD-1/2) Done: the app-state half (global `.suppressed` to bundle-scoped `abandonPrompt` + engine reset on every idle/engage) shipped earlier in 5c92b6e; this closes the remaining engine-level wedge: `PromotionEngine.handleStarting` now holds a lone uncorroborated ax-leave in `.starting` rather than promoting to terminal `.ended`, so a later Record still arms and detection never wedges. Item 4 (call-shaped SCK liveness) deferred to END6, which retunes that reliable end signal and owns the correlated-pair gate. |
| START2 | Browser-tab meetings discoverable | Detection | DONE (was P0) | URL fragments are matched against AX window titles, which carry page titles, never URLs; a plain-tab Meet/Teams/Zoom meeting can never prompt. (AUD-3) Done: the scanner's browser-bundle branch now admits a window whose page title matches the browser lifecycle adapter's meeting patterns (`browserWindowIndicatesMeeting`), not just a URL fragment, so a plain-tab Meet call is discoverable. The in-call corroborator that tightens title/name-only browser admissions is START3. |
| REC1 | Verify the ffmpeg merge before deleting intermediates | Recording | DONE (was P0) | stop()/recoverOrphan delete `.mic.wav`/`.system.wav` unconditionally while runFFmpeg discards exit status; one broken ffmpeg destroys a fully captured meeting. (AUD-5) Done: `runFFmpeg` now returns its exit status and the merge/convert helpers report a verified outcome, so `stop()` and `recoverOrphan` delete an intermediate only once the final WAV is verified to exist with a plausible (>4 KiB) size. A failed merge keeps every intermediate, writes a `<stem>.recordfail.json` breadcrumb, and surfaces a notification instead of enqueueing a missing file; the orphan sweep then recovers it on the next launch. The no-ffmpeg fallback keeps `system.wav`, and the start-time `.capturemode` marker is retained on failure so a redaction-opt-in orphan still quarantines. MuteRedactor was the safe-pattern template. |
| REC2 | Start-time identity manifest + recovery honors it | Security | DONE (was P0) | Meta sidecar exists only at clean stop; crash-orphaned BYO/NDA meetings auto-egress via Anthropic + Notion at next launch. (AUD-6) Done: a start-time `<stem>.recovery.json` manifest (`RecordingManifest`) persists `summary_mode` + the meta payload at recording start. Orphan recovery replays it: it rebuilds `<stem>.meta.json` (so an NDA/regulated orphan arms the pipeline egress guard and keeps its title) and enqueues a BYO orphan as `.byo` (paste bundle, no Anthropic/Notion) instead of `.auto`. Terminate now flushes synchronously: a SIGTERM `DispatchSource` + `applicationWillTerminate` both call `MeetingRecorder.flushIntermediatesForTermination` (drains the writers + closes the files, no async merge), replacing the dead fire-and-forget `Task { await stop() }`; `kill -9` is covered by the start-time manifest since no handler can run. Legacy pre-REC2 orphans keep the `.auto` default. Reused the pipeline's existing `egress_guard`/`apply_overrides` fail-closed path rather than adding `summary_mode` to the Swift↔Python `.meta.json` contract. |
| REC3 | Escalate swallowed write failures + 4 GiB WAV cap | Recording | DONE (was P0) | Per-buffer write errors are os_log-and-drop; disk-full or the RIFF u32 cap (~3h06m) silently truncates while the HUD shows live levels. (AUD-7) Done: a per-writer `WriteFailureTracker` counts consecutive failed writes in the mic/system serial-writer closures; after 32 unbroken failures (~2.7 s) it escalates ONCE: emits a `recording_error` event and fires `onWriteFailure`, which the Coordinator surfaces as a notification and force-stops the recording (the intact prefix is preserved by REC1's merge-or-keep path). One mechanism covers both AUD-7 triggers: disk-full at any duration AND the 4 GiB RIFF cap (the cap surfaces as a run of failed writes, so the same path force-stops at ~3 h with the prefix kept). Deviation: deferred the spec's CAF/segment-roll cap-*raising* (a >3 h meeting is now force-stopped near the cap rather than silently truncating) because it ripples into REC1's just-landed orphan-detection + merge contract and DOC6, and the cap is unreproduced (confidence 85). Surface is notification + force-stop (which tears down the HUD), not a transient degraded banner the immediate stop would dismiss. |
| UX14 | Light-theme legibility pass | UX | DONE (was P0) | The light (paper) theme ships WCAG-failing text/chrome (fgFaint/fgSubtle, 4 of 7 MPStatusPill states, primary/Rec labels, raw speaker palette). The "ugly white theme" fix. Done: every touched light-mode text-on-surface pair now clears 4.5:1 (UI/large 3:1) on paper, white card, and bgSunk, with dark mode byte-unchanged. Accent text (`.mpSignal`/`.mpSuccess`/`.mpWarning`) and the 8-hue speaker palette were made appearance-aware (deep step on paper, the existing 600/system step in dark) because the deep steps themselves fail in dark; redefining the accent tokens fixes ~12 `.mpWarning`/`.mpSuccess` sites at the token. Added three deep palette steps `success700` (#16713D), `warning700` (#8A5A00), `pulse700` (#BE353A), signal700 already existed, mirrored in `design/colors_and_type.css`. `MPStatusPill` now paints the label in the deep tone while the lighter dot stays the colour cue (processing/recording/failed/ready/warning fixed). The primary button and the Library Record/Stop button darken their fill to signal700 / new pulse700 so the white label clears 4.5:1. The three `fgFaint` text sites repoint off the 2.79:1 token: fgSubtle on the white card / paper, fgMuted for the match-count badge (it sits in the bgSunk well where fgSubtle is only 4.2:1). Deviations from the one-line spec: neutral pill bumped fgSubtle to fgMuted (was 4.2:1 on bgSunk); the accent fix is appearance-aware rather than a flat token change, to protect dark; added the three deep tokens the ".700 step" wording implies; folded in BatchActionsPane's primary button (same failing white-on-signal600 pair as the named MPButton); onboarding accent sites left to the deferred selling band; signal600 icons (lock/eye, 3.95:1) left untouched as they clear the 3:1 UI floor. The contrast CI gate plus the per-pair ratio table stay DSN18's. daemon debug build + `swift test` (864) green. |
| START3 | PWA admission needs an in-call corroborator | Detection | DONE (was P1) | An idle meeting-named PWA prompts on app name alone and is exempt from the corroboration gate; timeout-skip then triggers START1's suppression. (AUD-4) Done: the three name-only-PWA leaks are closed. (1) `MeetingSourceScanner` stamps the PWA candidate with the real per-window title match (`preTitleMatch: titleMatched`) instead of a synthetic `true`, so a landing-page admission carries `titleMatch = false`. (2) `MeetingSourceScorer`'s lone-candidate gate trusts a browser on title alone only when that title is genuine (the START2 plain-tab case); a name-only browser (`titleMatch == false`) must now show an in-call corroborator exactly like a native, so an idle PWA is dropped as zero-evidence. (3) `BrowserMeetingLifecycleAdapter` drops the `isMeetingPWA` shareable-content bypass, so a PWA uses the same strict title-gated liveness as a plain browser (a genuine in-room call always carries a hyphenated title that matches; this also gives PWAs a real end signal they never had). Change 1 is load-bearing (after it the existing zero-evidence filter already drops an idle name-only PWA); change 2 makes the corroboration invariant explicit at the gate and corrects the stale "browsers are exempt" rationale. The buggy `reads_live_from_identity` adapter test was inverted (landing-page title reads not-live, in-room title reads live) and the scorer gained name-only-PWA coverage. daemon build + 867 tests green. |
| MIC10 | Re-lock the Teams mini-window mute control + VAD-contradiction | Detection | P1 | The new Teams mini-window UI: the scoped AX walk reads a stale hub/pre-join "muted" button. Re-scope to the live control; add a VAD-contradiction staleness trigger. Needs a live-AX dump. |
| MIC6 | Attribute-based (futureproof) native mute read | Detection | P1 | Carried from Q4 (DONE-partial). The four MIC6 parts shipped (cross-locale fallback, element re-arm, decoupled blind-clear, uk labels); the headline part-1 migration to the button's stable state attribute (AXValue/pressed-state, not the localized title scrape) is owed, gated on an owner-run live AX dump (`daemon/scripts/ax-dump-meeting.swift`). Overlaps MIC8/MIC10. |
| MIC11 | Bounded, surfaced failure when `recorder.start()` cannot bring up the engine | Recording | DONE (was P1) | The real root cause of the 2026-06-09 storm: a failed start fails silently and does not advance the state machine, so detector + clicks re-attempt unboundedly. Surface (HUD + notification + event) and enter a per-source cooldown. Full spec in q4-final.md. Done: the failed-start `catch` in `MeetingSessionController.beginRecording` now arms the per-bundle `RepromptCooldown` (`recordCooldownEnd`, the same gate checked at both re-prompt sites: `handleMeetingStarted` + `handleDiscovery`) so a device the engine can't start on backs off for `repromptCooldownSec` (default 60 s) instead of the detector + re-clicks storming, and emits a structured `recording_error` event (category `coordinator`, `stage=start`, plus `reason`/`bundle_id`/`cooldown_armed`) so `mp logs` / `mp analyze-detection` can see failed starts (the catch was os_log-only). Scope note: the notification + return-to-idle the original prose asked for already landed with the 2026-06-12 HFP-freeze fix (which made `recorder.start()` `async throws`, typed `RecorderError`, and bounded the engine bring-up off-main); MIC11 adds the missing event + cooldown bound. Deviations: dropped the spec's "HUD" surface (nothing is recording on a failed start, so the notification is the surface, per REC3's notification-over-transient-banner precedent); used `RepromptCooldown` only, not the skip-latch, so a transient device flap can retry after the cooldown rather than being suppressed for the whole meeting; manual recordings (no bundle) rely on the existing `recordingStartInFlight` re-entry guard. New pure `MeetingSessionController.startFailureReason` (exhaustive over `RecorderError`) maps the event `reason` and is pinned by `MeetingSessionControllerTests`; the cooldown/state-machine wiring sits in the non-unit-testable controller (END7 precedent: build + review), and the Bluetooth/HFP device-flap end-to-end repro stays owner-owed. daemon debug build + `swift test` (869) green. |
| MIC12 | Span-windowed runaway-redaction guard | Privacy | DONE (was P1) | The MIC9 guard measures whole-file energy with an 85% cliff: always withholds on the canonical heavy-mute meeting, never engages below 85%; window the energy per muted span and surface withholds. (AUD-18) Done: replaced the whole-file `runawayWithholdReason` (85% coverage cliff + whole-file energy) with `MuteRedactor.partitionSpans`, which windows the existing chunked energy reader to each muted span and redacts only the genuinely-quiet ones (<= `speechFloorDb`); a muted span carrying speech is withheld (kept), never zeroed, and an unreadable span fails safe to withheld. This fixes both halves: a mostly-muted all-hands now redacts its quiet listening (instead of withholding everything once any speech appears), and a sub-85% timeline no longer redacts speech-bearing spans unchecked. Withholds are surfaced via the `mute_redaction_withheld` event with per-span counts; a user-facing banner is left out of scope (redaction is latent until a workflow opts in, and it would ripple into SinkDispatcher/Notifier). daemon build + 850 tests green. |
| MIC13 | originals/ retention, reaper, delete cascade | Security | P1 | ADR 0016's required retention policy + reaper were never built; meeting deletion leaves the full original behind; the quarantine path skips backup exclusion. (AUD-19) |
| END6 | Correlated-pair gate + device-idle corroborator | Detection | DONE-partial (was P1) | Absorbs the END2 validation residual. ax-leave + window-gone co-fire on a Teams share re-render and the fast path still promotes instantly; apply `requiresCorroboration` to `handleEndingProvisional`, treat the pair as one evidence class, land a genuinely-independent corroborator. (AUD-10) Done (correlated-pair gate; device-idle leg deferred per owner call): native meetings fuse only window-gone + ax-leave (ProcessAudio is disabled for every native client), and those two are correlated (a Teams / Webex share re-render invalidates the Leave button and drops the call window together), so `handleEndingProvisional`'s fast path and `tick`'s debounce guard now require a CROSS evidence-class corroborator. A new `EndEvidenceClass` groups ax-leave + window-gone into one class (every other signal is its own), so the pair can no longer instant-confirm or debounce-confirm each other; the AX re-walk (`confirmProvisionalEnd`, fired by `rescueProvisionalEnd`) stays the real native confirmation and a genuinely independent signal (process-audio, where enabled) still corroborates. `requiresCorroboration` stays scoped to ax-leave so window-gone's own lead + the browser paths are unchanged (no regression to the ends that work). The two PromotionEngine tests that asserted window-gone corroborates ax-leave are inverted (same class now absorbs), plus new cross-class + evidence-class coverage; the `webex_ultrasound_retention_en` corpus trace now ends via the 2 s debounce tick on the reliable window-gone lead (identical EndingReason) rather than the removed fast-path shortcut. Deferred (owner-owed): the `kAudioDevicePropertyDeviceIsRunningSomewhere` device-idle corroborator is device-global and confounded by our own recorder holding the input device (it can never read idle during a recording), so it needs live-Mac validation; the AX re-walk already backstops native corroboration, so it is not load-bearing. The screen-share dogfood trace the spec flags is still owner-owed. daemon debug + release build + `swift test` (875) green. |
| END7 | Clock-feed the IdleStopBackstop | Detection | DONE (was P1) | Absorbs the END3 validation residual. `ingest` is fed only on deduped verdict changes, so a meeting that stabilises silent never re-evaluates and the nudge/auto-stop horizons cannot fire; feed it from the ~1 Hz level callbacks or a 1 Hz tick. (AUD-9) Done: `MeetingSessionController` now caches the last MicGate verdict and re-feeds `silenceBackstop.ingest` from the recorder's ~1 Hz `onMicLevel` callback (previously unconsumed since END3), in addition to the change-only verdict stream, so the idle streak advances on the clock and the nudge / auto-stop horizons fire on a silent-stable recording. `onMicLevel` fires throughout any recording (mic-only or not) on the main queue, the same context the verdict consumer feeds on, so there is no cross-queue race; `latestVerdict` is reset at engage and the feed is torn down with the system-level mirror at stop. Test: `IdleStopBackstopTests` pins that re-ingesting the same silent verdict at advancing clocks fires both horizons with no new verdict transition. Build + 851 daemon tests green. The behavioural acceptance (a real forgotten recording auto-stops within ~15 min) is owner-owed, as END3 itself noted; `MeetingSessionController` is not unit-testable in isolation, so the host wiring is verified by build + review. |
| CONC3 | Move blocking SCK/AX probes off the main thread | Concurrency | DONE (was P1) | ShareableContentSignal blocks main behind an untimed semaphore at 1-2 Hz all meeting; a hung SCK call freezes the daemon mid-recording with no possible stop. Also close the `disengage()` engine-reset race. (AUD-11/29) Done: the SCK shareable-content probe now polls on a dedicated serial `DispatchSource` queue (off main) instead of a main-runloop `Timer`, with the signal's mutable state behind a lock and `probe()` / `onChange` run outside it; `defaultProbe` bounds the `SCShareableContent` wait with a timeout (returns nil = the established hold-prior-state path) so a hung SCK call can no longer wedge the poll queue for the rest of the meeting. `rescueProvisionalEnd`'s cross-process AX DFS moved onto the existing `axWalkQueue` (hops back to main to mutate the engine), mirroring `armLifecycleLeaveButton`. AUD-29: `disengage()` now routes `engine.reset()` + the idle publish through `engineQueue` so every engine mutation is serialised on one queue (was an inline main-thread reset racing the queued ingest / tick on `engine.phase`), plus a per-session generation token so a zombie event from a just-stopped adapter is dropped instead of resurrecting the freshly reset engine. New `MeetingLifecycleCoordinatorTests` case pins the post-disengage drop; daemon debug build + `swift test` (870) green. |
| REC4 | Surface mid-meeting SCStream death | Recording | DONE (was P1) | didStopWithError only logs: no banner, retry no-ops, no system-side watchdog; remote audio silently absent from the death point on. (AUD-13) Done: `SystemAudioCapture.didStopWithError` now fires a new `onStopWithError` callback (was log-only); the recorder hops to main and runs `handleSystemAudioLost`, which tears down the dead SCStream (clearing the stale non-nil `systemCapture` that made `retrySystemAudio` no-op), sets a `systemDegraded` flag, and fires the existing `onSystemAudioDegraded` so the HUD banner + Retry go live mid-meeting. A new ~1 Hz `systemFires` liveness watchdog (the generic `MicTapLivenessMonitor`, 3 s window, armed in `start()`, torn down in `stop()` + the termination flush) catches a silent death that delivered no delegate error; it engages only after the first buffer so the SCStream spin-up and a denied / mic-only recording never false-positive. The stop-time warning is gated on coverage via the pure `remoteAudioWarning(systemFires:degraded:)`: `.micOnly` (frames == 0, the existing whole-recording case) keeps `notifyMicOnlyRecording`, while a new `.interrupted` (frames > 0 but degraded) posts a distinct `notifyRemoteAudioInterrupted` ("System audio interrupted", accurate since the first part had both sides). Chose manual retry over auto-retry (preserves the pre-death partial; matches the existing degraded UX). Deviation: "gate on coverage" is a died-and-not-recovered flag rather than a frame-ratio, which would be confounded by legitimate system silence. New `MeetingSessionControllerTests` pin the pure gate; the AVFoundation / SCStream wiring is build + review (END7 / MIC11 precedent). daemon debug build + `swift test` (873) green. |
| PIPE1 | Honest publish-failure contract | Pipeline | P1 | All-sinks-failed exits 0: daemon clears error.json, emits pipeline_succeeded, and can notify with a stale page URL; retry re-summarizes instead of re-publishing. (AUD-14) |
| PIPE2 | Route republish + paste through the fanout | Pipeline | DONE (was P1) | Library Republish and BYO publish-from-paste call publish_notion directly, bypassing workflow sinks: obsidian-only workflows get Notion pages and never their configured sinks. (AUD-15) Done: the daemon's `PipelineLauncher.publish` now spawns `mp publish` (the `publish_router.fanout` command) instead of `mp publish-notion`, so Republish / Regenerate's tail / Save-and-Republish reach every configured sink; the page URL is read from the same `<stem>.notion.json` and is nil when Notion is not a sink. `publish_from_paste` routes its non-regulated path through the fanout (applying the workflow overlay + arming the egress guard first, mirroring `mp publish`, so an NDA workflow still drops Notion); the regulated path still preserves the hand-written `.md` and skips. The meta.json title override moved into a shared `publish_notion.apply_meeting_title` called by both `publish()` and `fanout`, so titles no longer diverge. cloudSecretPolicy is meeting-keyed (not subcommand-keyed), so secret passing is unchanged. pytest (321 green, CI-equivalent) + ruff + daemon build/test (847) green. |
| PIPE3 | Terminal skip states + startup job reconciliation | UX | DONE (was P1) | no_speech runs masquerade as success then decay to a futile Retry; daemon restart strands queued jobs invisibly; add `.skipped`/`.empty` states + a startup sweep. (AUD-16) Done: the Swift consumption half (LOCAL2 already wrote the `<stem>.empty.json` reason marker; no Python/contract change). (a) A new `EmptyReason`/`EmptyMarker` type is the single source of skip-reason wording; the Library pill + detail now read "No speech" vs "Unclear audio" off the marker (`Meeting.emptyReason`), and the done path posts an honest `Notifier.notifyEmptySkip` (no Notion, no completion chime) instead of the wrong-twice "Local Markdown ready (regulated mode)". (b) A startup `Coordinator.reconcileStrandedJobs()` (pure `StrandedJobRecovery.detect`) writes an honest `.interrupted` failure sidecar (new `PipelineFailureSidecar.Stage`) for a finished WAV whose pipeline never reached a terminal sidecar, so a restart-stranded job is retryable immediately instead of spinning `.processing` for 2 h then decaying to a generic Failed; `MeetingStore.unrecoveredFailureStems` now also counts age-inferred stale finals so the menu-bar badge matches the Library list. Deviation (owner-approved): stranded jobs are marked-interrupted + Retry, NOT auto-re-enqueued, because a cleanly-stopped meeting's REC2 recovery manifest is gone so the recorded BYO/auto mode is unknown and auto-reprocessing could egress a BYO meeting (REC2's no-auto-egress posture); the byo/too_long paste-ready completion keeps its legacy banner (pre-existing, out of scope). Side effect: the failed badge may jump once on first launch as previously-uncounted old strands are surfaced. `reconcileStrandedJobs` host wiring + `notifyEmptySkip` are build+review (END7/MIC11/REC4 precedent); the pure pieces are unit-tested. daemon debug + release build + `swift test` (899) green. |
| UX10 | Moment-of-loss surfacing for the prompt | UX | DONE (was P1) | Timeout-skip discards a detected meeting with zero surface, and the panel/HUD lack `.fullScreenAuxiliary` so full-screen meetings never see the prompt. (AUD-12/16c) Done: three surfaces close the silent timeout-skip. (1) Both floating panels (`MeetingPromptWindow`, `RecordingHUDWindow`) gain `.fullScreenAuxiliary` so the prompt + HUD appear over full-screen meeting Spaces. (2) On a `"skip"`-default prompt timeout, `Notifier.notifySkippedMeeting` posts a `Skipped <app>` banner with a Start recording action; tapping it rebuilds the `AppSource` from the notification `userInfo`, clears the bundle's cooldown + skip latch (`clearSuppression`, the manual-hotkey bypass), and starts the recording late. (3) `StatusBarController.setSuppressed` shows `Suppressed (<app>)` in the menu bar, polling the real skip latch (`isSkipLatched`) so the label lives exactly as long as the meeting is still detected and collapses to Idle ~15s after the call ends. Deliberately NOT a global `.suppressed` state transition (START1: that wedged detection for the whole meeting); the state machine stays `.idle` and the label is presentation-only. Scoped to the silent timeout-skip only - an explicit Skip / force-stop is unchanged (the user already knows they skipped). New pure `AppSource` <-> `userInfo` bridge pinned by `AppSourceTests`; the notification / menu / panel wiring is build + review (MIC11 / END7 precedent). daemon debug + release build + `swift test` (885) green. |
| LOCAL1 | Model-download completeness + resume | Local models | DONE (was P1) | A non-empty snapshot dir short-circuits as `.completed` forever, so an interrupted multi-GB download reports ready and every local run fails inside 120s timeouts. (AUD-20) Done: `ModelDownloadSupervisor.isCached` no longer treats any non-empty snapshot dir as complete; a new testable `isComplete(modelId:hubRoot:)` core verifies completeness with NO network call, rejecting the two interrupted-download signatures (a `blobs/*.incomplete` file, and a dangling snapshot symlink), so `ensure()` now spawns the resumable `mp prefetch-model` on a partial cache instead of reporting ready. `LocalModelPreloader.shouldPreload` shares the same check, so a partial cache no longer green-lights the warm-server preload either. The `.failed` menu row is now an actionable Retry (re-runs `ConfigRefreshCoordinator.ensureModelPrefetchIfNeeded`; the full error moved to the row tooltip). Deviation: chose the spec's "verify completeness" option over "always spawn prefetch" because the Python prefetch hits the HuggingFace API (`HfApi().repo_info`) on every call, so always-spawning would add a network round-trip to every launch and degrade offline, against the local-first / no-new-always-on-egress posture; the local check keeps the happy path network-free. Honest residual (documented in code): a download interrupted in the gap between finishing one file and starting the next leaves no local signature, so the Python side (with the network manifest) stays the authority whenever the local check returns false. No Python change (`prefetch_model.py` was already correct once reached). New `ModelDownloadSupervisorTests` pins six cases (complete -> true; `.incomplete` present, dangling symlink, empty, missing -> false; real-file layout -> true). daemon debug build + `swift test` (881) green. |
| LOCAL2 | Plumb local timeouts + transcript sanity checks | Local models | DONE (was P1) | 120s hardcoded timeouts contradict the 20-min watchdog budget; add config knobs and a degenerate-output guard before summarize. (AUD-21) Done: added `summarization.local_startup_timeout_sec` / `local_request_timeout_sec` config knobs wired into both `LocalSummaryClient` build sites (`local` + `auto`-fallback); the per-request read timeout now scales with `max_tokens` (`summarize_local.scaled_request_timeout`) so a long local generation is no longer cut off at 120s, with the daemon watchdog as the hard backstop. New `transcript_quality.transcript_issues` runs the cheap degenerate checks (no spoken text, n-gram repetition ratio, words-per-minute vs duration) and the orchestrator short-circuits a suspect transcript to `skipped: suspect_transcript` (terminal `.empty.json` marker carrying the true reason) instead of summarizing/publishing garbage. Conservative thresholds so a real meeting is never withheld. The distinct Library label for the new skip reason is left to PIPE3 (Swift currently keys off marker presence, showing "No speech"). pytest + ruff green. |
| SEC10 | Close the dogfood egress bypass | Security | DONE (was P1) | `mp dogfood` never applies the workflow overlay nor arms the egress guard and unconditionally POSTs transcripts to Anthropic, including regulated/NDA meetings. (AUD-23) Done: `mp dogfood`'s main now applies `apply_workflow_overrides` (resolving per-meeting `workflow_nda_mode`) and `arm_for_config` at entry, like every other LLM entry point. The gate sits before the API-key check, and uses `arm_for_config`'s return (True iff regulated/NDA) to refuse the run with rc 2 rather than POST the transcript to Anthropic; the armed guard is the structural backstop. Refuses on the zero-egress modes only (regulated_mode / workflow_nda_mode), NOT on `effective_backend == local`, so a user who simply prefers the local backend can still run the cloud-vs-local A/B. pytest (323 green, CI-equivalent) + ruff green. |
| CI2 | Cross-language sidecar contract fixture + schema_version | Tests | DONE (was P1) | meta.json is tested per-tree against duplicated literals and the reader is fail-open, so key drift silently turns local-only workflows into cloud publishes; add a golden fixture + schema_version. (AUD-24/28) Done: three committed golden sidecars in `daemon/Tests/MeetingPipeTests/Fixtures/meta-contract/` (source-only+regulated, full workflow, NDA) are now the single shared artifact both suites assert against, the ARCH4 chunker pattern: `MetaContractFixtureTests` (Swift) builds each shape and asserts it equals the committed JSON; `test_workflow_overlay.py` (Python) runs `apply_overrides` over the same files and asserts the resulting Config. A key rename/drop on either side breaks one suite, so the fail-open drift can no longer pass CI unseen. `schema_version: 1` is stamped on every JSON sidecar writer that lacked one: `MeetingMetaSidecar.build` (the contract, stamped only on a non-empty dict so the no-sidecar fallback still holds), `PipelineFailureSidecar` (error.json), and the pipeline receipts notion/obsidian/filesystem/lan/run/empty/apple_pending.json. Reader stays fail-open on the key (forward-migration stamp, not a gate). Two deliberate exclusions: `summary.json` is the pydantic `MeetingSummary` data schema (versioned through the model, not a marker), and `mute-timeline.json` already carries `version: 1`. CONVENTIONS.md sidecar table + the DOC6 handoff line updated. pipeline ruff + pytest (327 passed, +4 contract tests, the 14 pre-existing secrets-env failures unchanged) and daemon debug build + `swift test` (903) green. |
| DSN18 | Contrast CI gate + native-semantic-color guard | Design | DONE (was P1) | Add a computed-WCAG contrast test over the token pairs and reject bare `.secondary`/`.tertiary` text; locks in UX14 and exposes the ~141 native-semantic sites. Done: (a) `ContrastFloorTests` computes the WCAG 2.1 relative-luminance ratio from the real `MPColors` tokens (resolved per appearance via `performAsCurrentDrawingAppearance`, so it tests what ships, not a duplicated hex list) and pins every text-on-surface pair over paper/white-card/bgSunk in both appearances: `fg`/`fgMuted`/`fgSubtle` clear 4.5 (fgSubtle clears 3:1 in the shallow bgSunk well), the accent tokens clear 4.5 in light and 3:1 in dark, and the white button label clears 4.5 on the deep fills. Each ratio is pinned (accuracy 0.05) so a token edit that moves any pair fails the build. (b) A new `design-tokens` CI step rejects bare `.foregroundStyle/.foregroundColor(.secondary/.tertiary/.quaternary)` and `return .secondary` outside `Design/` (recursive scope, since the existing color/font guards used a shallow `daemon/Sources/*.swift` glob that never reached subdirectories); all 129 native-semantic text sites migrated to tokens (`.secondary`->`fgMuted`, `.tertiary`->`fgSubtle`, the `TranscriptDisplay.color` + neutral-pill + onboarding-radio specials likewise), leaving only the decorative `Color.secondary` malformed-hex dot fallbacks (not text, out of scope). (c) The per-pair ratio table is checked in at `docs/audits/dsn18-contrast-ratios.md` with the standing "contrast floor met" acceptance line. No token value changed (the measurement surfaced one owner-owed gap: dark-mode accents clear only 3:1, and `danger600` on a raised card in dark is 2.74; recorded as a deferred dark-accent pass per UX14's dark verify-only boundary). daemon debug + release build + `swift test` (907) green; the gate `git grep` returns zero. Visual eyeball owner-owed (daemon UI is not screenshotable here). |
| AI1 | Action-item lifecycle: resolved/done flag + aging | Intelligence | DONE (was P1) | Continues FEAT4. Add a resolved/done state to `ActionItem`, compute aging off the existing ISO due date, and teach `mp actions` open/closed/overdue filters. LLM-free, highest-leverage, both research tracks' top item. Done: `ActionItem.resolved` (bool, defaults to open) added to the Python schema and the Swift `MeetingSummary` mirror, tolerant of the legacy `done` spelling on both decoders; `mp actions` gained `--open` / `--closed` / `--overdue` with a day-count age on dated open actions; every publisher reflects the flag (Notion `to_do.checked`, the `[x]`/`[ ]` markdown checkbox in summarize / fs / obsidian, the BYO-paste round-trip, `actions.json` via `model_dump`). The flag lives in `<stem>.summary.json` (not meta.json, where action items never were), so it round-trips through a `publish_router.fanout` republish as an upsert with no duplicate; the daemon Edit-Save carries it through `EditableAction` so a text edit never reopens a closed action. pytest (323 green, the 14 pre-existing env failures unchanged) + ruff and the daemon debug build + `swift test` (849) green. No UI here (DV1/AI2 own that). |
| DV1 | Cross-meeting facts data-view | Data view | DONE (was P1) | The "data view" the owner sensed was missing. A new facts-altitude view reachable from the Library rail that aggregates `decisions[]` and open `actions[]` across the library read-only, each item linking back to its meeting, with a control to mark an action done. Continues FEAT4's missing UI half. Done: a new `LibraryScope.facts` rail row ("Facts", `list.bullet.rectangle`, last in the LIBRARY section) whose `includes()` returns false (a projection, not a list filter) and which takes the Library center column via a new `FactsView`. It loads every `<stem>.summary.json` off-main on appear + each `MeetingStore.revision` bump and renders two quiet native sections: open actions (`!resolved`, sorted soonest-due-first with an owner + aging label off the ISO `due` date, AI1's lifecycle) and decisions from meetings in the last 30 days, each row linking back to its source meeting (the Quick Find hook: scope -> All Meetings + select the stem). A per-action circle marks it done, writing `resolved: true` straight into that meeting's `summary.json` via `MeetingSummary.jsonObject()` (the SummaryTab edit-write template, atomic, no correction record) so it round-trips through a later republish as AI1's upsert; the row leaves optimistically and the watcher's revision bump confirms. Token-only (DSN18-compliant: `fg`/`fgMuted`/`fgSubtle`/`.mpSignal`/`.mpWarning`, no bare native semantics), VoiceOver labels on the done + open controls. `FactsView` is host-wired (build + review, the END7/MIC11/REC4 precedent); its load-bearing write path is the same `jsonObject()` + republish-upsert already pinned by `MeetingSummaryTests` and `test_publish_router`. README rail section updated. daemon debug + release build + `swift test` (907) green; DSN18 gate returns zero. Visual eyeball owner-owed. |
| [DONE] AI2 | Spike: on-device embedding index + long-context RAG latency | Intelligence | DONE (was P1) | Build an embedding index over the library and measure RAG-synthesis TTFT + small-model faithfulness at 4K/8K/16K context on the real Mac (MLX prefills fully before the first token, so the cited 6.9s is a short-prompt number). Returns a go/no-go on interactive vs async chat. Gates AI3; folds FEAT2's vector-RAG follow-up. Done: shipped `embed_index.py` (real on-device index, multilingual-e5-small on MLX, 2147 chunks over the library) + the `mp ai2-spike` harness; measured TTFT/wall-clock + needle faithfulness/citation at 4K/8K/16K on the real Mac for the 14B and 3B. Verdict: NO-GO for interactive live chat (ship AI3 async), recommended context budget ~4K on the 14B. The 14B at 16K OOM-crashed and hung the Mac (a finding); the 3B is fast-ish but cites unreliably. Full report: `docs/spikes/ai2-embedding-rag-latency.md`. |
| VALID1 | On-device acceptance measurements (owner-owed) | Validation | P1 | Carried from Q4 (DONE-partial). Scaffolding shipped (`scripts/valid1_check.py`, runbook); the five runtime measurements (A15 cold-start, A16, DIAR1 DER + step-ratio, SUM1-APPLE + Little-Snitch zero-egress, UX4 real failed SCStream) are owed by the user on a real Apple-Silicon Mac. Progress (engine-comparison run over 26 real meetings): the dogfood local-quality gate is produced (`docs/local-llm-quality.md`: DO NOT SHIP the 14B as the silent default, hand-graded ~0.42 action / ~0.52 decision capture vs Anthropic) and the MLX-vs-Apple-Intelligence read (the SUM1-APPLE quality+latency half) is in `docs/engine-comparison.md` (Apple Intelligence runs on-device but is minutes/meeting on its 4096-token context and lossy on Ukrainian). The actionable outcome: standardize the local backend on the 7B (`Qwen2.5-7B-Instruct-4bit`), which is on par with the 14B on capture but memory-safe (no OOM), ~30% faster, and better at owner naming. Still owner-owed: A15 cold-start, DIAR1 DER, UX4 degraded banner, and Little-Snitch zero-egress confirmation. |
| C6-FINISH | Real detection-trace corpus (owner-owed) | Detection | P1 | Carried from Q4 (DONE-partial). Redaction note + `scripts/redact_detection_trace.py` shipped; the 20+ real redacted dogfood traces are owed by the user (corpus is still 9 synthetic seeds). |
| [DONE] AI3 | Ask-AI: engine-backed cited answers + daemon surface | Intelligence | DONE | Continues FEAT2. Replace the `mp ask` TF-IDF ranker with engine-backed retrieval-augmented answers carrying `<stem>` citations, honour `effective_backend()` + the egress clamp, and surface it in the daemon (a QuickFind-style ask). Scope only after AI2 decides interactive vs async. Depends on AI2. Done: new `engine.complete_text` (shared free-form spine, honours `effective_backend()` + egress guard, no cloud fallback under regulated/NDA) + `rag.py` (context packing with `[stem]` markers + citation verification against the retrieved set) + rewritten `mp ask` (retrieve over the reused embedding index -> synthesize -> verify citations -> emit, `--out`/`--json`, always >=1 verifiable citation). Daemon: `PipelineDriver.ask` (spawns `mp ask --out`, reads the result sidecar) + a Library `Ask` rail scope hosting `AskView` (FactsView precedent, not the modal QuickFind), async spinner, citations link back to their meeting. Async per AI2 (NO-GO for live chat), ~4K context. pytest (373 pass, 14 pre-existing env failures unchanged) + ruff green; daemon debug + release build + `swift test` (922) green; DSN3/DSN18 gates return zero. |
| [DONE] AI4 | Weekly review digest (async, on-device) | Intelligence | DONE | A scheduled, locally generated digest of aging open actions and recent decisions, written to disk and publishable; the single-user-relevant slice of trend analysis, latency-tolerant because it runs in the background. Depends on AI1. Done: new `mp digest` aggregates open actions (reusing AI1's `mp actions` discovery) + recent decisions (last N days by meeting date), asks `engine.complete_text` (the AI3 spine) to narrate them, and writes the result as a `MeetingSummary` to a `digests/` sibling of the library (outside the scanned tree, so it never pollutes Facts/actions/ask). Facts stay deterministic (grounded, no hallucination); only the narrative is generated. Honours `effective_backend()` + the egress guard (regulated/NDA force local, no cloud fallback); degrades to a deterministic narrative if no engine is reachable, so a scheduled run never fails to produce output. `--publish` fans out through the existing sinks. No new always-on egress (a command you run/schedule). pytest (384 pass, 14 pre-existing env failures unchanged) + ruff green. |
| [DONE] DV2 | Spike: attendee quality for a People pivot | Data view | DONE | Inspect real `attendees[]` across the corpus, measure cross-meeting dedup hit-rate, and decide whether a People view is trustworthy now or must wait for FEAT3-ROSTER / CAL1. Verdict: NO-GO, confirmed on the real 165-meeting corpus. 65% of attendee values are diarization artifacts (`speaker_unknown`, bare indices, `speaker_N`) and cross-meeting dedup is 18% (34 of 189 names recur), so 2 of 3 promotion gates fail (artifact_ratio 0.65 vs <0.2, dedup 0.18 vs >=0.3). A real recurring-people signal exists but is buried under the artifacts; the blocker is structural (attendees inferred from `speaker_*` labels, no identity signal pre-FEAT3). People waits for FEAT3-VOICEPRINT -> FEAT3-ROSTER and/or CAL1. Shipped a re-runnable audit (`docs/spikes/dv2_attendee_audit.py`) + decision doc + the privacy-safe result aggregate (`docs/spikes/dv2-attendee-audit-results.json`). |
| [DONE] FEAT3-VOICEPRINT | Full speaker enrollment (voiceprint) | Feature | DONE | Promote the FEAT3 heuristic to a real voiceprint so "me" holds on mono/merged audio. Done: the enrollment/embedding API is present at the pinned 0.14.8, so no LOCAL5 bump was needed. Split by capability: the daemon (`FluidAudioRunner`) writes a 256-d duration-weighted mean embedding per diarized speaker into the draft `<stem>.json` (`speaker_embeddings`, from `result.segments`, so it is immune to the diarizer's cross-meeting speaker accumulation); the pipeline owns policy: a `VoiceprintStore` (`~/.config/meeting-pipe/voiceprint.json`, running-average, auto-enrolled from the mic-channel user of stereo recordings, capped after a few meetings) + `diarize.match_voiceprint` / `identify_user_speaker`, and `label_me_speaker` precedence is channel_me -> voiceprint_me -> dominant. `_finalize_streamed_transcript` consumes + strips the embeddings so the final transcript stays clean. Preferences -> Pipeline gains a read-only "Voice profile: learned from N meetings / Reset" (`VoiceprintProfile`). Enrollment UX = auto-learn (owner call; no new recording UI). daemon build + `swift test` (929) green; pipeline ruff + my pytest suites green (14 pre-existing secrets-env failures unchanged). CONVENTIONS + README + config.example.toml updated. |
| [DONE] FEAT3-ROSTER | Named multi-speaker enrollment + matching gate | Feature | DONE | Extend VOICEPRINT to a roster of named third parties so recurring people surface by name. Done, on VOICEPRINT's embedding foundation: a `RosterStore` (`~/.config/meeting-pipe/roster.json`, per-person capped samples reduced to up to 3 k-means centroids) with a two-gate accept rule (top cosine >= 0.65 AND runner-up margin >= 0.07, biasing to leave-unknown over a wrong name); `diarize.resolve_speaker_labels` maps each speaker to me / a roster name / a stable `THEM-A/B` unknown cluster; `orchestrate` applies it at finalize and persists per-speaker embeddings keyed by final label to `<stem>.embeddings.json` so naming can enroll later; `mp roster enroll/list/forget` (enroll reads that sidecar, folds the embedding in, relabels the transcript). Daemon: a "Name this speaker" affordance in the Library transcript tab spawns `mp roster enroll` end-to-end (PipelineDriver.rosterEnroll -> MeetingLibraryService -> Coordinator -> LibraryWindowModel), unknown clusters render as "Unknown A". Acceptance test pins: enroll one person, a later meeting names them, an unknown voice stays a THEM cluster, me untouched. daemon build + `swift test` (931) green; pipeline ruff + pytest (roster/diarize/orchestrate suites green; 14 pre-existing env failures unchanged). CONVENTIONS + README updated. |
| [DONE] FEAT3-FINISH | Surface `user_label` enrollment in Preferences | Feature | DONE | Carried from Q4 (FEAT3 DONE-partial). The MVP wired `summarization.user_label` but it was TOML-hand-edit only. Done: a "Your name" field in Preferences -> Pipeline bound to a new `ConfigStore.summarizationUserLabel` that round-trips `summarization.user_label` (the `summaryLanguage` pattern), the key documented in README's Configuration reference + `config.example.toml`, and the two ConfigStore round-trip tests extended. No pipeline change (`diarize.label_me_speaker` already consumes the field). daemon build + `swift test` (922) green. |
| MIC7 | Browser/PWA mute oracle via a stable platform signal | Detection | P2 | Browsers have no AX oracle; read the platform's own mute state (extension / DOM aria-state), Google Meet first. Spike the Meet DOM signal's stability first. Full spec in q4-final.md. |
| [DONE] REC5 | Invalidate the SCShareableContent cache | Recording | DONE (was P2) | Cached at launch, never cleared, reused by every retry; dock/undock can make system-audio start fail repeatedly. (AUD-17) Done: `SystemAudioCapture` now clears the static `cachedContent` on two triggers: a `start()` failure (so the next `retrySystemAudio()`, which builds a fresh capture, re-fetches instead of reusing a snapshot naming a now-dead display) and an `NSApplication.didChangeScreenParametersNotification` (dock/undock, resolution change), observed once from `prewarm()`. Re-fetching `SCShareableContent` does not re-prompt Screen Recording TCC, so the invalidation is safe under any architecture. Deviation from the spec's "do the dock/undock repro first": that physical repro is owner-owed (not reproducible here), but the fix (dropping a cache that has no business surviving a failure or a display change) is correct regardless; the confidence-65 caveat was about whether the bug exists, not whether the fix is safe. `SCShareableContent` can't be constructed in XCTest, so the SCK wiring is build + `swift test` (926) green + review (REC4 / MIC11 precedent); the behavioural dock/undock confirmation stays owner-owed. |
| [DONE] SEC8 | Tokens in Keychain | Security | DONE (was P2) | Move NOTION_TOKEN / ANTHROPIC_API_KEY out of secrets.env into the macOS Keychain; closes SEC1 properly. Done: both tokens (plus optional HF_TOKEN) now live in the login Keychain (generic-password, service `com.meetingpipe.daemon`, account = env-var name), read/written by all three trees through the stable `/usr/bin/security` CLI so a single accessor owns the item ACL (no per-access prompt, no per-rebuild cdhash churn; native `SecItem` would churn on the ad-hoc cdhash). New `KeychainSecrets` + injectable `SecretsBackend`/`KeychainBackend`; `SecretsStore` is Keychain-backed and migrates a legacy `secrets.env` once at daemon startup, then deletes it (`install.sh` does the same at install and prompts for any missing required token; `uninstall.sh --purge` clears the items). `PipelineLauncher.freshEnvironment` drops the per-spawn file read (tokens are seeded into the daemon process env at launch + on each Preferences save, so subprocesses inherit them); Python `load_secrets` reads the Keychain via `security` for hand-runs and `mp doctor` checks the Keychain. SEC1 subsumed: the mode-0600 warnings (`secretsFileIsTooOpen`, `_secrets_too_open`) are gone since there is no plaintext file. Deviation from the one-line spec (owner-approved): the legacy file is deleted, not kept-blanked, and `install.sh` + README gained a token-prompt path. daemon debug + release build + `swift test` (926) green; pipeline ruff + pytest (439) green. |
| [DONE] SEC11 | Log/repo hygiene sweep | Security | DONE (was P2) | events.jsonl is 0644 with verbatim titles; dogfood runs/ and secrets.env.writing are not git-ignored; HF model fetches float main; doctor pings cloud under regulated mode. Done, four parts: (1) the event log + tail logs are now created 0600 in `Logger.appendData`, with a startup `Log.tightenLogPermissions()` sweep tightening pre-existing 0644 files, and `events.py` chmods `pipeline_events.jsonl` to 0600 (idempotent, self-heals the old file). (2) `.gitignore` `secrets.env` -> `secrets.env*` catches the `.writing` atomic-write temp (`runs/` was already ignored in an earlier partial). (3) `prefetch_model` now pins `snapshot_download` to the commit sha resolved by the `repo_info` call it already makes (was floating the mutable `main`) and records the revision in the started/complete events; offline falls back to the unpinned default. (4) `mp doctor` skips the live Anthropic/Notion/HuggingFace probes under `regulated_mode` and says why (zero-egress). Sibling PERF7 (events.jsonl size cap) stays open, a different fix. pipeline ruff + pytest (448) and daemon build + `swift test` green. |
| [DONE] LOCAL3 | Hierarchical Apple Intelligence reduce | Local models | DONE (was P2) | The only backend exempt from the long-meeting guard concatenates all partials into one ~4K-context reduce call; re-chunk and reduce in rounds. (AUD-22) Confirmed by the engine comparison (`docs/engine-comparison.md`): the flat reduce overflows on real meetings, and a second facet, the 3200-char window itself overflows the 4096-token context on Ukrainian (~1.7 tokens/char). `daemon/scripts/ai-summarize.swift` prototypes the fix (smaller Cyrillic-safe windows + hierarchical batched reduce); scope is to port both into `AppleIntelligenceSummarizer.swift`. Part of LOCAL8. Done: both facets ported from the prototype. `maxWindowChars` 3200 -> 1000 (Cyrillic-safe map window), and the flat single reduce replaced by a `reduceBatch`-of-4 hierarchical reduce (partials merged in rounds until one remains, so no reduce call concatenates every partial). New pure `batched(_:size:)` helper carries the partition invariant and is unit-tested; the async `generate()` stays build+review (needs the on-device model, the existing AppleIntelligence precedent). daemon build + `swift test` (12 AppleIntelligence cases) green. LOCAL8's experimental gate + LOCAL7 language work still open. |
| [DONE] LOCAL4 | Doctor preflight for the local stack | Local models | DONE (was P2) | `mp doctor` never checks mlx importability, model cache, RAM, or disk against the chosen model; refuse-with-explanation beats first-run OOM. Done: new `check_local_stack(cfg)` in `doctor.py` (stdlib only) reports a `== local summarization stack ==` section - mlx installability via `importlib.util.find_spec` (no heavy Metal import), model-cache presence/size (reusing `prefetch_model._hf_cache_dir`/`_bytes_on_disk`), physical RAM (`sysctl hw.memsize`, os.sysconf fallback) vs a per-model resident estimate parsed from the repo id, and free disk vs the download size, each FAIL/WARN with the numbers so a first-run OOM is caught up front. Runs under local/auto/regulated backends, info-only skip otherwise. Pure `_estimate_model_gb` + gating unit-tested (test_doctor.py). ruff + pytest green. |
| [DONE] LOCAL5 | Pin FluidAudio version + track ANE risk | Local models | DONE (was P2) | Package.swift floats from 0.12.4 (resolved 0.14.8); pin exactly, evaluate the 0.15.x bump carrying the streaming + enrollment models (FEAT3-VOICEPRINT/ROSTER, I8 depend on it), and track upstream #738 (macOS 27 ANE access) and #726. Done: `Package.swift` pinned `exact: "0.14.8"` (the resolved version; `Package.resolved` is gitignored, so the manifest is the pin). 0.15.x bump deferred with rationale in the manifest comment: FEAT3-VOICEPRINT/ROSTER already shipped on the enrollment/embedding API present at 0.14.8, so no bump is needed now; #738 (macOS 27 ANE access) and #726 are named as the gate to re-check before any future bump (live issue status not chased from here). I8 (live transcription) remains P3 and can pin its own bump when promoted. daemon build + `swift test` green (resolved dep unchanged, so binary identical). |
| [DONE] LOCAL6 | Standardize the local backend on the 7B | Local models | DONE (was P1) | Outcome of the engine comparison (`docs/engine-comparison.md`): Qwen2.5-7B-4bit is the local sweet spot, on par with the 14B on action capture (~0.56 vs ~0.61 hand-graded over 26 real meetings) but with named owners (not speaker labels), zero failures (vs the 3B's 1/26), ~30% lower latency, and ~4.3 GB resident so it sidesteps the 14B OOM that hung the Mac. Bump the config default `summarization.local_model` to `mlx-community/Qwen2.5-7B-Instruct-4bit` and relabel the Preferences presets (Small 3B / Recommended 7B / Large 14B); drop the 32B "max" preset (the 14B already over-shoots RAM here). `mp dogfood --local-model` (shipped) is the A/B tool. Done: the 7B default is set in all three default sites (`config.py`, `summarize_local.DEFAULT_MODEL`, and ConfigStore's Swift fallback); the presets are relabeled Small 3B / Recommended 7B / Large 14B with the 32B dropped. The picker matches the stored model id against `preset.modelId`, so a config still holding the dropped 32B id falls through to "Custom" with the id intact (no silent rewrite); a 14B user shifts from Recommended to Large with no data change. README + config.example.toml doc lines updated. No test pinned the old 3B default. pipeline + daemon build/test green. |
| LOCAL7 | Local summaries honor summary_language on non-English meetings | Local models | P2 | The engine comparison found the local backends ignore `summary_language = auto` on Ukrainian: the 7B drifts uk->en, the 14B uk->ru; only Anthropic preserves Ukrainian. Strengthen the local language directive (`summarize_local` plus the Apple Intelligence instructions) and add a cheap detected-language-mismatch retry so a uk transcript yields a uk (or the configured-language) summary. |
| LOCAL8 | Apple Intelligence: experimental gate + viability fixes | Local models | P2 | The engine comparison showed the macOS 26 Apple Intelligence backend is native/free/zero-egress but not usable as a daily summarizer today: its 4096-token context forces heavy chunking (minutes/meeting), it echoes raw dialogue on short meetings, ignores the bullet cap (~11 vs 5), mislabels Ukrainian, and 1/5 failed. Keep the code but mark it experimental / not-recommended in Preferences (so it is not silently chosen), and track the fixes that make it viable: LOCAL3 (reduce + Cyrillic window), LOCAL7 (language), bullet-cap adherence, the short-meeting transcript-echo. Revisit-when: those land OR Apple widens the system-model context. Owner kept it (not dropped) as the only native zero-egress path and the I7 trigger. |
| CAL1 | Spike: EventKit calendar context (re-evaluate ADR 0011) | Detection | P2 | On-device EventKit metadata as hints only (pre-title, adaptive end thresholds, preflight warnings), never a trigger. Spike: does the user's calendar actually carry their meetings? Could seed FEAT3-ROSTER names. |
| DOC6 | Document the undocumented sidecars | Docs | DONE (was P2) | mute-timeline.json, .capturemode, and originals/ are absent from CONVENTIONS/ARCHITECTURE's on-disk inventory; add them plus schema_version to the writers. (AUD-28) Done: added a "Daemon-internal recording artifacts" section to CONVENTIONS.md documenting the exact shapes (`<stem>.mute-timeline.json` = `{version, spans:[{start_sec,end_sec}]}`; `<stem>.capturemode` = one of `capture_first`/`capture_first_redact`/`regulated_gate`; `originals/<stem>.wav` = the 0600, backup-excluded recovery copy), each with when it is written/read and why it is not part of the Swift-to-Python contract; and three matching rows to ARCHITECTURE.md's on-disk inventory. The `schema_version`-on-writers half is left to CI2 (cross-referenced in both files), since CI2 owns "add schema_version: 1 to all sidecar writers" plus the golden-fixture contract test; `mute-timeline.json` already carries `version: 1`. Docs-only, no code change. |
| DSN3 | Font-literal migration + one-button-language | Design | Absorbed (was P2) | Carried from Q4 (DONE-partial); colors + the CI guard landed. The 2026-07 redesign absorbs the remainder: the ~135 `.system(size:)` literal migration lands in DSN23, the one-button-language unification in DSN24 (with the named record-key exception). See `design/REDESIGN.md`. |
| DSN9 | Preferences pixel retune (owner-owed design review) | Design | Absorbed (was P2) | Carried from Q4 (DONE-partial); the audit + token-align landed. The pixel retune lands in DSN28 as part of the 2026-07 redesign port, reviewed against the locked mockups. See `design/REDESIGN.md`. |
| DSN15 | Liquid Glass: SDK recompile + conditional chrome | Design | Absorbed (was P2) | The 2026-07 redesign's Liquid Quiet substrate is this task grown up: the macOS 26 SDK recompile (macOS 14 floor kept) + glass on floating chrome only ships in DSN25, and the pending owner sign-off folds into that task. See `design/REDESIGN.md`. |
| DSN19 | Surface + overlay consolidation | Design | Absorbed (was P2) | The MPSurface card primitive + per-theme overlay token ship in DSN24 (the 2026-07 redesign control kit). See `design/REDESIGN.md`. |
| DSN20 | Redesign pass A: lock the Liquid Quiet substrate | Design | P1 | First lock of the owner-approved 2026-07 redesign (hybrid: Liquid Quiet substrate + Instrument capture controls). Retoken `colors_and_type.css` and restyle all six kit mockups to the macOS 26-era substrate; add the missing `RecordingHUD.jsx`. Design-only, no Swift. Exact prompt + ground rules: `design/REDESIGN.md` (the spec owner for DSN20-29). |
| DSN21 | Redesign pass B: lock the Instrument capture controls | Design | P1 | Delta on the locked DSN20: circular record key with travel, LED-segment meters, SF Mono numerals, mechanical motion, new on-air token family; capture surfaces only (prompt, HUD, dropdown record states, Library record control). Prompt in `design/REDESIGN.md`. Depends on DSN20. |
| DSN22 | Redesign pass C: lock the Library UX upgrades | Design | P1 | The 2026-07-05 Library audit's mockup-expressible fixes: tinted inline fix actions, Needs-you grows partial + empty, one paste-pending verb, publish-failure reason inline + a real Publish-anyway button, Reprocess in the header menu, an INSIGHTS rail group for Facts/Ask, "Kept local" re-voice, edited-meeting marker, Facts-arrival context line, transcript-edit hint, batch zero-selection state. Prompt in `design/REDESIGN.md`. Pairs with UX15 (the functional half). Depends on DSN20. |
| DSN23 | Redesign port: tokens + contrast re-pin + font-literal sweep | Design | P1 | Port the locked CSS values into `Design/Tokens.swift`, re-pin `ContrastFloorTests` pair by pair, and migrate the ~135 `.system(size:)` literals to `MPType` (load-bearing: the ramp changed). Absorbs DSN3's literal half. Depends on DSN20-22. |
| DSN24 | Redesign port: control kit | Design | P1 | MPButton capsule geometry, RecordKey, LED meter, mechanical toggle, MPSurface primitive + per-theme overlay token. Absorbs DSN19 and DSN3's one-button-language half. Depends on DSN23. |
| DSN25 | Redesign port: prompt panel + recording HUD + macOS 26 glass | Design | P1 | Restyle `MeetingPromptWindow` + `RecordingHUDWindow` to the locked mockups; build against the macOS 26 SDK (macOS 14 floor kept); glass on floating chrome only. Absorbs DSN15. Depends on DSN24. |
| DSN26 | Redesign port: menu bar + dropdown states | Design | P1 | `StatusBarController` icon + menu states to the locked look. Depends on DSN24. |
| DSN27 | Redesign port: Library chrome + UX + a11y hardening | Design | P1 | Library surfaces to the locked DSN20/DSN22 mockups, including the DSN22 UX changes, VoiceOver labels on status pills, and keyboard focus for inline fix buttons. Depends on DSN24; pairs with UX15. |
| DSN28 | Redesign port: Preferences retune | Design | P1 | `PreferencesControls` + panes to the locked look, the DSN9 retune landing here with the owner eyeballing rendered pixels. Absorbs DSN9. Depends on DSN24. |
| DSN29 | Redesign close-out: docs + specimen cards + re-sync | Design | P1 | Rewrite DESIGN.md's named rules and `design/README.md` to the shipped system, regenerate `design/preview/` cards, align PRODUCT.md wording, re-sync the Claude Design project. Prompt in `design/REDESIGN.md`. Depends on DSN25-28. |
| UX15 | Library triage honesty | UX | P1 | Functional half of the 2026-07-05 Library audit: the Needs-you scope also surfaces `.partial` and `.empty`; the scan computes a has-corrections flag the row + header render; the publish-failure reason from the error sidecar surfaces in the detail header; paste-pending uses one verb (Reveal bundle) in list and detail. Spec pointers in `design/REDESIGN.md`; pairs with DSN22/DSN27. |
| STOR1 | Audio library storage governance | Storage | P1 | Stereo WAV is ~0.7 GB/hour and `raw/` grows unbounded (165+ meetings); MIC13 covers only `originals/`, PERF7 only event logs. Per-workflow audio retention policy (keep forever / transcode to FLAC or AAC after N days / drop audio post-summary), a reaper sharing MIC13's mechanism, a Preferences Storage section (library + HF model cache + waveform cache sizes, evict unused models), and disk numbers in `mp doctor`. |
| SEC12 | iCloud/cloud-sync egress check on the library path | Security | P1 | The default `~/Documents/Meetings/` sits inside iCloud Desktop & Documents scope when that setting is on, so recordings silently upload, an OS-level hole in the zero-egress claim (ADR 0016 mandates exclusion for `originals/` only). Detect a synced library root (iCloud, Dropbox-style providers), WARN in doctor + Preferences naming the provider, hard-FAIL under regulated/NDA, offer an assisted move; never move without confirmation. |
| ASR1 | Post-ASR glossary / custom vocabulary | Pipeline | P1 | Recurring proper nouns and en/uk code-switched terms are mangled every meeting and the damage propagates into summaries, embeddings, ask, and roster display. Deterministic substitution at transcript finalize (global + per-workflow TOML), Unicode-aware whole-word matching, applied before summarize/embed; new meetings only (transcripts stay write-once). |
| FEAT8 | In-meeting moment flag | Feature | P1 | One global hotkey during recording stamps a timestamp marker (the HUD acknowledges with a single quiet blink): markers land in a `<stem>.markers.json` sidecar, render as anchor chips in the transcript (click = seek), and reach the summarizer as user-flagged moments. LLM-free capture, no new egress class. |
| STOR2 | Backup + restore manifest | Storage | P1 | The irreplaceable state (library, corrections corpus, voiceprint, roster, workflows) spans five roots plus the Keychain with no backup story. `mp backup` / `mp restore` over the non-rebuildable roots, a manifest naming the Keychain items to re-create (secret values never exported), a doctor last-backup line, and a new-Mac migration runbook in the README. |
| UX16 | One search story: merged on FTS5 | UX | P2 | Decided 2026-07-05 (was "decide merge vs label-and-cross-teach"): merge. Cmd+K Quick Find becomes the single search surface, backed by a SQLite FTS5 index over full transcripts + summaries (the old TECH-A3 upgrade path; the stale README pointer is repaired with this adoption); the filter bar keeps its chips and its free-text field feeds the same index. The index is a rebuildable cache over files (ADR 0003). After DSN27. |
| WF6 | Rebuild WorkflowEditor on house primitives | Workflow | P2 | The editor body still uses SwiftUI stock chrome rather than the SettingsGroup/SettingsRow design system, which is the residual "editing a workflow feels off". Builds on WF2-5. Full spec in q4-final.md. Sequence after DSN28 so the editor lands on the redesigned primitives once, not twice. |
| T2 | Portable visual-regression tests (or close) | Tests | P2 | Carried from Q4 (not-done): the swift-snapshot-testing dependency + SnapshotTests.swift + PNG refs were removed (commit de3bf35, non-portable across macOS/Xcode on the macos-15 runner) and replaced by construction-only smoke tests. Decide: close T2 as won't-do, or adopt a CI-portable visual-regression approach. |
| DIST1 | Bundle a runtime for a drag-n-drop installer | Distribution | P2 | Embed a relocatable Python + the wheels + a static ffmpeg and notarize, so a clean Mac (no Homebrew, no Python) can install by dragging the app. Also what locked-down regulated Macs need. Full spec in q4-final.md. |
| AI6 | Recurring-series continuity for summaries | Intelligence | P2 | Inject the same workflow's previous meeting (decisions + still-open actions, post-AI1 state) as a bounded context block into summarize, with instructions to mark carried-over items instead of re-extracting them as new. Workflow is the series key; prompt-only, no schema change, no egress change. |
| AI4-FINISH | Digest surfacing: schedule toggle + Digests view | Intelligence | P2 | AI4 shipped CLI-only with hand-rolled cron, the FEAT2/FEAT4 audit pattern. A Preferences digest schedule (installs/removes a LaunchAgent invoking `mp digest`, plus Generate now) and a read-only Digests row under the INSIGHTS rail group so digests are readable where meetings are reviewed. |
| MIC14 | Off-the-record manual redaction spans | Privacy | P2 | A hotkey toggles an off-the-record span mid-recording; spans ride the existing mute-timeline + MuteRedactor machinery tagged `manual` (always redacted, exempt from MIC12's speech-withhold), the HUD shows a truthful state while active, and the regulated/NDA gate path zero-fills live instead. |
| LOCAL9 | Corrections Phase 3: local LoRA fine-tune | Local models | P2 | `mp corrections-stats` advertises a Phase 3 readiness gate no task owns. An owner-invoked `mp train-adapter` (MLX LoRA on the 7B over the corrections corpus), gated on the ~20-correction bar, `mp dogfood` A/B as the ship/no-ship gate, adapter opt-in via config and never silently applied. Fully on-device. |
| FEAT3-SEGMENT | Per-segment speaker reassignment | Feature | P2 | Roster naming fixes whole clusters; a misattributed segment cannot be moved and diarization has no user knobs. Right-click a transcript segment to reassign it (me / roster name / unknown) via the FEAT3-ROSTER relabel path; label fix only in v1 (no centroid side effects), regenerate/republish offered, never auto-fired. |
| PIPE4 | Backend-aware long-meeting routing | Pipeline | P2 | The 80k guard protects the Anthropic bill but is backend-blind: `backend = local` meetings over the threshold dead-end in a paste bundle even though summarizing locally is free (only Apple Intelligence is exempt, per LOCAL3). Make the guard cloud-only and route long local meetings through a hierarchical map-reduce on the MLX path (generalize LOCAL3's batched reduce, Cyrillic-safe windows, ~4K per-call budget per AI2); the paste bundle stays as the cloud-over-threshold path and the failure fallback. |
| UX18 | In-app paste completion for BYO / long-meeting bundles | UX | P2 | Completing a paste-pending meeting means hand-saving `<stem>.summary.md` and a CLI call. Add "Paste summary..." to the paste-pending detail pane: paste, parse through the publish-from-paste parser with inline validation, publish through the PIPE2 fanout with the workflow overlay + egress guard armed. CLI path stays; DSN22's Reveal bundle stays the manual hatch. |
| WF8 | Post-hoc workflow reassignment | Workflow | P2 | Matching is record-time only and manual recordings always fall to default, so a misrouted meeting is stuck with the wrong context prompt, sinks, and NDA posture. "Change workflow..." in the detail menu rewrites the meta sidecar's `workflow_*` fields, then offers Regenerate / Republish; NDA transitions confirm explicitly (a move into NDA cannot un-egress past publishes and the UI says so). |
| PIPE5 | Notion sink property enrichment | Pipeline | P2 | The sink writes Name/Date/Status while attendees, workflow, duration, and open-action count sit on disk. Probe the target DB schema and fill matching optional properties by name + type, fail-soft (absent = skip, type mismatch = skip with one log line), never creating or mutating DB schema; a bare DB behaves exactly as today. |
| PROV1 | Pluggable cloud providers: claude CLI first, then OpenAI API | Providers | P2 | Formalize the provider seam over the two call shapes (structured summarize + `engine.complete_text`), then add `claude_cli` (headless `claude -p`, no tools, parsed through the existing pydantic path; zero marginal cost on a Claude subscription, pairs with PIPE4) and an `openai` API provider (httpx, Keychain key, egress-guard-clamped). A CLI provider egresses via a child process outside the httpx guard, so `effective_backend()` classifies every CLI provider as cloud and refuses it under regulated/NDA fail-closed. codex CLI only if the CLI shape proves out. |
| PERF5 | 4 Hz engine-tick gating | Performance | P3 | Carried from Q4 (DONE-partial). Poll backoff landed and is wired into the three 1 Hz pollers; the 4 Hz engine-tick gating (expose PromotionEngine pending-deadline state, arm/disarm the coordinator tick on engine-state changes across engineQueue<->main) is the deferred remainder. |
| PERF6 | Gate discovery-scan AX walks behind a cheap precondition | Performance | P3 | The deeper lever under the 2026-06-30 idle-energy finding: Activity Monitor showed MeetingPipe spiking ~8-13% CPU while idle, and `sample` put the bursts in `MeetingSourceScanner.enumerateCandidates` doing three recursive AX tree walks per running meeting app (`MeetingAXHandleBuilder.findToolbar`/`findButton`, dominated by `mach_msg2_trap` AX IPC). The fixed 3 s discovery backstop re-walks Teams' large AX tree every 3 s all day even when no meeting is plausible; PERF5's backoff only ever covered the in-meeting 1 Hz pollers, never discovery. Fix (kept separate from PERF5, whose remainder is the unrelated 4 Hz engine-tick gating): in `collectSignals`, gate the three button walks (`findCallingControlsToolbar` + `findAllLeaveButtons` + `findAllMuteButtons`) behind a cheap precondition (a meeting-titled window from `collectAXWindowTitles`/`isActiveMeetingWindow`, or active process audio), so an idle backgrounded meeting app skips the expensive walk. Changes signal-collection semantics, so validate: `DetectionCorpusTests` green and a real meeting still scores (dogfood / `mp analyze-detection`). Pairs with the lighter first-line fix, adaptive backoff on the discovery backstop poll. |
| PERF7 | Event-log rotation / size cap | Performance | P3 | `events.jsonl` grows unbounded (63 MB / 338k lines on the dogfood Mac), dominated by the 1 Hz `ax_leave_button_state` + mute-state events written for the whole of every meeting; no rotation or trim exists. Add a size- or age-based rotation (or ring-buffer trim) for `events.jsonl` / `pipeline_events.jsonl` so the logs self-bound while `mp logs` / `mp analyze-detection` keep working on the recent window. Pure hygiene (per-meeting disk-write energy + unbounded growth), no detection change. Sibling to SEC11's `events.jsonl` `0644`/gitignore hygiene, a different fix (mode vs size). |
| AI5 | Spike: meeting-type classification | Intelligence | P3 | Prototype on-device meeting-type labels over the library (a short local call or a heuristic over attendees/cadence/title); the owner judges usefulness before any permanent schema field. Uncommon among competitors; spike before committing. |
| UX17 | Playback speed + skip-silence | UX | P3 | The player is 1x-only (the Fit/1x/2x/4x/8x menu is waveform zoom, not rate). A pitch-corrected rate control (1x / 1.25 / 1.5 / 2x) shared by the Transcript and Audio tabs, plus an optional skip-silence hop over gaps the peaks cache already knows; the transcript highlight stays synced at every rate. |
| CAL2 | Pre-meeting prep card | Feature | P3 | A user-invoked "Last time" affordance on the prompt panel (and `mp prep <workflow>`): the matched workflow's last summary + open actions. Quiet register, never a popup. The workflow-keyed version needs nothing new; calendar-aware timing and attendee seeding depend on CAL1. |
| AUTO1 | Automation surface: App Intents + URL scheme | Automation | P3 | Expose start/stop/BYO record, open Library, ask, and digest via App Intents (Shortcuts) and a `meetingpipe://` scheme so Raycast / Stream Deck can drive the daemon. External triggers respect the same gates as the hotkey (permissions, cooldowns); local IPC only, no new egress. |
| FEAT9 | Merge fragmented recordings | Feature | P3 | A dropped-and-rejoined call yields two stems, two summaries, two pages. A batch "Merge into one meeting..." concatenates audio (the REC1 verified-outcome pattern), merges transcripts with a gap marker, re-summarizes, republishes under the primary stem, soft-deletes the fragment; same-workflow and matching-NDA fragments only. |
| WF7 | Per-workflow summary sections | Workflow | P3 | Workflows season extraction but cannot change the output shape (a 1:1 wants "feedback given / received"). Workflow-defined extra sections (name + instruction in the workflow TOML) landing in an optional `extra_sections` list on the typed model (tolerant decoders both sides, every publisher renders); promote when a concrete workflow needs one. |
| MIC8 | Spike: UI-independent native mute signals (logs/SDK) | Detection | P3 | Investigate whether Zoom/Teams local logs or SDKs expose mute transitions that survive every UI redesign; if found, supersedes that tool's AX read in MIC6/MIC10. The strongest futureproofing. Full spec in q4-final.md. |
| END5 | Browser/PWA end-signal hardening | Detection | P3 | Browser end rests on window/workspace/title only and is untested; when browser usage appears, add a Meet DOM/extension end signal (pairs with MIC7). Promotion trigger: a real browser meeting recorded. |
| I7 | Drop Python entirely | Deferred | P3 | Promotion trigger: Apple Intelligence proves out as the Swift-native local summarizer. See DIST2 (q4-final.md). Update: the engine comparison (`docs/engine-comparison.md`) shows Apple Intelligence has not proven out (slow + lossy on its 4096-token context), so the trigger remains unmet; gated behind LOCAL8. |
| I8 | Live transcription during recording | Deferred | P3 | The streaming floor now exists natively in FluidAudio (streaming ASR + Sortformer/LS-EEND on the ANE). De-risk via two-pass. Still P3 (a post-meeting tool may not need live). Depends on LOCAL5. |
| G1 | Personal two-Mac hub | Deferred | P3 | All P0/P1 dogfood bars met first. |
| D8 | Developer ID + notarization in CI | Deferred | P3 | Partly overlapped by DIST1; full notarization-in-CI parked until a second user. The durable fix for the per-build TCC churn. |
| CAP1 | Mic/system end-of-call skew | Deferred | P3 | Monitor only; promotion trigger is the few-second skew reappearing across multiple recordings. Spec retained in q3-final.md. |
| BRAND1..9 | Launch readiness band | Brand | P3 | Domains, trademark, landing page, demo GIF, screenshots, OG card, compliance pages. Selling is tertiary; do not pull forward without an explicit launch decision. |

## Task specs

The bands below carry Q4's audit specs forward unchanged; their full text is in [`q4-final.md`](./q4-final.md) (search the id). Full specs are written here only for the new (`AI*`, `DV*`) and changed items.

### Intelligence layer (new, AI band)

Origin: two deep-research reports ([local AI intelligence](../research/2026-06-26-local-ai-intelligence.md), [meeting-level UX](../research/2026-06-26-meeting-level-ux.md)). The competitor scan (Granola, Notion AI, Otter, Fathom, Fireflies, tl;dv, Reflect, Mem, Hyprnote) found that ask-across-all chat is now table stakes but the genuinely under-served seams are action-item lifecycle (capture-but-never-tracked) and scheduled digests; trend dashboards and speaker analytics are team-shaped and skipped for a single user. Hyprnote is the posture twin (fully on-device, BYO-LLM) and shares meeting-pipe's exact gaps, which is the differentiation opening. The load-bearing feasibility risk is long-context generation latency: MLX prefills the full context before the first token, so the lowest-risk first builds are latency-tolerant (the lifecycle is LLM-free; the digest runs in the background), and anything interactive (chat) is gated behind a measured spike. Every AI item routes through the local engine path so `egress_guard` clamps it under regulated/NDA and honours `effective_backend()`, adding no new always-on egress.

**[DONE] AI1 (P1): action-item lifecycle, resolved/done flag + aging.** The highest-leverage, lowest-risk item and the named next step in `pipeline/src/mp/actions.py`; both research tracks ranked it first. Add an optional `resolved`/`done` state to `ActionItem` (`pipeline/src/mp/schemas.py`) tolerant on both decoders, compute aging off the existing ISO `due` date, and teach `mp actions` `--open` / `--closed` / `--overdue` with an age for dated open actions. Mirror the schema change in the Swift `MeetingMetaSidecar` contract and every publisher (touch both sides + `test_workflow_overlay.py`). Acceptance: `mp actions --open/--closed/--overdue` works with aging on dated actions; the flag round-trips through the sidecar across a republish (upsert, no duplicate); pipeline pytest + ruff and the daemon build are green. Continues FEAT4. No UI here (that is DV1/AI2's surface plus the close control in DV1). Done: `ActionItem.resolved` (pydantic `bool`, default `False`, `validation_alias` accepting the legacy `done` spelling, serialized as `resolved`) is mirrored on the Swift `MeetingSummary.ActionItem` decoder + `jsonObject()` write-bridge, both tolerant of an absent flag (legacy items decode as open). One correction to the spec's wording: action items live in `<stem>.summary.json`, not the `MeetingMetaSidecar` (`meta.json`) workflow sidecar, so the "two decoders" are `schemas.py` and `MeetingSummary.swift`, and the round-trip works because `publish_router.fanout` re-reads `summary.json` on republish (it never regenerates it). `mp actions` gained the mutually-exclusive `--open`/`--closed`/`--overdue` filters and a stdlib-`date` age (day count off `due`, "Nd overdue"/"in Nd"/"due today") shown for dated open actions, plus `resolved`/`age_days`/`overdue` in `--json`. Publishers reflect the flag: Notion `to_do.checked`, the `[x]`/`[ ]` checkbox in `summarize._render_summary_md` / `publish_fs` / `publish_obsidian`, the BYO-paste `[x]` parse, and `actions.json` via `model_dump`. The daemon Edit-Save threads `resolved` through `EditableAction` so a text correction never reopens a closed action. Tests: `test_schemas`, `test_actions` (filters + aging + alias), `test_publish_fs` / `test_publish_obsidian` / `test_publish_notion_blocks` (checkbox/checked), `test_publish_router` (republish upsert round-trip), `test_publish_from_paste` (markdown round-trip), a `test_workflow_overlay` guard that the meta-overlay does not shadow per-action state, and Swift `MeetingSummaryTests` (decode both spellings + `jsonObject` round-trip). pytest 323 passing (the 14 pre-existing secrets-env failures unchanged), ruff clean, daemon debug build + `swift test` (849) green.

**[DONE] AI2 (P1, spike): on-device embedding index + long-context RAG latency and faithfulness.** Gates everything chat-shaped. Build an on-device embedding index over the library (reusing the local engine; embeddings are cheap, ~tens of K tok/s on M-series) and a small vector index, then measure RAG-synthesis time-to-first-token and total wall-clock at 4K / 8K / 16K context on the user's actual Mac, plus a faithfulness/citation-accuracy read on multi-document answers. The cited 6.9s figure is a short-prompt number; MLX full-prefill collapses throughput at longer context, which is exactly RAG's worst case. Acceptance: an index is built; measured TTFT + wall-clock at the three context sizes and a citation-accuracy read are recorded on the real hardware; the spike returns a go/no-go on interactive chat vs async-only and a recommended context budget. Folds FEAT2's deferred vector-RAG follow-up. Depends on nothing; gates AI3. Done: shipped `embed_index.py` (pluggable embedder: `MLXEmbedder` multilingual-e5-small on MLX for production, numpy `HashingEmbedder` for CI; chunked via `chunked_windows`; cosine search; the durable artifact AI3 imports) and `mp ai2-spike` (streaming TTFT capture, needle-in-haystack faithfulness/citation, per-size incremental results, go/no-go recommender). Built a 2147-chunk index over the 168-meeting library (~94s). Measured on the real Mac, cache-busted: 14B steady TTFT ~31s at 4K (citation 1.00) but 8K aborted under memory pressure and 16K OOM-crashed + swap-stormed the machine; 3B 7.8s/17s/41s at 4K/8K/16K (faithful 1.00 but citation 0.00 to 0.33). Prefill throughput falls with context (the predicted MLX collapse). Verdict: NO-GO for interactive live chat, so AI3 is async, 14B for citations, context budget ~4K (16K unsafe on this Mac); also found the embedding model must load with `local_files_only` / `HF_HUB_OFFLINE` so it does not trip the egress guard under regulated/NDA. New dependency `mlx-embeddings` (darwin/arm64, lazy, not in CI; user-approved). New tests `test_embed_index.py` + `test_ai2_spike.py`. pytest + ruff green; full report in `docs/spikes/ai2-embedding-rag-latency.md`.

**[DONE] AI3 (P2): Ask-AI, engine-backed cited answers + a daemon surface.** Continues FEAT2 (the CLI-only `mp ask` TF-IDF MVP). Replace the ranker with retrieval-augmented answers that carry at least one verifiable `<stem>` citation, route through `httpx` so the egress guard clamps it, and use the forced-local engine with no cloud fallback under regulated/NDA. Surface it in the daemon (a QuickFind-style "ask my meetings" entry, quiet register, no sparkle). Scope the UX (interactive vs async) only after AI2. Acceptance: a natural-language answer with a real citation, within AI2's latency budget, clamped under regulated/NDA; pytest + ruff green; the daemon surface is reachable. Depends on AI2. Done: (a) `engine.py` is a new shared free-form-text spine (`complete_text`) that resolves the backend via `effective_backend()` and routes through httpx, so regulated/NDA force local with no cloud fallback and the egress guard clamps either path; `apple_intelligence` (Swift-only) degrades to the local MLX path for ask/digest, logged. (b) `rag.py` packs retrieved chunks with `[stem]` citation markers and verifies the model's citations against the retrieved set, so a hallucinated tag is dropped and the answer always carries >=1 real citation (falling back to the top retrieved meeting when the model cites nothing valid). (c) `embed_index.load_or_build` reuses a persisted index, rebuilding only when a source fingerprint or the embedder model changes, so a repeat ask is fast. (d) `mp ask` is rewritten: retrieve -> pack (~4K tokens, AI2's budget) -> synthesize -> verify -> emit, with `--out` (a result JSON the daemon reads) / `--json` / `--model` / `--rebuild`. Daemon: `PipelineDriver.ask` spawns `mp ask --out <tmp>` and parses `AskAnswer` (the `runMP` sidecar-read pattern, since it reports only success/failure); a new `LibraryScope.ask` rail row hosts `AskView` in the center column (the DV1/FactsView host-wiring, not the modal QuickFind, which cannot render prose + citations), async with a spinner (AI2 returned NO-GO for live chat), each verified citation linking back to its meeting; quiet register, no sparkle. One spec deviation, named: "QuickFind-style" is realized as a Library-hosted Ask view (FactsView precedent) rather than the ephemeral QuickFind picker, because the picker cannot host an answer + citation chips; the surface is still a fast cross-library ask. Tests: `test_engine` (backend routing + regulated/NDA force-local + auto fallback), `test_rag` (packing + citation verify/hallucination-drop), `test_ask` (retrieve->verify->emit, `--out`, empty, engine-error), `test_embed_index` (fingerprint + load_or_build reuse/rebuild), Swift `AskAnswer` decode contract + `LibraryScope.ask` + `MeetingLibraryService.askMeetings`. pytest 373 pass (14 pre-existing secrets-env failures unchanged) + ruff green; daemon debug + release build + `swift test` (922) green; DSN3/DSN18 gates return zero. README `mp ask` + Library rail sections updated. Daemon UI eyeball owner-owed (menu-bar app is not screenshotable here).

**[DONE] AI4 (P2): weekly review digest, async and on-device.** A scheduled, locally generated digest of the week's aging open actions and recent decisions, written to disk and publishable through the existing fanout. The single-user-relevant slice of "trend analysis", and latency-tolerant because it runs in the background. Acceptance: a digest is generated on-device from decisions + open actions (a first version needs no RAG), routed through `httpx` so the egress guard clamps it under regulated/NDA and honours `effective_backend()`; no new always-on egress. Depends on AI1. Done: new `mp digest` (`digest.py`). It aggregates the deterministic facts (open actions via AI1's `mp actions` discovery + filter; recent decisions from `<stem>.summary.json` for meetings dated in the last `--since` days), then narrates them through `engine.complete_text` (the shared AI3 spine, no RAG), so generation honours `effective_backend()` and the egress guard clamps it under regulated/NDA with no cloud fallback. The facts are grounded (never hallucinated); only the narrative summary comes from the model, and if the engine is unreachable the digest still generates with a deterministic narrative so a scheduled run never fails. The digest is modelled as a `MeetingSummary` written to a `digests/` sibling of the library (a token-only decision: keeping it out of the scanned `raw/` tree so its rolled-up actions/decisions do not double-count in Facts / `mp actions` / ask); `--publish` fans it out through the existing sinks (Notion slug from the `digest-YYYYMMDD` stem, so a same-day re-run upserts). Scheduling is left to a user `launchd`/cron entry invoking `mp digest` (no new always-on egress; on-device by default). New tests `test_digest.py` (fact collection windowing/sorting, engine-narrated vs deterministic-fallback, assembly grounding, `main` writes to the digests dir not the library, `--publish` fans out). pytest 384 pass (14 pre-existing secrets-env failures unchanged) + ruff green. README `mp digest` section added.

**AI5 (P3, spike): meeting-type classification.** Prototype on-device meeting-type labels over the existing library (a short local call, or a heuristic over attendees / cadence / title) and show the results to the owner before committing a permanent `MeetingSummary` field. Uncommon among competitors (only MeetGeek auto-detects). Acceptance: a throwaway classifier labels the library by type; the spike returns a recommendation on whether the labels are useful enough to wire into grouping/digests and earn a schema field; no schema change lands until that decision.

### Meeting-level data view (new, DV band)

Origin: the [meeting-level UX report](../research/2026-06-26-meeting-level-ux.md). Verdict: meeting-pipe earns a meeting/facts-altitude view, but as a derived, read-only, zero-egress projection built from facts already on disk (ADR 0003: the filesystem is the source of truth; a data view must be an index over files, not a new database of record), not a CRM. The competitor scan (Granola People/Companies, Obsidian Bases `attendees.contains(this)`, Otter My Action Items, Notion saved views) converges on lightweight derived entity pages + an action inbox; the heavy coaching/scorecard end is out of register. The Library today is date/status/workflow-only (`LibraryScope`) with no person/decision/topic pivot.

**[DONE] DV1 (P1): cross-meeting facts data-view.** (See the TOC row for the implementation note.) The "data view" the owner sensed was missing, and FEAT4's missing UI half. A new facts-altitude view reachable from the Library rail (a new `LibraryScope`) that aggregates `decisions[]` and open `actions[]` (post-AI1, with their resolved state) across the whole library read-only, each row linking back to its meeting `<stem>`, plus a control to mark an action done (persisted through the sidecar so it survives a republish). Quiet register, native idioms (a `Table` or grouped list, not a dashboard). Acceptance: the view lists open actions and recent decisions across meetings, each navigates to its source meeting, an action can be marked done from here and the state round-trips through AI1's sidecar; no a11y or token regressions; daemon builds debug + release. Depends on AI1.

**[DONE] DV2 (P2, spike): attendee quality for a People pivot.** Before building a People view, inspect real `summary.json` `attendees[]` across the corpus and measure the cross-meeting dedup hit-rate (how often the same person is named consistently). Acceptance: a written read on attendee data quality and dedup rate, and a decision on whether a People pivot is trustworthy now or must wait for FEAT3-ROSTER (voiceprint identity) / CAL1 (calendar attendee seeding). No People view ships until this says the data supports it. Done: shipped a re-runnable stdlib audit (`docs/spikes/dv2_attendee_audit.py`) that classifies each `attendees[]` value as a real name vs a diarization artifact (`speaker_*` / bare index) and measures fill rate, artifact ratio, distinct names, cross-meeting recurrence, and the dedup hit-rate, plus the decision doc (`docs/spikes/dv2-attendee-quality.md`). Verdict: NO-GO, confirmed on the owner's real 165-meeting library (transferred as a privacy-safe aggregate, `docs/spikes/dv2-attendee-audit-results.json`; raw names never left that machine). 878 attendee mentions: 65% are diarization artifacts (`speaker_unknown`, bare cluster indices, `speaker_N`) and only 35% are candidate real names (189 distinct, 34 recurring across >=2 meetings), for an 18% cross-meeting dedup rate. Against the proposed bar (>=15-20 summaries, artifact_ratio < ~0.2, dedup_hit_rate >= ~0.3) the corpus size passes but the two quality gates fail (0.65 and 0.18). The load-bearing finding is structural, not sample-size: `attendees[]` is model-inferred from FluidAudio `speaker_*` labels with no identity signal for un-named speakers pre-FEAT3, which the real data confirms is the dominant blocker; the field cannot carry a trustworthy cross-meeting roster until voiceprint identity (FEAT3-VOICEPRINT -> FEAT3-ROSTER) turns `speaker_N` into names or calendar seeding (CAL1) supplies clean ones. A genuine recurring-people signal does exist (34 people), so this is "not yet," not "never"; the 18% is also an exact-match floor (fuzzy-dedup of name variants like "Sarah"/"Sarah Chen" would lift it, but not past the 65% artifact gap). A People view stays deferred; DV1 (Facts, identity-free) is the DV theme's shipped value.

### Redesign 2026-07 (new, DSN20-29 + UX15/UX16)

Origin: the owner-approved 2026-07-05 redesign proposal (three explored directions; the chosen hybrid is the Liquid Quiet substrate + the Instrument capture-control language) plus a same-day Library UX audit. Owner-promoted to P1, the AI1/DV1 precedent. **The single spec owner is [`design/REDESIGN.md`](../../design/REDESIGN.md):** the exact Claude Design prompts (passes A/B/C and the close-out), the kit ground rules, the amended DESIGN.md rules, and the port map. Backlog rows stay one-liners; do not restate specs here.

Sequencing: DSN20 -> DSN21 + DSN22 (both delta the locked A; C does not need B) -> DSN23 -> DSN24 -> DSN25/26/27/28 (parallelizable) -> DSN29. Every design pass ends in an owner eyeball + a lock commit; every port task is one `/tech-task` session, one commit, `swift build` + `swift test` + `rebuild.sh` + owner eyeballs both modes (visual changes need eyes; the daemon is not screenshotable). Absorptions, so the trail stays readable: DSN3 -> DSN23/DSN24, DSN9 -> DSN28, DSN15 -> DSN25, DSN19 -> DSN24. UX15 is the audit's functional half (triage honesty) and can land before or alongside DSN27; UX16 is the search-story decision, after DSN27. WF6 sequences after DSN28.

### Feature review 2026-07 (new: STOR/PROV/AUTO bands, ASR1, and additions across existing families)

Origin: an owner-commissioned review of the shipped surface against this backlog (2026-07-05), deduplicated against the archived quarters, the two 2026-06-26 research reports, and the redesign band; this section is the review's record. The finding: correctness, detection, and design are well covered, and the gaps cluster in **archive stewardship** (the product accumulates an irreplaceable corpus, corrections, voiceprint, roster, library, with no storage, backup, or sync-safety story) and in **half-closed loops** (corrections advertise a LoRA phase no task owns; digest and long-meeting flows dead-end in CLI or manual steps). The owner approved all twenty review items on 2026-07-05 and proposed the provider seam (PROV1) in the same pass. Bucket mapping: must -> P1, should -> P2, nice -> P3 (P3 here means promote on demand, per the AI5/I8 precedent). The standing AI/DV rule applies to every item that touches an engine: route through the local engine path, honour `effective_backend()`, clamp under regulated/NDA, add no new always-on egress.

**STOR1 (P1): audio library storage governance.** Stereo WAV is ~0.7 GB per hour and `raw/` grows unbounded; MIC13 covers only the `originals/` recovery copies and PERF7 only the event logs. Scope: a per-workflow audio retention policy (keep forever / transcode to FLAC or AAC N days after a terminal published-or-skipped state / drop audio keeping transcript + summary), a reaper sweep sharing MIC13's mechanism (one reaper, two scopes), a Preferences Storage section (library size, per-policy breakdown, HuggingFace model-cache size with an evict-unused-models control, waveform-cache size), and disk numbers in `mp doctor`. Consumers must survive a transcode: playback and the waveform peaks re-derive from the new file (the cache is keyed on size + mtime, so it self-invalidates) and the Library's audio affordances follow the extension. Defaults are conservative: every workflow starts on keep-forever until the owner opts it in, and a policy never touches a non-terminal meeting (`Needs you` members are exempt). Acceptance: a meeting past its window is transcoded or reaped on the next sweep with playback and waveform intact; delete cascades stay correct against MIC13; Preferences and doctor report real sizes; keep-forever workflows are byte-unchanged.

**SEC12 (P1): iCloud/cloud-sync egress check on the library path.** The default `~/Documents/Meetings/` sits inside iCloud's Desktop & Documents scope when that setting is on, so every WAV and transcript silently uploads to Apple, an OS-level hole in the zero-egress claim; ADR 0016 already mandates iCloud/Time-Machine exclusion for `originals/`, but the main library has no check. Scope: detect a cloud-synced library root (iCloud Desktop & Documents state, plus Dropbox / Google Drive / OneDrive path patterns), WARN in `mp doctor` and in a Preferences Storage status line naming the provider, and hard-FAIL the doctor check under `regulated_mode` or when any NDA workflow exists; offer an assisted move to a non-synced root (guided, never performed without explicit confirmation) or a per-folder exclusion where the provider supports one. README documents the caveat. Acceptance: a synced library path is flagged with the provider named; regulated + synced is a FAIL, not a note; the assisted move yields a working library at the new root with config updated.

**ASR1 (P1): post-ASR glossary / custom vocabulary.** Recurring proper nouns, client and product names, and en/uk code-switched terms are mangled by ASR every meeting, and the damage propagates into summaries, embeddings, ask, and roster display. Scope: a deterministic substitution layer applied at transcript finalize in the pipeline, before summarize/embed ever see the text: a global glossary plus an optional per-workflow table (TOML under `~/.config/meeting-pipe/`, the workflows precedent), case-insensitive whole-word matching that is Unicode-aware (Cyrillic word boundaries), longest-match-first, with applied-substitution counts logged to pipeline events. Transcripts stay write-once: the glossary applies to new meetings at finalize; re-applying to an old meeting is an explicit regenerate, never a mutation. An optional bounded-edit-distance fuzzy stage for names ships behind a flag, off by default (a wrong substitution is worse than a missed one). Any new `uk` entries are verified with the user (the locale note above). Acceptance: a glossary term is normalized consistently across transcript, summary, and the embedding index; per-workflow entries apply only to that workflow's meetings; zero egress impact (a pure local transform).

**FEAT8 (P1): in-meeting moment flag.** The one in-meeting capability the competitor set has that meeting-pipe lacks, and it fits the register: one global hotkey (configurable, chosen in-task alongside `manual_hotkey`) during a recording stamps a timestamp marker; the HUD acknowledges with a single quiet blink (opacity-only, reduced-motion honoured), no persistent chrome. Markers are written at stop as `<stem>.markers.json` (schema_version stamped, documented in CONVENTIONS, the DOC6 precedent); the transcript tab renders each marker as an anchor chip at its timestamp (click = seek, the existing play-head path); summarize receives the flagged excerpts (the segments spanning each marker) as user-flagged moments with an instruction to reflect them, so capture is LLM-free and deterministic while emphasis is model-side. BYO and long-meeting paste bundles include the marker list. No new egress class: flagged excerpts travel to whichever backend already summarizes the transcript. Acceptance: a hotkey press mid-recording lands a marker in the sidecar; the chip seeks correctly; a flagged moment demonstrably surfaces in the summary; works under regulated/NDA; visual eyeball owner-owed.

**STOR2 (P1): backup + restore manifest.** The compounding assets are irreplaceable and scattered: the library (`raw/` + sidecars + `digests/`), `~/.config/meeting-pipe/` (config, workflows, voiceprint, roster, glossary), the corrections corpus in `~/Library/Application Support/MeetingPipe/`, and the Keychain items; caches are rebuildable and excluded. Scope: `mp backup <dir>` writes a dated archive of the non-rebuildable roots plus a manifest naming the Keychain items to re-create (secret values are never exported); `mp restore <archive>` unpacks to the configured roots (per ADR 0003 a manual copy also works, and the runbook says so); `mp doctor` reports last-backup age when a backup marker exists; a README migration runbook (new Mac: install, restore, re-add the Keychain items, done). Acceptance: a backup restored into a scratch HOME yields a working library, workflows, roster, and corrections state; the manifest names every Keychain item; doctor reports backup age; the runbook is verified by walking it.

**AI6 (P2): recurring-series continuity.** Summaries of recurring meetings re-extract known context and lose the thread between sessions. Scope: at summarize time, inject the same workflow's previous meeting as a bounded context block (its decisions plus its still-open actions, post-AI1 resolved state), with instructions to mark carried-over items as carried over rather than re-extracting them as new discoveries. The workflow is the series key (explicit, already resolved per meeting); no fuzzy series inference. Prompt-only in v1, no schema change; the block is bounded (~1K tokens, latest prior meeting only) and assembled from disk, so cloud and local backends get it identically and the egress profile is unchanged. The first meeting in a workflow is a clean no-op. Acceptance: the second meeting in a workflow produces a summary that acknowledges the carried-over open action instead of re-listing it as new (dogfoodable via `mp dogfood`); regulated/NDA behaviour unchanged.

**AI4-FINISH (P2): digest surfacing, schedule toggle + Digests view.** AI4 shipped CLI-only with scheduling left to hand-written cron, exactly the CLI-no-UI pattern the Q4 validation audit flagged on FEAT2/FEAT4. Scope: (a) a Preferences digest control (enable + weekday/time) that installs/removes a per-user LaunchAgent invoking `mp digest` (the daemon already manages a LaunchAgent; reuse the pattern), plus a Generate now button; (b) a read-only `Digests` row under the INSIGHTS rail group (DSN22's group; the FactsView/AskView host precedent) listing the `digests/` sibling with the standard summary rendering, reveal-in-Finder, and delete. If it lands before DSN27, a plain rail row is fine and the INSIGHTS grouping arrives with the port. No new egress (the digest already routes through the engine + clamp). Acceptance: the toggle installs a LaunchAgent that fires `mp digest` at the configured time; digests render in the Library; disabling removes the agent; visual eyeball owner-owed.

**MIC14 (P2): off-the-record manual redaction spans.** Mute redaction only protects spans where the meeting app was muted; the human cases (a side conversation, an interruption, "this next part stays out") have no affordance. Scope: a hotkey toggles an off-the-record span mid-recording; spans are recorded into the existing mute-timeline machinery tagged `source: manual` (`<stem>.mute-timeline.json` grows a per-span source field, version-bumped and documented), and MuteRedactor redacts them in the transcribed, summarized, and published artifact while the full recording stays local for recovery, the ADR 0016 posture. Two invariants: a manual span is explicit intent, so it is always redacted and exempt from MIC12's speech-bearing withhold; and under the regulated/NDA real-time gate path the hotkey zero-fills live instead (no unredacted audio at rest there). The HUD shows a truthful persistent state while active ("Off the record") so it cannot be forgotten, and an open span auto-closes at stop. Acceptance: span audio is absent from transcript, summary, and every published artifact and present in the local original (capture-first path); the HUD state is truthful; the timeline round-trips; MIC12's guard leaves manual spans alone.

**LOCAL9 (P2): corrections Phase 3, local LoRA fine-tune.** `mp corrections-stats` advertises a Phase 3 readiness check (~20 corrections over ~200 minutes) that no backlog task owns; make it real or stop advertising it. Scope: an owner-invoked `mp train-adapter` that fine-tunes a LoRA on the standard 7B via the MLX LoRA path, building prompt/completion pairs from the corrections corpus (original vs corrected summaries); evaluation is `mp dogfood` A/B (base vs adapter) on held-out meetings, hand-graded like the engine comparison; the adapter ships only through an explicit config opt-in (`summarization.local_adapter_path`), never silently applied. Fully on-device, no egress; training is a command the owner runs, no background training. An honest terminal outcome is allowed: if the A/B shows no capture win, close as adapter-not-worth-it, keep the corpus as a prompt-context asset (few-shot exemplars are the cheaper fallback lever), and fix the README wording. Gated on the corpus bar, which is owner-owed grading volume. Acceptance: with the bar met, a training run produces a loadable adapter; the dogfood scorecard decides adoption; README's corrections section describes the real flow either way.

**FEAT3-SEGMENT (P2): per-segment speaker reassignment.** Roster naming fixes whole clusters, but a misattributed segment (diarization splitting or merging people mid-meeting) cannot be moved, and the README troubleshooting entry offers no user-side recourse. Scope: right-click a transcript segment, "Reassign to..." (me, a roster name, or unknown), applied through the FEAT3-ROSTER relabel path so the transcript, summary attendees, and later meetings stay consistent. v1 is a label fix only: no embedding is folded into any roster centroid on reassignment (a wrong fold poisons the centroid; enrollment stays the explicit "Name this speaker..." path), and the existing regenerate/republish affordances are offered afterward rather than auto-fired. Acceptance: a reassigned segment renders under the right speaker and persists across rescans; a subsequent republish reflects it; roster centroids are byte-identical after a reassignment.

**PIPE4 (P2): backend-aware long-meeting routing.** The 80k-char guard exists to protect the Anthropic bill, but it is backend-blind: `backend = local` meetings over the threshold dead-end in a paste bundle even though summarizing them locally is free (LOCAL3 confirms only Apple Intelligence is exempt today). Scope: make `skip_above_chars` gate the cloud backend only; when the effective backend is local (chosen, auto-resolved, or forced by regulated/NDA), a long transcript routes through a hierarchical map-reduce on the MLX path, generalizing LOCAL3's batched-reduce pattern out of `AppleIntelligenceSummarizer` into `summarize_local`, with Cyrillic-safe windows and the ~4K per-call context budget AI2 measured as safe (16K OOM-crashed the Mac). The paste bundle remains the cloud-over-threshold behaviour and the failure fallback, and the daemon watchdog stays the hard backstop. Acceptance: a >80k-char meeting on the local backend produces a summary end-to-end with no `READY_FOR_MANUAL`; the anthropic path is unchanged; a long Ukrainian meeting does not overflow the window (the LOCAL3 finding); memory stays bounded on the real Mac (owner-eyeballed, the VALID1 precedent).

**UX18 (P2): in-app paste completion for BYO / long-meeting bundles.** Completing a paste-pending meeting today means hand-saving `<stem>.summary.md` next to the transcript and running `mp publish-from-paste`, the only core flow that leaves the app. Scope: the paste-pending detail pane gains "Paste summary...", opening a sheet (raw markdown box, parsed through publish-from-paste's existing parser into the typed model, with inline validation errors); on confirm it publishes through the PIPE2 fanout with the workflow overlay applied and the egress guard armed, exactly like the CLI path, which stays supported. DSN22's "Reveal bundle" verb remains the manual escape hatch. Acceptance: a paste-pending meeting goes to published entirely in-app; an NDA workflow's paste still drops Notion (the PIPE2 behaviour); malformed input surfaces inline instead of failing silently; visual eyeball owner-owed.

**WF8 (P2): post-hoc workflow reassignment.** Matching happens once at record time and manual hotkey recordings always fall to the default, so a misrouted meeting is permanently stuck with the wrong context prompt, sinks, Notion DB, and NDA posture. Scope: "Change workflow..." in the detail `...` menu rewrites the `workflow_*` fields of `<stem>.meta.json` (the Swift writer; the CI2 golden-fixture contract catches drift), refreshes the row's chip and scope membership, and then offers the two follow-ups with plain-English consequences: Regenerate (the context prompt changed) and Republish (the sinks or DB changed). NDA transitions are the sharp edge and confirm explicitly: moving into NDA re-clamps everything from now on but cannot un-egress past publishes (the sheet says so); moving out of NDA is a deliberate downgrade and requires confirmation. Acceptance: a reassigned meeting republishes to the new workflow's sinks and DB; scope counts update; the meta contract fixtures gain the rewrite case; no automatic regenerate or republish without the user choosing it.

**PIPE5 (P2): Notion sink property enrichment.** The sink writes a three-property husk (Name/Date/Status) while attendees, workflow, duration, and the open-action count all sit in the sidecars, so the database the owner actually lives in cannot filter by any of it. Scope: probe the target database schema (reuse the workflow editor's cached `notion-databases.json` pattern) and fill matching optional properties by name and type when present, e.g. `Workflow` (select), `Attendees` (multi-select), `Duration` (number, minutes), `Open actions` (number), `Source` (select); fail-soft everywhere: an absent property is skipped silently, a type mismatch is skipped with one log line, and the sink never creates or mutates database schema. The README's schema table documents the optional set. Acceptance: a database carrying the optional properties gets them filled on publish and updated on republish (the upsert path); a bare Name/Date/Status database behaves byte-identically to today; the regulated no-op is unchanged.

**PROV1 (P2): pluggable cloud providers, claude CLI first, then OpenAI API.** Owner-proposed 2026-07-05. Today the backend set is anthropic / local / auto / apple_intelligence, each wired point-to-point. Two motivations: cost, a Claude subscription already pays for Claude Code, so spawning headless `claude -p` summarizes at zero marginal API cost (and, with PIPE4, removes the long-meeting bill entirely on that backend); and flexibility, a BYO OpenAI-compatible key. Scope, in order: (1) formalize the provider seam over the two existing call shapes, structured summarize and `engine.complete_text`, so a provider is one module implementing both; (2) `claude_cli`: spawn `claude -p --output-format json` with tools and MCP disabled, non-interactive, parsed and validated through the existing pydantic path, with `mp doctor` preflighting binary presence, auth state, and version; (3) `openai`: an API provider over httpx (so the egress guard clamps it like anthropic), key in the Keychain via the SEC8 pattern, model id in config; (4) codex CLI only if (2) proves the CLI shape stable. The load-bearing sharp edge: a CLI provider egresses through a child process, outside the in-process httpx egress guard, so `effective_backend()` must classify every CLI provider as cloud and refuse it under regulated/NDA fail-closed at the policy layer (the SEC10 posture), with tests asserting the CLI is never spawned in those modes; `auto` keeps its anthropic-then-local ladder and never falls back into a CLI provider silently. This amends the README's "Messages API directly, not Claude Code" rationale, which predates headless `-p`; record the amendment (a short ADR or a note on ADR 0007). Acceptance: `backend = claude_cli` summarizes end-to-end with no API key configured; doctor preflights it honestly; regulated/NDA never spawn a CLI; the openai provider passes the same egress-guard tests as anthropic; the Preferences backend picker gains the entries.

**UX16 (P2, decision recorded 2026-07-05): one search story, merged on FTS5.** The open question ("merge vs label-and-cross-teach") is decided: merge. Cmd+K Quick Find becomes the single search engine, backed by a SQLite FTS5 index over full transcripts and summaries (the deferred TECH-A3 upgrade path; the README pointer to that retired id is repaired with this adoption), and the filter bar keeps its chips while its free-text field feeds the same index. The index is a rebuildable cache over the files (ADR 0003: an index over files, not a database of record), built incrementally off the existing sidecar-watch revision bumps. Still sequenced after DSN27.

**UX17 (P3): playback speed + skip-silence.** The player is 1x-only; the Fit/1x/2x/4x/8x menu is waveform zoom, not rate (the q3 TECH-UI-10 note). Add a pitch-corrected rate control (1x / 1.25 / 1.5 / 2x via `AVAudioUnitTimePitch` on the existing engine) shared by the Transcript and Audio tabs, plus an optional skip-silence hop that jumps gaps the peaks cache already knows about. The transcript highlight and play-head math stay synced at every rate. Acceptance: rate applies live, survives tab switches, and the active transcript line tracks correctly at 2x.

**CAL2 (P3): pre-meeting prep card.** Before a recurring meeting, the relevant context (the matched workflow's last summary and its open actions) is a hunt through the Library. Add a user-invoked "Last time" affordance on the prompt panel (and `mp prep <workflow>` for the shell), quiet register, never a popup. The workflow-keyed version needs nothing new; calendar-aware timing and title/attendee seeding depend on CAL1, so sequence the full version behind that spike.

**AUTO1 (P3): automation surface, App Intents + URL scheme.** The daemon is driveable only by two hotkeys and the menu bar; the stated user reads CLI flags faster than landing pages. Expose start/stop/toggle record (with a BYO variant), open Library (optionally scoped), ask, and digest via App Intents (Shortcuts) and a `meetingpipe://` URL scheme, so Raycast and Stream Deck ride for free. External triggers respect exactly the gates the hotkey does (mic permission, cooldowns, no start while recording); local IPC only, no new egress. Acceptance: a Shortcut toggles a recording end-to-end and a denied-mic trigger routes to the Permissions tab instead of silently failing.

**FEAT9 (P3): merge fragmented recordings.** A dropped-and-rejoined call yields two stems, two summaries, two published pages. A batch-selection "Merge into one meeting..." concatenates the audio (ffmpeg, the REC1 verified-outcome pattern: nothing is deleted until the merged artifact is verified on disk), merges transcripts with an explicit gap marker, re-summarizes, republishes under the primary stem (the upsert path), and soft-deletes the fragment locally with a note about its published page. Guardrails: same-workflow fragments only, NDA pairing must match. Acceptance: two fragments become one meeting with continuous playback, one summary, one page.

**WF7 (P3): per-workflow summary sections.** Workflows season extraction (`context_prompt`) but cannot change the output shape; a 1:1 workflow may want "feedback given / received", a client workflow "billable follow-ups". Scope: workflow-defined extra sections (name + instruction pairs in the workflow TOML) landing in an optional `extra_sections` list on the typed model (ADR 0014), tolerant decoders on both sides, rendered by every publisher and read-only in the editor at first. Heavier than it looks (schema, publishers, editor, and the BYO-paste round-trip all touch it), which is why it sits at P3 until a concrete workflow needs one.

### Carried from Q4 with a changed scope

**MIC6 (P1): attribute-based futureproof mute read.** Carried from Q4 DONE-partial. The four shipped parts (cross-locale category-precedence fallback, `AXMuteButtonProbe` element re-arm after N `.unknown`, decoupled `MeetingAXWindowWatcher` blind-clear, verified `uk` labels) are in place and tested. The owed remainder is part 1's headline: read the mute button's stable state attribute (AXValue / pressed-state / mark-char) instead of scraping the localized title, so the read survives UI redesigns and locale. Gated on an owner-run live AX dump (`daemon/scripts/ax-dump-meeting.swift`) to pick the per-app attribute + polarity. Overlaps MIC10 (re-lock the live control) and MIC8 (the durable non-UI signal); sequence with whichever live Teams session enables the dump.

**END6 / END7 (P1):** these already carry the END2 and END3 validation residuals respectively (see the TOC rows); no separate END2/END3 items remain.

**T2 (P2): portable visual-regression tests, or close.** Carried as not-done. The Q4 image-snapshot approach (swift-snapshot-testing + `__Snapshots__` PNGs) was removed in commit de3bf35 as non-portable across macOS/Xcode on the macos-15 CI runner, and replaced by `RenderedViewSmokeTests` that only assert a view constructs without crashing. Decide one of: (a) close T2 as won't-do and keep the smoke tests as the bar, or (b) adopt a CI-portable visual-regression approach (perceptual tolerance tuned for the runner, or a layout-assertion harness instead of pixels). Pairs with the DSN3/DSN9 visual-verification needs.

**[DONE] FEAT3-FINISH (P2):** the FEAT3 MVP wired `summarization.user_label` but it was TOML-hand-edit only. Done: a "Your name" text field in Preferences -> Pipeline bound to a new `ConfigStore.summarizationUserLabel` that round-trips `summarization.user_label` (mirroring `summaryLanguage`), the key documented in README's Configuration reference and `config.example.toml`, and the two ConfigStore round-trip tests extended. No pipeline change (`diarize.label_me_speaker` already consumes the field at finalization). daemon build + `swift test` (922) green. The voiceprint and roster work are the separate FEAT3-VOICEPRINT / FEAT3-ROSTER items.

### Carried from Q4 unchanged

Full specs in [`q4-final.md`](./q4-final.md). Start/end-detection and capture: START1, START2, START3, MIC10, MIC11, MIC12, MIC13, END5, REC1, REC2, REC3, REC4, REC5, CONC3, MIC7, MIC8. Pipeline and local models: PIPE1, PIPE2, PIPE3, LOCAL1, LOCAL2, LOCAL3, LOCAL4, LOCAL5, UX10. Security and CI: SEC8, SEC10, SEC11, CI1, CI2. Design and UX: UX14, DSN15, DSN18, DSN19, WF6. Features and distribution: FEAT3-VOICEPRINT, FEAT3-ROSTER, DIST1, CAL1, DOC6. Deferred: I7, I8, G1, D8, CAP1, BRAND1..9.

### Watch list (monitor, promote on a real recurrence)

- **'Today'-filter then Clear crash (2026-06-09):** a generic-runtime use-after-free during a trivial `filter = MeetingFilter()` on a degraded 2-day process mid-storm; most likely a secondary symptom of the MIC11 failed-start storm. Re-test on a clean post-A17 build; promote to a real fix only if it reproduces on a fresh process.
- **CAP1** mic/system end-of-call skew (above).
