# MeetingPipe Q6 backlog

This is the active source of truth. Q5 is archived to [`q5-final.md`](./q5-final.md) (the frozen done-only record; earlier quarters beside it, and git history preserves the rest). Q6 carries forward the 46 items Q5 left unfinished: the not-yet-started work plus every item that shipped only partially (a dev remainder is still owed). Nothing done-done was carried; if a task needs no further engineering, it stayed in the archive.

The one structural change this quarter: the Table of contents is reshaped to the `sadok` six-column form. **Status** (the done-state) is split out of **Priority** into its own column, and the old verbose Description column is replaced by a short **Comment**; the full spec for each item lived in Task specs below the table (moved to per-task files by the fourth structural change, below).

Second structural change (2026-07-10): the owner-commissioned architecture review added an engineering-health band (SEC13, ARCH4, DOC7, LOCAL10, CONC4, PIPE7, TYPE1, STOR3, FEAT3-MANAGE, UX20, SEC14, CI3, HYG1, DEP1), amended AUTO1, I7, PERF7, and LOCAL9, closed UX18 as already-shipped, and re-banded priorities. Done items now move to the running archive [`q6-final.md`](./q6-final.md) at ship time instead of waiting for the quarter roll (see Marking a task done).

Third structural change (2026-07-12): the owner-commissioned multi-angle assessment (eight parallel audit passes, every critical/high finding adversarially re-verified) added the detection-coverage band (DET1-DET5, END8), the reliability band (REC6-REC8, MIC16, ASR2), the pipeline/platform band (PIPE8, PIPE9, LOCAL11, LOCAL12), the truth and measurability band (DOC8-DOC10, HYG2, CI4, CI5, T3), the setup/UX band (UX21-UX23), and corpus features (STOR4, DV3, AI7, plus P3 rows UX24, UX25, AI8, ASR3, AI9, ARCH5); promoted END5 and CAL1 to P1 and MIC8 to P2; and commissioned LOCAL12 as the I7 trigger evaluation. Origin and specs in the Assessment review 2026-07-12 section.

Fourth structural change (2026-07-17): task specs moved out of this file into [`tasks/`](./tasks/), one `tasks/<ID>.md` per task, because the spec body had outgrown a single read (~32K tokens against a 25K read cap). This file keeps the ToC (the index), the process sections, and the band-origin narratives; a session reads the ToC here plus only the spec it needs. A shipped task's spec text moves into `q<N>-final.md` and its `tasks/` file is deleted, so `tasks/` is active-only.

## How to pick up a task (delegation)

Mechanics are codified in `/tech-task <ID>` (read the task here, read the orientation docs, implement, verify, one commit on `main`, do not push). Soft context that command does not carry:

- Task ids carry no `TECH-` prefix (e.g. `/tech-task STOR1`). Historical code comments, ADRs, and commits keep the old `TECH-` form as provenance.
- **Full specs live one per task in [`tasks/<ID>.md`](./tasks/)**; items carried from Q4 unchanged keep theirs in [`q4-final.md`](./q4-final.md) (search the id), so each spec has a single owner.
- User locale: 99% English / Ukrainian. Do not default examples to German. Verify any new `uk` labels in `MuteLabels.toml` with the user.
- The user is vibe-coding: high-level explanations, does not read code. Lean on the ARCHITECTURE.md diagrams. Blind sub-pixel visual churn is out of bounds; visual changes need eyes, and the daemon is a menu-bar app that is not screenshotable here, so a rendered eyeball is owner-owed.
- No em-dashes in any output (CI fails on a U+2014 in any added line repo-wide, and on any U+2014 present at all under `daemon/Sources` / `daemon/Resources`; `scripts/pre-commit` blocks both at commit time when installed). Use hyphens, commas, or rewrite. Commit with the repo's git identity. Do not push without permission.
- Binding constraint (CLAUDE.md): the primary user is the author; selling is tertiary; technical excellence in core functionality comes first. The BRAND launch band stays deferred. The 2026-07 redesign band (DSN20-29 + WF6 + UX15) shipped in Q5; the only redesign residual is the owner-run `/design-sync` push, an outward sync tracked as an owner action, not a task.
- The AI/DV standing rule: every `AI*`/`DV*` capability and any new engine call must route through the local engine path so the `egress_guard` clamps it under regulated/NDA, honour `effective_backend()` (forced local under regulated/NDA), and add no new always-on egress.
- The de-CLI standing rule (2026-07-10): the CLI is a debugging escape hatch, not a product surface. A task that ships a user-facing capability includes its UI affordance in the same task; a CLI-only ship is `partial`, not done. Exempt by class (owner-dev diagnostics): dogfood, analyze-detection, corrections-stats, train-adapter, spike harnesses. Rationale: the Q4 validation audit carried 13 tasks for the CLI-only-no-UI pattern, it recurred in Q5 (AI4), and unwritten goals lose to written personas in an AI-steered repo.

## Marking a task done

When a task ships done-done, move it to the running quarter archive [`q6-final.md`](./q6-final.md) in the same commit: its ToC row (Status `done`, a one-line ship note in the Comment) moves there, and the spec text from `tasks/<ID>.md` moves there prefixed `[DONE]` with that file deleted, so the active backlog stays lean, `tasks/` stays active-only, and the archive keeps the whole trail in one file (process change 2026-07-10; specs split to per-task files 2026-07-17). A partial ship stays here as `partial` with the remainder named in the Comment; owner-owed runtime or eyeball work stays here as `owner-owed`. `/tech-task` codifies the move.

## Priority bands

- **P1**: correctness, a broken core promise, or a high-leverage must-have; do first. This quarter P1 is the open correctness/detection debt (MIC, and DIAR2's merge defect); VALID1 closed 2026-07-16 with every bar measured (its one waived leg split out as EGRESS1, P4) and lives in [`q6-final.md`](./q6-final.md); C6-FINISH shipped its 20 real-trace corpus and END6 closed (gate validated in production, device-idle leg refuted by measurement), both now in [`q6-final.md`](./q6-final.md). The engineering-health P1s the 2026-07-10 architecture review added (SEC13, PIPE1, DOC7, ARCH4) have all shipped. The archive-stewardship P1 band from the 2026-07-05 feature review (MIC13, STOR1, STOR2, SEC12, ASR1, FEAT8) shipped and lives in [`q6-final.md`](./q6-final.md). The 2026-07-12 assessment added a detection-coverage and reliability do-first band: DET1, DET3, DET4, DET5, REC6, DOC8, LOCAL12, plus END5 and CAL1 promoted (see the Assessment review section).
- **P2**: polish, power-user payoff, and the half-closed loops (digest/paste/long-meeting surfacing, the provider seam).
- **P3**: deferred; listed so the trail stays readable. Leave P3 alone unless a task explicitly promotes it.

## Table of contents

| ID | Task | Category | Status | Priority | Comment |
|---|---|---|---|---|---|
| DIAR2 | Diarization + segmentation quality | Transcription | partial | P1 | Leg (b) shipped: `SegmentBuilder.coalesce` drops phantom punctuation-only rows and merges same-speaker fragments into readable turns (applied by the runner after `build`; 11 tests). Leg (a) partial: wired a `transcription.diarization_clustering_threshold` knob, default lowered 0.7 to 0.65 (over-split beats naming the wrong owner). Owner-owed: sweep the threshold against the hand-labelled corpus via `scripts/valid1_check.py --der` (its MERGED CLUSTERS line) to confirm the merge is fixed. Rejected `speakerManager.reset()` removal (reintroduces the ASR2 drift, and the 5-person merge was within one meeting). The stepRatio lever is `OfflineDiarizerManager`-only; the daemon runs the online `DiarizerManager`, so that lever needs a spike-sized migration (not done). |
| MIC7 | Browser/PWA mute oracle via a platform signal | Detection | partial | P2 | Spike shipped: analysis doc (`docs/spikes/mic7-browser-mute-oracle.md`) + an owner-run read-only DOM probe (`daemon/scripts/mic7-meet-mute-probe.js`, pasted into a live Meet tab's console). Verdict: architecture GO (a `MeetMuteAdapter` fed by a content-script extension, no fusion changes), build DEFER (gate on a real browser meeting, END5's trigger). **Signal gate resolved GO 2026-07-20:** owner ran the probe on a live Meet call and `data-is-muted` flips `'false'`/`'true'` on every toggle (the spike's candidate 1); `aria-pressed` is absent entirely, and the localized `aria-label` + obfuscated `className` stay rejected. Gotchas pinned for the build: the attribute is *state* while the label is the *inverse action*, and it is a string that can be absent, so absent must degrade to unknown, not to unmuted. Remainder narrowed to one item: a real browser meeting (still 0 recorded; the 2026-07-20 probe call was prompted twice and declined both times, so the trigger has not fired). |
| DIST1 | Bundle a runtime for a drag-n-drop installer | Distribution | partial | P2 | Daemon plumbing shipped + tested: `findMP` runs an embedded relocatable runtime as `python3 -m mp` (relocation-safe; pure `resolveDirectMP` + 4 tests), `findFFmpeg` falls back to a bundled static ffmpeg. Build tool `scripts/bundle-runtime.sh` (relocatable Python + wheels + static ffmpeg, signs mach-Os inside-out) + runbook `docs/spikes/dist1-bundle-runtime.md` written. Owner-owed: run the tool, notarize with a Developer ID (`notarytool`), and drag-install-test on a clean Mac (not exercisable here). DEP1 GO doesn't remove ffmpeg (`MuteRedactor` still shells it). **Owes a SETUP.md edit**: [`docs/SETUP.md`](../SETUP.md) documents the from-source path and closes by saying a drag-install is in progress; when the notarized bundle lands, that guide gains a short drag-install section at the top and the build-from-source route becomes the alternative (DOC10). |
| LOCAL9 | Corrections Phase 3: local LoRA fine-tune | Local models | partial | P2 | Unblocked 2026-07-23: the corrections corpus is still 0 records after two months, so `--source runs` trains on the 167 cloud-summarized meetings already on disk (2.4M chars, 12x the bar) instead of waiting for grading. Same pass fixed three train/inference prompt mismatches that would have made any run worthless, and made the A/B refuse trained-on meetings. Owner-owed remainder: run `mp train-adapter --source runs` (~1h on the M1 Pro), then the held-out dogfood A/B, and hand-grade it to decide adoption. **Blocked on LOCAL15 (filed 2026-07-23):** mlx-lm silently drops `--adapter-path`, so the adapter never serves and the A/B would compare the base model against itself and return a false negative. |
| T2 | Portable visual-regression tests (or close) | Tests | new | P2 | Snapshot infra removed as non-portable; decide: close as won't-do (smoke tests are the bar), or adopt a CI-portable approach. |
| REC8 | Self-healing capture | Recording | partial | P2 | Implementation shipped + unit-tested: one automatic ~7 s system-audio retry then the manual banner; an `IOPMAssertPreventUserIdleSystemSleep` assertion held across recording (released in stop + termination); an `NSWorkspace.didWakeNotification` re-arm; and the tap watchdog un-gated from `engine.isRunning` via a pure `TapWatchdogPolicy` (3 tests). Owner-owed runtime eyeball (not exercisable here): `pmset -g assertions` shows the assertion during recording and not after, a killed SCStream recovers hands-off, and a real sleep/wake mid-recording ends with a usable artifact. |
| MIC8 | Teams `LogMuteAdapter` (native mute signal) | Detection | partial | P2 | **Spike resolved 2026-07-23: GO for Teams, against its own a-priori lean.** Vendor SDK and OS-level stay closed from documentation. The local-log path is now measured, and not on a live instrumented call but offline against 11 days of real dogfood (`docs/spikes/mic8_log_correlation.py`, aggregates in `mic8-log-correlation-results.json`): Teams' SlimCore media log writes `AirPodsService: Input mute event received: mute\|unmute` on every in-call toggle, 594 transitions across 9 log generations, identical format throughout (survives client updates), default-on with no troubleshooting toggle. Paired against the incumbent AX scrape: 61 pairs, 59 agree, and the log **leads** the AX read on 59 of 61 (p50 50 ms); of the 4 imperfect pairs 3 are AX-side defects, including a notification that fired 16.8 s late and a transition AX invented. Not candidate C in disguise: `system_mute_state` was false in 34/34 while the log tracked 60 client-side transitions. The 2026-07-13 recon that leaned close had read the wrong file family (the Electron shell log, not `MSTeamsNM_SlimCore_*`), and the probe had two defects that would have confirmed that false close (a flat newest-6 cut evicting the only file with mute lines; a token match on the hourly ECS config blob's `hfpOsUnmuteFixEnabled`); both fixed and verified. Remainder is now a **build**, not a measurement: a `LogMuteAdapter` conforming to `MicGateAdapter`, feeding `MicGate.injectAxMuteEvent`. Owner decision first: supersede the Teams AX read or fuse with it (doc recommends fuse, "log wins when present, AX carries when absent"). Zoom stays unmeasured and effectively moot (zero Zoom calls in the corpus). |
| ASR3 | Batch re-transcribe ratchet | Transcription | new | P3 | Transcripts are frozen at capture-time quality; STOR1 chose FLAC precisely to keep reprocessing possible. Library multi-select re-transcribe with the current stack; overlays survive; glossary/roster reach old meetings. |
| AI9 | Learned workflow routing | Intelligence | new | P3 | WF8 reassignments quietly produce labelled correction pairs nothing consumes; suggest (never auto-apply) a workflow on the prompt chip. **Gate cleared 2026-07-23:** AI5 resolved NO-GO on a derived `meeting_type` field, which does not block AI9 and settles its input question, the WF8 reassignment pairs (real owner corrections) are the better-grounded signal. AI9 must not take a dependency on `meeting_type`. |
| AI10 | Digest action list is unbounded | Intelligence | new | P2 | `mp digest` dumps every open action across the whole library into one weekly digest (970 on the 199-meeting library), past reviewable. The daemon scroll freeze is fixed (LazyVStack + reader moved to the wide detail pane, 2026-07-13); what remains is upstream scoping: bound the list to the review window, then cap + group + rank it. Complements AI7 dedup. Local-only. |
| PIPE10 | Sleep-resilient summarize retry | Pipeline | new | P2 | Filed 2026-07-17 from the 2026-07-16 incident: lid closed 1 min after a 110-min meeting ended, the Mac slept through the in-flight Anthropic call, and the 120 s read timeout (monotonic, frozen during sleep) fired on wake as APITimeoutError, which summarize deliberately never retries; pinned-anthropic has no auto fallback, so the run hard-failed until a manual Retry, which then succeeded in 36 s. Detect the slept-through case (wall vs monotonic divergence) and retry once in-process. |
| ARCH5 | Refactoring band | Architecture | new | P3 | Promote when touching the files: MeetingRecorder post-processor extraction, PipelineLauncher's embedded Apple-Intelligence engine + 17-method driver protocol, LocalSummaryClient supervisor split (sequence vs LOCAL12), plus the small duplication list (see spec). |
| I7 | Drop Python entirely | Architecture | deferred | P3 | Trigger amended 2026-07-10: MLX-Swift matches the mlx-lm 7B on quality/latency (Apple Intelligence's 4096-token context was the wrong gate). DIST1, the de-CLI goal, and dual-language maintenance all hang on this decision. LOCAL12 (2026-07-12) delivered the verdict: NO-GO now (the MLX-Swift candidate does not exist and building it is the risk), keep the embedded-Python runtime; the trigger is now operationalized as an owner-run bench (`docs/spikes/local12_bench.py`), not a maturity guess. |
| I8 | Live transcription during recording | Detection | deferred | P3 | The streaming floor exists natively in FluidAudio; de-risk via two-pass. A post-meeting tool may not need live. Depends on LOCAL5. |
| G1 | Personal two-Mac hub | Sync | deferred | P3 | All P0/P1 dogfood bars met first. |
| D8 | Developer ID + notarization in CI | Distribution | deferred | P3 | Partly overlapped by DIST1; full notarization-in-CI parked until a second user. The durable fix for the per-build TCC churn. AUTO1's native App Intents leg is now gated on this: the intents are built and discoverable but cannot be invoked on an ad-hoc-signed bundle, so a Developer ID buys working Shortcuts / Spotlight / Siri actions on top of the TCC-churn fix. |
| CAP1 | Mic/system end-of-call skew | Detection | watch | P3 | Monitor only; the promotion trigger is the few-second skew reappearing across multiple recordings. Spec retained in q3-final.md. |
| C7 | Backstop-end corpus coverage | Detection | new | P3 | C6-FINISH's real corpus can't cover the idle-stop backstop ending (a lone ax-leave `.ended`, empty `confirmed_by`), which is ~1/3 of real sessions: it's promoted by `IdleStopBackstop`, not `PromotionEngine`, so the pure-engine replay harness has no way to model it. Extend the corpus harness to drive a backstop-level end, or file the gap as accepted. Pairs with T3. |
| BRAND1..9 | Launch readiness band | Brand | deferred | P3 | Domains, trademark, landing page, demo GIF, screenshots, OG card, compliance pages. Selling is tertiary; do not pull forward. |
| EGRESS1 | Confirm Apple Intelligence zero-egress on the wire | Validation | deferred | P4 | Split out of VALID1 2026-07-16 by owner decision: the bar was **waived, not measured**. The claim rests on argument (on-device Foundation Model, no outbound HTTP in the daemon, `egress_guard` clamps under NDA), never on watching the wire. Arm Little Snitch during an Apple Intelligence summary and confirm zero non-loopback connections. Promote only if the claim becomes load-bearing (an NDA meeting that actually depends on it, or a claim made to someone else). |
| AUTO1 | Automation surface: App Intents + URL scheme | Automation | deferred | P3 | **Blocked on a Developer ID; deferred by the owner 2026-07-20** (their signing plan slipped), so this sits out of the active band until D8 lands. **The half that matters is already done and needs nothing**: the `meetingpipe://` URL scheme is done-done and hardware-verified (2026-07-20, an `automation command` event with verb `open_library`), which meets AUTO1's stated acceptance and gives Shortcuts (via Open URL), Raycast, and Stream Deck a working surface. Only the **native App Intents** leg is blocked, and not on engineering: the six intents, their `AppShortcutsProvider`, 8 drift-fence tests, and the `install.sh` metadata extraction are all built and correct, and the actions DISCOVER properly in Shortcuts, but invoking one fails with "Shortcuts couldn't communicate with the app". Six causes were excluded by measurement (symbols present + AppIntents linked, Launch Services registers the app, `openAppWhenRun` both ways, launchd vs `open -a`, `LSUIElement` both ways, no crash); what is left is that the bundle is `spctl`-rejected with `TeamIdentifier=not set`, since the intent XPC handoff needs that trust while the URL path does not. `install.sh` therefore emits the metadata only under `MP_APP_INTENTS=1` (default off), so an unsigned build shows no always-failing actions. **Promote when D8 gives the app a real Developer ID**: the only step then is flipping that flag and re-checking Shortcuts. Full trail in `docs/spikes/auto1-app-intents-metadata.md`. |
| DET6 | Browser-tab meetings other than Meet are undetectable | Detection | new | P2 | Filed 2026-07-20 off the DET2 close. Only Google Meet can clear the plain-browser admission gate: `strongTitleMatchers` holds one entry (the Meet room-code pattern), and `fragmentMatch` reads the AX window title against URL fragments while browsers put the page title there and never the URL, so it effectively never fires. A browser tab running Teams, Webex or a Slack huddle matches weakly and is then rejected, and Zoom-in-browser has no title matcher at all. The rejection is silent: candidates are dropped by `continue` before entering `candidates`, and `droppedCandidates` filters that same array, so no `candidate_dropped` event is emitted. The brand-token escape hatch END5 shipped for exactly this case never functioned, because the process audio it required was already returning nil on the day END5 shipped. Natives and Chromium PWAs are unaffected. P2 rather than P1 because the dogfood is Teams-native with 0 recorded browser meetings; re-band to P1 the moment a browser meeting is wanted. Probe-first: measure whether a live call's title is distinguishable from a doc title and whether the real URL is readable from the browser AX tree, then fix `fragmentMatch` to read the URL it was always meant to read. Note the MIC7 interaction: its build gate is "a real browser meeting appears in the corpus", which this gap partly suppresses. |
| FEAT10 | Evidence-grounded summaries | Feature | new | P2 | Filed 2026-07-22 (owner ask): summary points are uncheckable against the meeting. Up to two verified anchors (verbatim phrase + timestamp) per bullet/decision/action/question: quiet [mm:ss] chips in the Summary tab jump the Transcript tab to the source row (time-to-row mapping + scroll already exist from FEAT8 markers); published notes get timestamps-only suffixes. Model input gains per-turn timestamps (stored .md unchanged); a post-generation verifier drops any anchor that does not resolve to a real segment (the mp ask precedent), so a wrong citation never lands. Local map-reduce + Apple Intelligence paths degrade to no evidence; old meetings backfill via Regenerate; Swift must model the new field or its two summary.json write paths silently drop it. |
| UX26 | Resolve action items from the Summary tab | UX | new | P2 | Filed 2026-07-22 (owner ask): actions can be marked done only from the Facts rail, and the Summary tab renders resolved and open actions identically (truthful-states gap). Extract FactsView.markDone into a shared writer, add an always-visible circle/checkmark toggle per action row (struck-through in place, reversible, VoiceOver-labelled, new `action_reopened` event); needs-republish badge + Notion checked-to-do round-trip already exist, so no new publish logic. Removal-as-noise stays in the edit flow. |
| UX27 | Floating-panel keyboard operability | UX | new | P4 | Deferred out of UX23 (owner did not need it, 2026-07-22). The detection prompt and recording HUD are not Tab/Space operable: in an accessory (menu-bar) app the borderless floating panels do not reliably take key focus, so no focus ring appears and Tab does nothing (VoiceOver labels on the close/chevron/off-record controls are in place; only keyboard/Tab is missing). A first attempt (key-capable `NSPanel` subclasses, `becomesKeyOnlyIfNeeded=false`, per-control `keyDown` + focus rings + `nextKeyView` tab order, and a click-to-focus that forced `makeKey` + `NSApp.activate` on empty-area clicks, mirroring QuickFind which works) still showed no ring in owner testing and was reverted with the UX23 close. P4: revisit only if keyboard-driving the passive panels becomes a real need; the global hotkeys (toggle / force-stop / off-the-record) remain the keyboard path meanwhile. PRODUCT.md floor: prompt Record/Skip/BYO reachable by Tab, nothing focus-only-via-mouse. |
| AUTO2 | Published-note transcript deeplinks | Automation | new | P3 | Filed 2026-07-22 (owner ask, promote-on-demand): make FEAT10's published [mm:ss] timestamps clickable from Obsidian/filesystem/LAN notes via `meetingpipe://open?stem=&t=` (new parser verb + router to the Transcript tab's pending seek; unknown stem opens the Library quietly; Notion stays plain text). Depends on FEAT10's timestamps + seek seam; the URL scheme is already registered, so this is parse-side only. |
| UX28 | Library processing visibility and control | UX | new | P2 | Filed 2026-07-23 (owner findings): a healthy run-all offers no Cancel (the button is stall-gated, MeetingRow.swift:337-346), queued jobs are invisible and cannot be dequeued, and regenerate/republish publishes no state at all (view-local `inFlight` on one affordance only), so a mid-reprocess re-summarize reads as "nothing happened". Always-visible Cancel, Queued state + dequeue, published per-stem busy chip from every affordance, unified completion/failure feedback, cancellable regenerate. Serialization and provenance stay PIPE11's. |
| PIPE11 | Regenerate correctness: serialization, provenance, policy snapshot | Pipeline | new | P2 | Filed 2026-07-23 (log forensics, stem 20260723-103118): regenerate bypasses the job queue so same-stem writers race last-writer-wins (an Anthropic summary survived a concurrent stale local writer by ~9 s), standalone `mp summarize` never rewrites run.json so the engine badge lied ("On-device (MLX)" over a Sonnet summary) and corrections attribution drifts, and a regenerate-then-republish chain re-reads meta per spawn so a mid-chain workflow flip published the stale local summary to Notion first. Per-stem serialization (queue-preferred), run-sidecar rewrite on standalone summarize (candidate runs excluded), close the INTO-NDA TOCTOU direction. |
| UX29 | NDA-aware re-summarize affordance | UX | new | P3 | Filed 2026-07-23 (owner finding): "Re-summarize with Anthropic" is enabled on NDA meetings, the pipeline silently and correctly clamps to local (PIPE6 override + egress posture, both untouched), and the owner read the silent substitution as a bug. Disable the cloud item under NDA/regulated with a quiet caption, deep-link "Change workflow..." (the existing confirmed escape hatch); the after-the-fact provenance label rides PIPE11's run.json fix. |
| LOCAL13 | Local model refresh bench | Local models | new | P2 | Filed 2026-07-23 (owner ask): the local floor is a 2024-generation Qwen2.5-7B-4bit and the 2026-07-23 side-by-side (stem 20260723-103118) shows the class gap. Bench newer 4-bit 8-14B candidates (Qwen3 2507 line, Gemma-3-12B QAT, Phi-4) via `mp dogfood` on a fixed transcript set, pre-registered thresholds, caffeinate + free-RAM watchdog protocol (14B at 4-8K ctx only); flip `summarization.local_model` on a measured win. Probe legs (arch support, response_format honoring, template quirks) feed LOCAL14. Owner-owed runs + scoring. |
| LOCAL14 | Staged local summarization pipeline | Local models | new | P2 | Filed 2026-07-23 (owner ask, cloud parity not the target): the local single mega-call over-suppresses a small model into genericity and the PIPE4 reduce summarizes summaries of 1,500-char windows. Replace with extract (verbatim quotes + timestamps, model-aware windows) -> per-section compose from fact rows only -> deterministic-first verify + one critic pass; bounded k=3 consensus on decisions/owners only if the bench proves it; prefix-cache-aware message ordering; subsumes the local map-reduce and lands FEAT10's local evidence leg in the same design. Adopt on pre-registered `mp dogfood` thresholds after LOCAL13 picks the model; single-pass stays as fallback. |
| FEAT11 | In-app context instruments | Feature | new | P2 | Filed 2026-07-23 (owner ask: no hand-edited config, the app provides the instruments): glossary (ASR1) has zero daemon UI and no file on the owner's Mac so garbles reach both backends; voice naming exists (FEAT3) but nothing says whether roster matching ran/matched/missed (roster.json populated 2026-07-16 yet 20260723-103118 kept THEM-B/C); empty workflow `context_prompt` gets no guidance (both NDA workflows are empty). Glossary editor with TOML round-trip, per-meeting voice-resolution surface + naming deep-link, empty-context placeholder nudges. |
| WF9 | Workflow store duplicates | Workflow | new | P3 | Filed 2026-07-23: the owner's config holds 4x "Client work" (one default, all NDA), 2x "Personal", and an "Untitled workflow", all with empty matching rules; WORKFLOWS rail scopes resolve by name (WF8), so collisions collapse into unexplainable scopes. Find the duplication cause (editor save path vs migrator re-seed vs manual), add a load-time duplicate-name event + a quiet collision cue; no auto-delete, cleanup and the NDA-default decision stay owner-owed. |
| HYG3 | Dead-code removal, 2026-07-23 sweep | Hygiene | new | P3 | Filed 2026-07-23 (owner-commissioned sweep, all claims re-verified by repo-wide greps): both trees are clean except seven items: dead `_numbered`/`_todo` (publish_notion), test-only `reap_orphan` (Swift owns reaping) and unwired `icloud_desktop_sync_enabled`, test-only `MuteLabelsValidator` whose docs claim a nightly CI VM farm that never existed (fix the two doc surfaces), the `RecordingEventLog` test double shipped in Core (relocate or document), two never-loaded menu-bar SVGs, and the never-thrown `CaptureError.bufferConversionFailed`. Kept-deliberately register included (DET2 signal, App Intents, two documented aliases) so nothing gets re-filed. |
| UX30 | Workflow delete flow | UX | new | P2 | Filed 2026-07-23 (owner reported 2, tracing found 3): the Delete control is a neutral ghost capsule on a stale "there is no `.mpDanger` capsule" premise (MPDangerButtonStyle ships at BatchActionsPane.swift:504), and `deleteSelf` never calls `onCommit` (WorkflowsView.swift:644-650) so the sheet stays open on a confirmed delete. Consequence: Save carries `.defaultAction`, so one Return re-persists the deleted workflow via `upsert`'s append branch, silently undoing it. Also: a refused delete is indistinguishable from a successful one (discarded Bool), and the alert copy omits that past meetings drop out of the WORKFLOWS rail (name-based resolve, not swept into Untagged). |

---

## Task specs

One file per task in [`tasks/`](./tasks/): the full spec and progress notes for `<ID>` live in `tasks/<ID>.md`; the ToC above is the index, and a task's Status and Priority live only in its row there. Items carried from Q4 unchanged keep their spec in [`q4-final.md`](./q4-final.md) (search the id). The band-origin notes below carry the shared context the specs reference. When a task ships done-done, its spec text moves into the quarter archive prefixed `[DONE]` and its `tasks/` file is deleted (see Marking a task done above).

### Intelligence layer (AI band)

The Q5 intelligence build (AI1 lifecycle, AI2 spike, AI3 ask, AI4 digest, DV1 facts, DV2 attendee spike) shipped; their specs are in [`q5-final.md`](./q5-final.md), and AI6 (recurring-series continuity) shipped in Q6 and lives in [`q6-final.md`](./q6-final.md). What remains is one spike and one follow-on. The DV People pivot is closed: DV3 shipped 2026-07-24 as the People rail and lives in [`q6-final.md`](./q6-final.md), on owner enrollment alone (CAL1's calendar-attendee seeding, once floated as the complementary path, closed NO-GO on 2026-07-17: the owner's calendar is not in macOS EventKit, so there is no covering event to read attendees from).

### Feature review 2026-07 (STOR/PROV/AUTO bands, ASR1, and additions across existing families)

Origin: an owner-commissioned review of the shipped surface against this backlog (2026-07-05). The finding: correctness, detection, and design are well covered, and the gaps cluster in **archive stewardship** (the product accumulates an irreplaceable corpus, corrections, voiceprint, roster, library, with no storage, backup, or sync-safety story) and in **half-closed loops** (corrections advertise a LoRA phase no task owns; digest and long-meeting flows dead-end in CLI or manual steps). The owner approved all twenty review items on 2026-07-05 and proposed the provider seam (PROV1) in the same pass. Bucket mapping: must to P1, should to P2, nice to P3 (P3 here means promote on demand, per the AI5/I8 precedent). The standing AI/DV rule applies to every item that touches an engine. The band's shipped members (MIC13, STOR1, SEC12, ASR1, FEAT8, STOR2, MIC14, MIC15) live in [`q6-final.md`](./q6-final.md), where UX18 is also closed as already-shipped.

### Architecture review 2026-07-10 (engineering-health band)

Origin: an owner-commissioned architecture and gaps review (2026-07-10), run as four deep passes (daemon architecture, pipeline architecture, CLI-surface inventory, security/performance) with every recommendation-driving finding re-verified in code. The headline: the codebase is above average for an AI-coded product (real tests, disciplined deps, honest comments, working security chokepoints), and the debt concentrates in three places: an untested god-object spine in the daemon, documentation drift that misleads the sessions steering by it, and the CLI-vs-UI policy being written nowhere (hence the de-CLI standing rule in the delegation section). All items approved by the owner on 2026-07-10. The review also closed UX18 as already-shipped, promoted AUTO1, and amended I7, PERF7, and LOCAL9 in place.

### Assessment review 2026-07-12 (detection-coverage, reliability, truth, and setup bands)

Origin: an owner-commissioned multi-angle assessment (2026-07-12, post-FEAT9), run as eight parallel audit passes (daemon architecture, pipeline architecture + cross-language contracts, SOLID/KISS/YAGNI, doc/functional consistency, coverage + test quality with both suites executed, detection stability, UX/setup, product opportunities) with every critical/high finding re-verified by an independent adversarial agent; verifiers downgraded five of eleven originally-high claims and refuted one absolute, and the severities filed here are the corrected ones. The headline: the capture half of the core promise is genuinely robust (REC1-REC5 verified as described, contracts mostly pinned, suites green, ruff/pyright clean), and the distance to the owner's stated bar (Spark-Mail-grade "it just notices every call") is structural, not tuning: detection is a hand-maintained whitelist single-gated on window-title text with its audio-evidence leg dead in production, the recovery half of the crash story is weaker than the capture half, and several surfaces assert behavior the code no longer has (worst: the mute story). The owner approved filing everything on 2026-07-12. Bands: DET (app-agnostic detection, the owner's #1 priority), REC/END/MIC/ASR (lose-nothing reliability), PIPE/LOCAL (pipeline posture + the platform decision), CI/T/DOC/HYG (truth maintenance + measurability), UX (first-run + shipped-surface floor), and corpus features (STOR4, DV3, AI7, AI8, AI9). Setup items file at P2 because the owner's single install already works; re-band if a fresh install or a second Mac is imminent. The P1 line is the assessment's recommended do-first order, not a contract; re-band freely.

**Amendments (2026-07-12).** CAL1 promoted P2 to P1 (row updated, moved to the P1 cluster): the GO follow-on is the cheapest independent corroborator for DET1, names the meeting in the prompt and sidecar, is the only plausible in-person nudge, and seeds FEAT3/DV3 attendees; run the owner probe early in the DET band. END5 promoted P3 to P1 and re-scoped (row updated, moved to the P1 cluster): browser end detection is active-tab-title-only (`BrowserMeetingLifecycleAdapter.swift:115-132`; `browserTabTitle` carries `requiresCorroboration=false`), so a >5 s tab switch mid-call chops the recording, materially worse than the filed framing, and the old trigger ("a real browser meeting recorded") was circular with the start-side holes (0 browser meetings in 19.8 days, per MIC7's spike); scope now: cross-class corroboration or a minutes-long debounce for tab-title-led ends, plus a corroborator on the bare Meet-code/brand-token matchers (3-4-3 slugs and doc titles containing "huddle"/"webex"/"microsoft teams" false-prompt today); a build here unblocks MIC7's deferred half (shared extension artifact). MIC8 promoted P3 to P2 (row updated): both P1 mute items are wedged on owner AX dumps, and a UI-independent signal supersedes the whole scrape class. I7 (row + spec updated): LOCAL12 is the commissioned evaluation of the 2026-07-10 trigger, so the decision stops drifting. DIST1 (no row change): its daemon plumbing and build tool shipped 2026-07-12 in parallel with this review; LOCAL12's verdict decides whether the embedded-Python runtime is the end state or a transitional artifact, and the owner-owed notarize + clean-Mac test is worth doing either way.

### Carried from Q4 unchanged

Full specs in [`q4-final.md`](./q4-final.md) (search the id). Detection and capture: MIC8 (promoted and amended 2026-07-12, see the Assessment review section; base spec stays in q4-final); END5 shipped 2026-07-12, row in [`q6-final.md`](./q6-final.md); MIC13 shipped, row in [`q6-final.md`](./q6-final.md); MIC10 shipped 2026-07-14, row in [`q6-final.md`](./q6-final.md), and its live AX dump also settled MIC6 (see that spec: no AXValue / AXIdentifier exists on Teams); MIC7 now has a progress note in its own section above). Pipeline and distribution: none remain unchanged (PIPE1 shipped, row in [`q6-final.md`](./q6-final.md); DIST1 now has a progress note in its own section above; CAL1 closed 2026-07-17 NO-GO, now in [`q6-final.md`](./q6-final.md)). Deferred (promote on a real trigger only): I7 (trigger amended 2026-07-10, see the review section above), I8, G1, D8, CAP1, BRAND1..9. One-line reminders live in the ToC Comment column above; the id-searchable full text stays in q4-final so each carried spec has a single owner.

### Watch list (monitor, promote on a real recurrence)

- **'Today'-filter then Clear crash (2026-06-09):** a generic-runtime use-after-free during a trivial `filter = MeetingFilter()` on a degraded 2-day process mid-storm; most likely a secondary symptom of the MIC11 failed-start storm (fixed in Q5). Re-test on a clean post-MIC11 build; promote to a real fix only if it reproduces on a fresh process.
- **REC3 deferred >3h cap-raising:** REC3 fixed the AUD-7 silent-truncation bug (a >3 h meeting is force-stopped near the 4 GiB RIFF cap rather than truncating). The cap-*raising* (CAF or segment-roll so a >3 h meeting keeps recording) was deferred as an unreproduced enhancement that ripples into REC1's orphan/merge contract. Promote if >3 h meetings become a real need.
- **CAP1** mic/system end-of-call skew (see the ToC row; spec in q3-final.md).
- **MIC10 re-anchor, live confirmation (2026-07-14):** the mute walk now anchors on the floating call panel's `AXSystemDialog` subrole, evidenced by a live AX dump and unit-tested against its exact topology, but it has not yet run against real meetings. After a few Teams calls on the rebuilt daemon, confirm from `events.jsonl`: `mute_state_polled` should carry `buttons_found: 1` on every poll (the frozen full-window button is no longer read), `mute_state_window_disagreement` should never fire, and `ax_handles_built` should keep `found_mute: true` (proving the walk re-anchored rather than blinding the gate). A disagreement still firing means the panel is absent in some call shape and the fallback chain is carrying it, which is safe but worth knowing. Needs a `./scripts/rebuild.sh` to take effect.
- **Transcription RAM peak (2026-07-12):** FluidAudioRunner reads the whole meeting into RAM (about 2 GB peak for a 3 h recording: a full-file AVAudioPCMBuffer plus a mono copy held across ASR and diarization) inside the always-resident daemon. Fine at current meeting lengths; promote to windowed/chunked reading (FluidAudio's streaming floor, pairs with I8) if a real >2 h meeting shows memory pressure.
