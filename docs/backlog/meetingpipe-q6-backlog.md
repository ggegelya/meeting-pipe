# MeetingPipe Q6 backlog

This is the active source of truth. Q5 is archived to [`q5-final.md`](./q5-final.md) (the frozen done-only record; earlier quarters beside it, and git history preserves the rest). Q6 carries forward the 46 items Q5 left unfinished: the not-yet-started work plus every item that shipped only partially (a dev remainder is still owed). Nothing done-done was carried; if a task needs no further engineering, it stayed in the archive.

The one structural change this quarter: the Table of contents is reshaped to the `sadok` six-column form. **Status** (the done-state) is split out of **Priority** into its own column, and the old verbose Description column is replaced by a short **Comment**; the full spec for each item lives in Task specs below the table, not in the row.

Second structural change (2026-07-10): the owner-commissioned architecture review added an engineering-health band (SEC13, ARCH4, DOC7, LOCAL10, CONC4, PIPE7, TYPE1, STOR3, FEAT3-MANAGE, UX20, SEC14, CI3, HYG1, DEP1), amended AUTO1, I7, PERF7, and LOCAL9, closed UX18 as already-shipped, and re-banded priorities. Done items now move to the running archive [`q6-final.md`](./q6-final.md) at ship time instead of waiting for the quarter roll (see Marking a task done).

Third structural change (2026-07-12): the owner-commissioned multi-angle assessment (eight parallel audit passes, every critical/high finding adversarially re-verified) added the detection-coverage band (DET1-DET5, END8), the reliability band (REC6-REC8, MIC16, ASR2), the pipeline/platform band (PIPE8, PIPE9, LOCAL11, LOCAL12), the truth and measurability band (DOC8-DOC10, HYG2, CI4, CI5, T3), the setup/UX band (UX21-UX23), and corpus features (STOR4, DV3, AI7, plus P3 rows UX24, UX25, AI8, ASR3, AI9, ARCH5); promoted END5 and CAL1 to P1 and MIC8 to P2; and commissioned LOCAL12 as the I7 trigger evaluation. Origin and specs in the Assessment review 2026-07-12 section.

## How to pick up a task (delegation)

Mechanics are codified in `/tech-task <ID>` (read the task here, read the orientation docs, implement, verify, one commit on `main`, do not push). Soft context that command does not carry:

- Task ids carry no `TECH-` prefix (e.g. `/tech-task STOR1`). Historical code comments, ADRs, and commits keep the old `TECH-` form as provenance.
- **Full specs for items carried from Q4 unchanged live in [`q4-final.md`](./q4-final.md)** (search the id); the Task specs section below restates the new and changed items and points to q4-final for the rest, so each spec has a single owner.
- User locale: 99% English / Ukrainian. Do not default examples to German. Verify any new `uk` labels in `MuteLabels.toml` with the user.
- The user is vibe-coding: high-level explanations, does not read code. Lean on the ARCHITECTURE.md diagrams. Blind sub-pixel visual churn is out of bounds; visual changes need eyes, and the daemon is a menu-bar app that is not screenshotable here, so a rendered eyeball is owner-owed.
- No em-dashes in any output (CI fails on a U+2014 in an added line under `daemon/Sources` and `daemon/Resources`). Use hyphens, commas, or rewrite. Commit with the repo's git identity. Do not push without permission.
- Binding constraint (CLAUDE.md): the primary user is the author; selling is tertiary; technical excellence in core functionality comes first. The BRAND launch band stays deferred. The 2026-07 redesign band (DSN20-29 + WF6 + UX15) shipped in Q5; the only redesign residual is the owner-run `/design-sync` push, an outward sync tracked as an owner action, not a task.
- The AI/DV standing rule: every `AI*`/`DV*` capability and any new engine call must route through the local engine path so the `egress_guard` clamps it under regulated/NDA, honour `effective_backend()` (forced local under regulated/NDA), and add no new always-on egress.
- The de-CLI standing rule (2026-07-10): the CLI is a debugging escape hatch, not a product surface. A task that ships a user-facing capability includes its UI affordance in the same task; a CLI-only ship is `partial`, not done. Exempt by class (owner-dev diagnostics): dogfood, analyze-detection, corrections-stats, train-adapter, spike harnesses. Rationale: the Q4 validation audit carried 13 tasks for the CLI-only-no-UI pattern, it recurred in Q5 (AI4), and unwritten goals lose to written personas in an AI-steered repo.

## Marking a task done

When a task ships done-done, move it to the running quarter archive [`q6-final.md`](./q6-final.md) in the same commit: its ToC row (Status `done`, a one-line ship note in the Comment) and its full Task-spec text both move there, so the active backlog stays lean and the archive keeps the whole trail (process change 2026-07-10; before that, done rows sat here until the quarter roll). A partial ship stays here as `partial` with the remainder named in the Comment; owner-owed runtime or eyeball work stays here as `owner-owed`. `/tech-task` codifies the move.

## Priority bands

- **P1**: correctness, a broken core promise, or a high-leverage must-have; do first. This quarter P1 is the open correctness/detection debt (MIC, END6, VALID1, C6-FINISH); the engineering-health P1s the 2026-07-10 architecture review added (SEC13, PIPE1, DOC7, ARCH4) have all shipped. The archive-stewardship P1 band from the 2026-07-05 feature review (MIC13, STOR1, STOR2, SEC12, ASR1, FEAT8) shipped and lives in [`q6-final.md`](./q6-final.md). The 2026-07-12 assessment added a detection-coverage and reliability do-first band: DET1, DET3, DET4, DET5, REC6, DOC8, LOCAL12, plus END5 and CAL1 promoted (see the Assessment review section).
- **P2**: polish, power-user payoff, and the half-closed loops (digest/paste/long-meeting surfacing, the provider seam).
- **P3**: deferred; listed so the trail stays readable. Leave P3 alone unless a task explicitly promotes it.

## Table of contents

| ID | Task | Category | Status | Priority | Comment |
|---|---|---|---|---|---|
| MIC10 | Re-lock the Teams mini-window mute control | Detection | partial | P1 | Part 2 (VAD-contradiction staleness trigger) shipped: the AX watcher discredits a confident `.muted` read contradicted by sustained HAL VAD, mode-gated to clear only in capture-first (never the regulated gate), re-latching when the contradiction clears. Part 1 (re-scope the AX walk to the live mini-window control) still owed on an owner live AX dump; plus the dwell/regulated-escalation tuning. |
| MIC6 | Attribute-based (futureproof) native mute read | Detection | partial | P1 | Four parts shipped; part-1 (read the button's stable state attribute, not the localized title) owed, gated on an owner AX dump. |
| END6 | Device-idle end corroborator | Detection | partial | P1 | Correlated-pair gate shipped; the `kAudioDevicePropertyDeviceIsRunningSomewhere` idle leg is deferred, needs live-Mac validation. (AUD-10) |
| VALID1 | On-device acceptance measurements | Validation | owner-owed | P1 | Scaffolding shipped; five runtime measurements (A15 cold-start, A16, DIAR1 DER, SUM1-APPLE + zero-egress, UX4) owed on a real Mac. |
| C6-FINISH | Real detection-trace corpus | Detection | owner-owed | P1 | Redaction tooling shipped; 20+ real redacted dogfood traces owed (corpus is still 9 synthetic seeds). |
| CAL1 | Spike: EventKit calendar context | Detection | partial | P1 | Spike shipped: analysis doc (`docs/spikes/cal1-eventkit-calendar-context.md`, re-scoped to hints-never-triggers, re-evaluates ADR 0011) + an owner-run read-only probe (`daemon/scripts/cal1-calendar-probe.swift`) that measures the headline question ("does the calendar carry the meetings?"). Owner-owed: run the probe, then GO (superseding ADR + pre-title / expected-end horizon / preflight) or close, per the coverage %. Could seed FEAT3-ROSTER. Promoted P2 to P1 2026-07-12 (assessment): the GO follow-on is the cheapest independent corroborator for DET1, names meetings in prompt + sidecar, and is the in-person nudge path; run the probe early in the DET band. |
| DOC8 | Mute-story truth | Docs | new | P1 | Confirmed high (privacy): the Preferences toggle, config.example.toml, and ARCHITECTURE still describe live mic zeroing, but default capture-first records and publishes muted speech (only the regulated gate zeroes; redaction is per-workflow opt-in). Owner picks the posture; every surface stops asserting the old model. |
| LOCAL12 | Spike: MLX-Swift trigger evaluation | Local models | new | P1 | The commissioned I7 gate: MLX-Swift vs mlx-lm 7B on quality/latency/memory for this workload, written verdict like DEP1's. Decides whether DIST1's embedded Python runtime is the end state or transitional; the mirror-upkeep tax is ~8% of pipeline LOC and growing. |
| CONC4 | Strict concurrency on the executable target | Concurrency | partial | P2 | Measured, not shipped. Flipping targeted on MeetingPipe yields **20 unique warnings across 5 files** (MeetingRecorder 7, CorrectionsTab 5, MeetingSessionController 2, SinkDispatcher 2, DoctorRunner 2, + 2 in Core). The 2 Core ones are fixed and shipped: `MeetingLifecycleVerdict` / `MeetingLifecycleContext` / `EndingReason` / `Kind` are now `Sendable`, matching `MicGateVerdict`. The remaining 18 are **not** the mechanical Sendable annotations the spec predicted; see the spec below. Flag reverted so `main` stays warning-clean. |
| AUTO1 | Automation surface: App Intents + URL scheme | Automation | partial | P2 | `meetingpipe://` URL scheme shipped (pure parser + router + Info.plist), driving toggle/record/byo/stop/library/ask/digest through the hotkey's gates; Shortcuts ride via its "Open URL" action, so the acceptance is met. Remainder: native App Intents deferred (a plain `swift build` runs no `appintentsmetadataprocessor`, so they would be undiscoverable) + owner-owed runtime eyeball (live URL routing, one-time install.sh re-run). |
| MIC7 | Browser/PWA mute oracle via a platform signal | Detection | partial | P2 | Spike shipped: analysis doc (`docs/spikes/mic7-browser-mute-oracle.md`) + an owner-run read-only DOM probe (`daemon/scripts/mic7-meet-mute-probe.js`, pasted into a live Meet tab's console). Verdict: architecture GO (a `MeetMuteAdapter` fed by a content-script extension, no fusion changes), build DEFER (gate on a real browser meeting, END5's trigger; 0 in 19.8 days), Meet DOM-stability owner-owed (run the probe: a stable boolean = GO, a localized-label-only = close the Meet leg). |
| DIST1 | Bundle a runtime for a drag-n-drop installer | Distribution | partial | P2 | Daemon plumbing shipped + tested: `findMP` runs an embedded relocatable runtime as `python3 -m mp` (relocation-safe; pure `resolveDirectMP` + 4 tests), `findFFmpeg` falls back to a bundled static ffmpeg. Build tool `scripts/bundle-runtime.sh` (relocatable Python + wheels + static ffmpeg, signs mach-Os inside-out) + runbook `docs/spikes/dist1-bundle-runtime.md` written. Owner-owed: run the tool, notarize with a Developer ID (`notarytool`), and drag-install-test on a clean Mac (not exercisable here). DEP1 GO doesn't remove ffmpeg (`MuteRedactor` still shells it). |
| LOCAL9 | Corrections Phase 3: local LoRA fine-tune | Local models | partial | P2 | Tooling shipped: `mp train-adapter` (corpus to prompt/completion pairs to `mlx_lm.lora`), `summarization.local_adapter_path` config + `--adapter-path` serving seam, `mp dogfood --adapter` base-vs-adapter mode, README/config/ARCH. Owner-owed remainder: run the training + the dogfood A/B on a real corpus (empty on this Mac) to decide adoption. |
| T2 | Portable visual-regression tests (or close) | Tests | new | P2 | Snapshot infra removed as non-portable; decide: close as won't-do (smoke tests are the bar), or adopt a CI-portable approach. |
| DET2 | CoreAudio process-tap attribution | Detection | new | P2 | DET1 phase 2: audio-capture consent (macOS 14.2+) makes per-process mic attribution resolve, reviving the dead ProcessAudioSignal; upgrades the generic prompt to a named, high-confidence one and gives end detection an independent leg. |
| END8 | Window-gone false-end hardening | Detection | new | P2 | A minimized / other-Space native meeting window promotes a false end after the debounce (onScreenWindowsOnly, no corroboration required) and the AX rescue cannot revert a window-gone-led provisional. Let a healthy-Leave rescue revert, or require cross-class corroboration. |
| REC7 | Bounded, crash-consistent stop path | Recording | new | P2 | stop() bounds SCStream awaits but not the writer drain or the ffmpeg merge (a wedged write makes .stopping inescapable); the merge writes the final WAV in place, shadowing intact intermediates on interruption. Bounded drains, temp+promote merge, marker flush before branching, plus the small failure-path bundle (see spec). |
| REC8 | Self-healing capture | Recording | new | P2 | One automatic bounded retry after a mid-meeting system-audio death (REC4 plumbing exists; recovery is banner-gated today), an idle-sleep power assertion across recording, a didWake re-arm, and un-gate the tap watchdog from engine.isRunning. |
| MIC16 | Mute-backstop AX poll off main | Detection | new | P2 | The 1 Hz mute backstop runs a synchronous cross-process AX tree walk on the main thread for the whole recording; a wedged client can delay the force-stop hotkey. Move the resolver to axWalkQueue, hop the fused result back. |
| ASR2 | Per-job diarizer reset | Transcription | new | P2 | The session-lifetime DiarizerManager accumulates a cross-meeting speaker database with no reset. Re-init per transcription job; keep the CoreML models resident. |
| MIC8 | Spike: UI-independent native mute signals | Detection | new | P2 | Do Zoom/Teams logs or SDKs expose mute transitions that survive UI redesigns? If found, supersedes the AX read. The strongest futureproofing. Promoted P3 to P2 2026-07-12 (assessment): both P1 mute items are wedged on owner AX dumps; a UI-independent signal supersedes the whole scrape class. |
| PIPE8 | Pipeline posture + honesty bundle | Pipeline | new | P2 | serve-local exec with an unscrubbed env / no HF_HUB_OFFLINE / no cache gate; an entry-contract tripwire test; atomic transcript/summary writes; digest --publish skips the exit-3 contract; FEAT9 merge stamps provenance after the audio replace (crash re-concatenation window). |
| PIPE9 | Transcript corrections reach the pipeline | Pipeline | new | P2 | Text corrections have zero Python readers: regenerate, ask, digest, and the embedding index run on uncorrected text while the UI shows corrected. Overlay reader mirroring speaker_overlay, schema_version stamp, parity fixture. |
| LOCAL11 | Warm-server identity verification | Local models | new | P2 | Warm mlx_lm.server reuse checks HTTP 200 only, never the served model, and --adapter-path exists only at spawn: a config flip serves stale weights, run sidecars mis-attribute, and the LOCAL9 adapter can silently never serve. Verify /v1/models + marker identity, respawn on mismatch, doctor probe. |
| CI4 | Pin the remaining cross-tree contracts | CI | new | P2 | publish.json (the PIPE1 outcome contract), the prefetch-model progress JSONL, and the speaker-overlay resolution have no shared fixtures; the speaker-labels sidecar is the one unversioned cross-language sidecar. Extend the CI2/CI3 goldens + stamp schema_version. |
| CI5 | Truth fences | CI | new | P2 | CI diffs documented config keys vs actual readers (the dead-knob class HYG2 clears) and emitted event categories vs CONVENTIONS (five undocumented today, one non-snake action). Sequence after HYG2 + DOC9. |
| T3 | Shipped-untested tests + coverage harness | Tests | new | P2 | SearchIndexer reconcile (shipped untested), a pure StatusBarModel derivation, Notifier response routing, __main__ dispatch; pytest-cov + scripts/coverage.sh for both trees (no coverage measurement exists anywhere). |
| DOC9 | Docs truth sweep | Docs | new | P2 | Post-DOC7 drift: README backstop numbers 5-10x off, three-vs-four Keychain keys, config.example.toml missing every sink section + default_prompt_action, Republish-all copy vs fan-out, the "Whisper" sublabel, five event categories, stale detail-pane docs + dead RawFilesTab.swift, files-table gaps, the VCR claim + dead deps. |
| DOC10 | SETUP.md + persona amendment | Docs | new | P2 | One exhaustive non-programmer clean-Mac path (real prerequisites incl. CLT/Homebrew/download sizes, the four TCC grants + AX relaunch, keys, Notion DB); add prerequisites to README + a swift check to install.sh; amend PRODUCT.md / CLAUDE.md so setup ease is written in scope while selling stays deferred. |
| HYG2 | Dead-knob wire-or-delete | Hygiene | new | P2 | Three live controls drive nothing: transcription.language (wire it, the languageHint plumbing exists end-to-end), recording.sample_rate (delete, capture is a deliberate 16 kHz constant), detection.debounce_start_sec (the END4(b) class, wire or delete). |
| UX21 | First-run traps | UX | new | P2 | The startup TCC burst fires before onboarding's framed permissions step; a local/NDA workflow never prefetches the model (the first NDA summary fails with a CLI-only remedy); onboarding's AX row lacks the relaunch hint. Gate the burst, inline model preflight + Download now, add the hint. |
| UX22 | Setup completion surfaces | UX | new | P2 | A menu-bar Finish-setup checklist (key / database / permissions / model, deep-linked), an onboarding publish-target step (Keychain status + the existing Notion DB picker + a test publish), picker reuse in Integrations, install.sh epilogue parity + mp on PATH. |
| UX23 | Shipped-surface floor | UX | new | P2 | Prompt panel keyboard + VoiceOver (PRODUCT.md's own floor), off-the-record discoverability (Preferences hotkey row + HUD affordance), search index building/degraded states + a doctor probe, Digests error surfacing, permission warnings that name consequences. |
| STOR4 | Scheduled automatic backups | Storage | new | P2 | The corpus is irreplaceable; backup is click-or-CLI only and staleness is merely observed. Copy the AI4 launch-agent pattern: a Preferences toggle scheduling mp backup, last-run surfaced. |
| DV3 | People rail | Intelligence | new | P2 | Roster + management shipped, meeting the DV re-open condition: a Facts-style projection of per-person meetings, owned open actions, last-seen. Gated on a minimally populated roster (owner-owed enrollments). |
| AI7 | Commitments loop | Intelligence | new | P2 | Overdue actions resurface only if Facts or a digest is opened, and recurring meetings accumulate near-duplicate actions. Amber overdue count on the Facts rail row + embedding-cluster dedupe with resolve-the-cluster semantics (local-only). |
| AI5 | Spike: meeting-type classification | Intelligence | partial | P3 | Prototype shipped: `mp classify-meetings` (heuristic over title/workflow/attendees, zero-egress; `--llm` adds a local-engine label routed through `effective_backend`), report + `--json`, 8 tests. Owner-owed: run it on the real library (only 2 test meetings here, both `client`) and decide whether the labels earn a `MeetingSummary` schema field. No schema change landed. |
| CAL2 | Pre-meeting prep card | Feature | new | P3 | A "Last time" affordance on the prompt (+ `mp prep`): the matched workflow's last summary + open actions. Quiet register, never a popup. |
| UX24 | Saved smart folders | UX | new | P3 | Save a query + filter chips as a named rail scope over the UX16 FTS engine; PRODUCT.md already names the surface. |
| UX25 | Quick-ask handoff | UX | new | P3 | Question-shaped Cmd+K input hands off prefilled-and-running to the Ask rail via the existing meetingpipe://ask plumbing. |
| AI8 | Quiet meeting analytics | Intelligence | new | P3 | Hours-per-workflow and you-vs-them talk ratio from data that already exists (durations, workflow tags, the ADR 0009 stereo split); quiet register, no gamification. |
| ASR3 | Batch re-transcribe ratchet | Transcription | new | P3 | Transcripts are frozen at capture-time quality; STOR1 chose FLAC precisely to keep reprocessing possible. Library multi-select re-transcribe with the current stack; overlays survive; glossary/roster reach old meetings. |
| AI9 | Learned workflow routing | Intelligence | new | P3 | WF8 reassignments quietly produce labelled correction pairs nothing consumes; suggest (never auto-apply) a workflow on the prompt chip. Gated on the AI5 owner verdict. |
| ARCH5 | Refactoring band | Architecture | new | P3 | Promote when touching the files: MeetingRecorder post-processor extraction, PipelineLauncher's embedded Apple-Intelligence engine + 17-method driver protocol, LocalSummaryClient supervisor split (sequence vs LOCAL12), plus the small duplication list (see spec). |
| I7 | Drop Python entirely | Architecture | deferred | P3 | Trigger amended 2026-07-10: MLX-Swift matches the mlx-lm 7B on quality/latency (Apple Intelligence's 4096-token context was the wrong gate). DIST1, the de-CLI goal, and dual-language maintenance all hang on this decision. LOCAL12 (2026-07-12) is the commissioned evaluation of this trigger. |
| I8 | Live transcription during recording | Detection | deferred | P3 | The streaming floor exists natively in FluidAudio; de-risk via two-pass. A post-meeting tool may not need live. Depends on LOCAL5. |
| G1 | Personal two-Mac hub | Sync | deferred | P3 | All P0/P1 dogfood bars met first. |
| D8 | Developer ID + notarization in CI | Distribution | deferred | P3 | Partly overlapped by DIST1; full notarization-in-CI parked until a second user. The durable fix for the per-build TCC churn. |
| CAP1 | Mic/system end-of-call skew | Detection | watch | P3 | Monitor only; the promotion trigger is the few-second skew reappearing across multiple recordings. Spec retained in q3-final.md. |
| BRAND1..9 | Launch readiness band | Brand | deferred | P3 | Domains, trademark, landing page, demo GIF, screenshots, OG card, compliance pages. Selling is tertiary; do not pull forward. |

---

## Task specs

Items carried from Q4 unchanged have their full text in [`q4-final.md`](./q4-final.md) (search the id); the new and changed items are written out below.

### Intelligence layer (AI band)

The Q5 intelligence build (AI1 lifecycle, AI2 spike, AI3 ask, AI4 digest, DV1 facts, DV2 attendee spike) shipped; their specs are in [`q5-final.md`](./q5-final.md), and AI6 (recurring-series continuity) shipped in Q6 and lives in [`q6-final.md`](./q6-final.md). What remains is one spike and one follow-on. The DV People pivot's re-open condition is now structurally met (FEAT3-ROSTER + FEAT3-MANAGE shipped): DV3 (2026-07-12, assessment band) re-opens it as the People rail, gated on owner enrollment volume; CAL1's attendee seeding remains the complementary path.

**AI5 (P3, spike): meeting-type classification.** Prototype on-device meeting-type labels over the existing library (a short local call, or a heuristic over attendees / cadence / title) and show the results to the owner before committing a permanent `MeetingSummary` field. Uncommon among competitors (only MeetGeek auto-detects). Acceptance: a throwaway classifier labels the library by type; the spike returns a recommendation on whether the labels are useful enough to wire into grouping/digests and earn a schema field; no schema change lands until that decision.

**Progress (2026-07-11): prototype shipped, usefulness judgment owner-owed.** `mp classify-meetings` (`classify.py`) discovers every `<stem>.summary.json` and labels it against a fixed 10-type taxonomy. Default is a zero-egress heuristic over the title, workflow name, and attendee count (first-match keyword rules, then attendee-count fallbacks); `--llm` adds a per-meeting label from `engine.complete_text` beside it, routed through `effective_backend` so a regulated/NDA config stays on-device (the AI/DV standing rule), plus a `--json` payload. It arms the egress guard via `entry.prepare` (secrets only under `--llm`). CLI-only is intentional (a spike harness, de-CLI exempt like dogfood / analyze-detection); registered in `__main__.py` + USAGE + the ARCHITECTURE subcommand table. The pure heuristic, discovery, LLM-snapping, and report are pinned by 8 `test_classify.py` tests (698 pipeline green, ruff + pyright clean); a live heuristic run on this Mac confirmed the end-to-end flow, but the library is only 2 test meetings (both landed `client`), too thin to judge. Owner-owed remainder, the spike's headline question: run `mp classify-meetings [--llm]` on the real, diverse library and decide whether the labels cluster usefully enough to wire into grouping/digests and earn a permanent `MeetingSummary.meeting_type` field. Recommendation if adopted: heuristic-first (cheap, zero-egress, no model) with the LLM as a fallback/refinement, not an always-LLM field. No schema change landed (acceptance honored).

### Feature review 2026-07 (STOR/PROV/AUTO bands, ASR1, and additions across existing families)

Origin: an owner-commissioned review of the shipped surface against this backlog (2026-07-05). The finding: correctness, detection, and design are well covered, and the gaps cluster in **archive stewardship** (the product accumulates an irreplaceable corpus, corrections, voiceprint, roster, library, with no storage, backup, or sync-safety story) and in **half-closed loops** (corrections advertise a LoRA phase no task owns; digest and long-meeting flows dead-end in CLI or manual steps). The owner approved all twenty review items on 2026-07-05 and proposed the provider seam (PROV1) in the same pass. Bucket mapping: must to P1, should to P2, nice to P3 (P3 here means promote on demand, per the AI5/I8 precedent). The standing AI/DV rule applies to every item that touches an engine. The band's shipped members (MIC13, STOR1, SEC12, ASR1, FEAT8, STOR2, MIC14, MIC15) live in [`q6-final.md`](./q6-final.md), where UX18 is also closed as already-shipped.

**LOCAL9 (P2): corrections Phase 3, local LoRA fine-tune.** `mp corrections-stats` advertises a Phase 3 readiness check (~20 corrections over ~200 minutes) that no backlog task owns; make it real or stop advertising it. Scope: an owner-invoked `mp train-adapter` that fine-tunes a LoRA on the standard 7B via the MLX LoRA path, building prompt/completion pairs from the corrections corpus (original vs corrected summaries); evaluation is `mp dogfood` A/B (base vs adapter) on held-out meetings, hand-graded like the engine comparison; the adapter ships only through an explicit config opt-in (`summarization.local_adapter_path`), never silently applied. Fully on-device, no egress; training is a command the owner runs, no background training. An honest terminal outcome is allowed: if the A/B shows no capture win, close as adapter-not-worth-it, keep the corpus as a prompt-context asset (few-shot exemplars are the cheaper fallback lever), and fix the README wording. Gated on the corpus bar, which is owner-owed grading volume. Classification note (2026-07-10): `mp train-adapter` is owner-dev tooling under the de-CLI standing rule, CLI-only by design, like dogfood. Acceptance: with the bar met, a training run produces a loadable adapter; the dogfood scorecard decides adoption; README's corrections section describes the real flow either way.

**Progress (2026-07-11): tooling shipped, adoption owner-owed.** The full tooling is in: `train_adapter.py` (`mp train-adapter`) reads the summary-grading corpus, builds `{prompt, completion}` pairs (the base summarization prompt + transcript, from `mp.prompts/meeting_summary.md`, mapped to the owner's corrected summary JSON), splits train/valid/test, and shells out to `mlx_lm.lora --train` (subprocess, matching `build_server_command`'s precedent, so the module stays importable on Linux CI); it refuses below the `corrections-stats` readiness bar unless `--force`, and runs local-only via `entry.prepare(secrets=False)`. `summarization.local_adapter_path` (config, empty default) is the opt-in, threaded through `summarize_local.build_server_command` (+`--adapter-path`) and both `LocalSummaryClient` construction sites plus the `mp serve-local` warm path, so a configured adapter is actually served and never applied when unset. `mp dogfood --adapter <dir>` adds the base-vs-adapter A/B (local-only, no cloud baseline, no zero-egress refusal). README's "Improving local quality" section, `config.example.toml`, and the ARCHITECTURE subcommand table describe the real flow. Verified: pipeline 673 green (ruff + pyright clean), incl. pair-building / split / command-construction / `--adapter-path`-when-set / dogfood-adapter-mode-skips-cloud tests; `mp train-adapter --help` and the readiness gate (0/20 on this Mac) confirmed by hand. The two owner-owed remainders keep this partial: the **corpus is empty on this Mac** (grading volume is owner-owed), and the **A/B adoption decision** (run `mp train-adapter` then `mp dogfood --adapter`, hand-grade, set `local_adapter_path` only if it wins) needs a real corpus + Apple-Silicon Mac, not exercisable here. An honest negative result stays a valid terminal outcome.

**CAL2 (P3): pre-meeting prep card.** Before a recurring meeting, the relevant context (the matched workflow's last summary and its open actions) is a hunt through the Library. Add a user-invoked "Last time" affordance on the prompt panel (and `mp prep <workflow>` for the shell), quiet register, never a popup. The workflow-keyed version needs nothing new; calendar-aware timing and title/attendee seeding depend on CAL1, so sequence the full version behind that spike.

**AUTO1 (P2, promoted from P3 on 2026-07-10): automation surface, App Intents + URL scheme.** Promoted by the architecture review as the highest-leverage de-CLI enabler (Shortcuts, Raycast, and Stream Deck ride for free). The daemon is driveable only by two hotkeys and the menu bar. Expose start/stop/toggle record (with a BYO variant), open Library (optionally scoped), ask, and digest via App Intents (Shortcuts) and a `meetingpipe://` URL scheme, so Raycast and Stream Deck ride for free. External triggers respect exactly the gates the hotkey does (mic permission, cooldowns, no start while recording); local IPC only, no new egress. Acceptance: a Shortcut toggles a recording end-to-end and a denied-mic trigger routes to the Permissions tab instead of silently failing.

**Progress (2026-07-11): URL scheme shipped and verified; App Intents deferred on the toolchain.** The `meetingpipe://` half is done. `Automation/AutomationCommand.swift` is a pure, unit-tested parser (`toggle` / `record[?byo]` / `stop` / `library[?scope=ask|digests|facts]` / `ask?q=` / `digest`); `AppDelegate.application(_:open:)` receives the deeplink (Launch Services delivers it to the running daemon once `CFBundleURLTypes` is registered), and `Coordinator+Automation.handleAutomation` routes it through the same `session` entry points the hotkey uses, so a denied mic still deeplinks to Permissions (via `beginRecording`) and `record` only ever starts from idle (never stacks a second recording). `library` / `ask` / `digest` land at the right Library rail via new `pendingScope` / `pendingAskQuestion` on `LibraryWindowModel` (`ask` prefills + runs the question, `digest` triggers `generateDigest`). `CFBundleURLTypes` (scheme `meetingpipe`) is in `scripts/install.sh`'s Info.plist heredoc. This meets the acceptance: a Shortcut using the built-in "Open URL" action with `meetingpipe://toggle` toggles a recording end-to-end, and Raycast / Stream Deck ride the same way with no App Intents metadata. Verified: daemon 1121 green (6 new `AutomationCommandTests`). Two remainders keep this partial: (a) **native App Intents are not shipped** (a first-class, no-URL-typing Shortcuts action), because the daemon builds with a plain `swift build` and no Xcode project, which does not run the `appintentsmetadataprocessor` build phase Shortcuts needs to discover App Intents, so shipping them now would be undiscoverable dead code; lighting them up means either moving the build to Xcode or invoking the metadata processor in `install.sh`, a separate spike that DIST1's build-system question may force anyway. (b) **Owner-owed runtime eyeball**: confirm `open meetingpipe://toggle` actually routes on a real Mac (Launch Services URL delivery is not exercisable in this harness, and the menu-bar app is not screenshotable here), and note the one-time `scripts/install.sh` re-run to register the scheme on an existing install (the fast `rebuild.sh` does not rewrite Info.plist).

### Distribution: bundle a runtime (DIST1)

**DIST1 (P2, partial): bundle a runtime for a drag-n-drop installer.** Base spec in [`q4-final.md`](./q4-final.md) (search the id). **Progress (2026-07-12): daemon plumbing + build tool + runbook shipped; the notarized bundle build + clean-Mac test are owner-owed.** Design + runbook: [`docs/spikes/dist1-bundle-runtime.md`](../spikes/dist1-bundle-runtime.md). The problem is that a clean Mac has no Python 3.11+ and the install needs Homebrew + uv + ffmpeg (and locked-down regulated Macs often cannot install Homebrew at all, the exact zero-egress target). Approach: embed a relocatable python-build-standalone (`install_only`, no baked rpaths) + the pipeline wheels + a static ffmpeg under `Contents/Resources/pipeline-runtime/`. **Shipped + tested:** `PipelineLauncher.findMP` now prefers the embedded runtime and runs it as `python3 -m mp` (NOT the `bin/mp` console script, whose pip-baked absolute shebang breaks when the .app is relocated to /Applications, the crux relocation gotcha), extracted into a pure injected `resolveDirectMP` with 4 `PipelineLauncherFindMPTests` pinning the priority (embedded > venv > uv > PATH); `MeetingRecorder.findFFmpeg` falls back to `pipeline-runtime/bin/ffmpeg` after PATH, covering the recorder merge and `MuteRedactor`. The build tool `scripts/bundle-runtime.sh` fetches the runtime, `pip install`s the pipeline into it, verifies `python3 -m mp --version`, installs a static ffmpeg (owner pins an LGPL/GPL-compliant `FFMPEG_URL`), and signs every embedded mach-O inside-out (a notarization prerequisite). 1224 daemon green. **Reconciliation:** DEP1 (GO on native AVFoundation merge/transcode) does not remove ffmpeg because `MuteRedactor` is a third spawn site out of DEP1's scope, so a static ffmpeg is still bundled (the `findFFmpeg` fallback makes dropping it later a no-op); I7 (drop Python) is the strategic fork DIST2 recommended deferring, and bundling now buys a shippable clean-Mac install without betting on the port. Owner-owed remainder (needs Developer ID creds + a second clean Mac, neither here): run `scripts/bundle-runtime.sh` with a real `CODESIGN_IDENTITY` + `FFMPEG_URL`, notarize (`notarytool submit --wait`) + staple, package a .dmg, and drag-install-test on a clean Mac confirming (via `events.jsonl`) the pipeline ran entirely from the bundle. D8 is the durable CI home for the notarize step once a second user exists.

### Spike: browser/PWA mute oracle (MIC7)

**MIC7 (P2, partial): browser/PWA mute oracle via a stable platform signal (Google Meet first).** Base spec in [`q4-final.md`](./q4-final.md) (search the id). **Progress (2026-07-12): architecture + measuring instrument shipped; the Meet DOM-stability verdict + the build are owner-owed.** The spike doc [`docs/spikes/mic7-browser-mute-oracle.md`](../spikes/mic7-browser-mute-oracle.md) confirms the surface (browser meetings run `NoOpMuteAdapter` for both `.meet` and `.browser`, so they are energy-gated; MIC5 redaction degrades to an RMS guess there, though MIC4 already removed the data-*loss* mode, capping urgency) and the futureproof shape: a `MeetMuteAdapter: MicGateAdapter` fed by a content-script extension over a loopback/native-messaging channel, calling the same `sink(.muted/.unmuted)` the native adapters do, so no fusion changes. The crux, "spike the Meet DOM signal's stability," cannot be answered in this harness (no authenticated live Meet), so the deliverable is an owner-run, read-only console probe `daemon/scripts/mic7-meet-mute-probe.js` that dumps the mic button's candidate signals (`data-is-muted` / `aria-pressed` booleans vs the localized `aria-label`, the fragile native-title analog) and logs which one flips on every mute toggle. Verdict: **architecture GO, build DEFER, signal-stability owner-owed.** The build is gated on a real browser meeting appearing (END5's identical promotion trigger, 0 browser meetings in 19.8 days; they share the extension artifact + consent gating and should be built together), and on the probe finding a stable boolean rather than only a localized label (do not ship a localized-string oracle, the native incident settled that). Owner-owed remainder: run the probe on a live Meet call; a stable-boolean result promotes MIC7 + END5 to a real build, a localized-only result closes the Meet leg until Meet exposes something stabler.

### Spike: EventKit calendar context (CAL1)

**CAL1 (P2, partial): spike EventKit calendar context (re-evaluates ADR 0011).** Base spec in [`q4-final.md`](./q4-final.md) (search the id). **Progress (2026-07-12): analysis + measuring instrument shipped; the empirical verdict is owner-owed.** The spike doc [`docs/spikes/cal1-eventkit-calendar-context.md`](../spikes/cal1-eventkit-calendar-context.md) reframes the question against ADR 0011: ADR 0011 rejected calendar as an end-detection *corroborator* (telemetry, no verdict impact, no consumer) and leaned on an *unmeasured* assumption ("this user's meetings are not reliably calendar-bound"); CAL1 evaluates calendar as **hints only, never a trigger** (pre-title the prompt + sidecar, feed the covering event's scheduled end into `IdleStopBackstop`'s horizon, preflight-warn a broken capture precondition before a scheduled meeting) and measures the assumption. The unavoidable cost is a new Calendar TCC prompt, at odds with the reduce-first-run-prompts direction, so a GO is only justified above a coverage bar. The owner-run, read-only probe `daemon/scripts/cal1-calendar-probe.swift` (compiles clean; not exercisable here, no calendar/TCC) enumerates recorded meetings (stems carry their start time) and reports, per meeting, whether a covering EventKit event exists (±tolerance) and whether the titles resemble, then a coverage % and a READ line mapping the % to GO (>=~60%: superseding ADR + build pre-title first) / marginal (~30-60%: maybe pre-title only) / close (<~30%: confirm ADR 0011 empirically). Provisional lean, pending the number: pre-title is the one hint worth the permission at moderate coverage; the horizon + preflight need high coverage. Owner-owed remainder, the spike's whole point: run `swift daemon/scripts/cal1-calendar-probe.swift` on the real calendar and decide GO-or-close from the coverage it reports. Synergy noted: a covering event's attendee list is a clean-attendee seed for FEAT3-ROSTER / the DV2 pivot (a separate follow-on spike, not a freebie).

### Architecture review 2026-07-10 (engineering-health band)

Origin: an owner-commissioned architecture and gaps review (2026-07-10), run as four deep passes (daemon architecture, pipeline architecture, CLI-surface inventory, security/performance) with every recommendation-driving finding re-verified in code. The headline: the codebase is above average for an AI-coded product (real tests, disciplined deps, honest comments, working security chokepoints), and the debt concentrates in three places: an untested god-object spine in the daemon, documentation drift that misleads the sessions steering by it, and the CLI-vs-UI policy being written nowhere (hence the de-CLI standing rule in the delegation section). All items approved by the owner on 2026-07-10. The review also closed UX18 as already-shipped, promoted AUTO1, and amended I7, PERF7, and LOCAL9 in place.

**CONC4 (P2, partial): extend compiler-checked concurrency to the executable.** `-strict-concurrency=targeted` covers only MeetingPipeCore ("an island", per Package.swift); the 104-file executable mixes four idioms (47 `Task {`, 15 `Task.detached`, 67 DispatchQueue sites, 36 `@MainActor`, plus Combine) with comments as the only enforcement, and this exact bug class has shipped three incidents (the 2026-05-13 VPIO degradation, the 2026-06-05 stopCapture hang, the 2026-06-12 stacked starts). Scope: flip targeted mode on the MeetingPipe target, triage the warning list, fix file-by-file, tests green after each file. `complete` mode is explicitly out of scope, a separate later decision. Acceptance: the executable builds warning-clean under targeted strict concurrency; no behavior change; the full suite stays green.

**Progress 2026-07-10 (measured, then stopped deliberately).** Flipping the flag produces **20 unique warnings** (the raw build prints ~400; they are the same 20 repeated per compilation unit). By file: `MeetingRecorder` 7, `CorrectionsTab` 5, `MeetingSessionController` 2, `SinkDispatcher` 2, `DoctorRunner` 2, plus 2 surfaced in Core.

The 2 Core ones **are shipped**: `MeetingLifecycleVerdict`, `MeetingLifecycleContext`, its nested `Kind`, and `EndingReason` are now `Sendable`. They are immutable value types crossing an `AsyncStream` boundary, exactly like `MicGateVerdict`, which already declared it; annotating them is correct on its own terms and is a prerequisite for the rest. The flag itself was reverted so `main` stays warning-clean.

The remaining 18 are **not** the "mechanical Sendable annotations, not logic changes" the spec anticipated, which is why this stopped rather than guessing:

- `MeetingSessionController` (2) and `SinkDispatcher` (2) capture `self` into `@Sendable` closures. Both are documented main-queue-only, so the correct fix is `@MainActor` on the type, not `@unchecked Sendable`. That is a real isolation decision: it cascades into `Coordinator` (which calls them from non-isolated context), into `SessionHost`'s protocol requirements, and into ARCH4's new tests.
- `MeetingRecorder` (7) captures `AVAudioEngine`, `SystemAudioCapture?`, and two optional callback closures into `@Sendable` closures, plus wants `@preconcurrency import AVFoundation`. Its cross-thread state is lock-guarded, so `@unchecked Sendable` is arguable, but it is a promise about synchronisation, and the audio-render-thread rules mean a wrong one silences a warning while leaving a real race.
- `CorrectionsTab` (5) sends a `Task<[String: Any]?, Never>` across an actor boundary; `[String: Any]` is not Sendable, so this needs a typed payload, not an annotation.
- `DoctorRunner` (2) captures a `(Data) -> ()` appender; the closure needs to be `@Sendable`-typed at its declaration.

Next session: take `MeetingRecorder` first as the spec says, then decide `@MainActor` for `MeetingSessionController` + `SinkDispatcher` as one change, then `CorrectionsTab`'s payload type. Re-flip the Package.swift line last, once the count is zero.

**I7 (P3, deferred; trigger amended 2026-07-10).** Base spec in [`q4-final.md`](./q4-final.md). The Q4 trigger (Apple Intelligence proves out as the Swift-native summarizer) is the wrong gate: the realistic Swift-native local path is MLX-Swift (the same models mlx-lm serves, Apple-maintained Swift bindings), not the 4096-token system model. New trigger: MLX-Swift LLM inference matches the mlx-lm 7B on quality and latency for this workload, or Apple widens the system-model context. Context for when the trigger fires: DIST1 (bundling a relocatable Python), the de-CLI goal, and the dual-language maintenance burden all hang off the Python sidecar, so evaluate the consolidation deliberately rather than letting it drift. LOCAL12 (2026-07-12, assessment band) is the commissioned evaluation of this trigger; see the Assessment review section.

### Assessment review 2026-07-12 (detection-coverage, reliability, truth, and setup bands)

Origin: an owner-commissioned multi-angle assessment (2026-07-12, post-FEAT9), run as eight parallel audit passes (daemon architecture, pipeline architecture + cross-language contracts, SOLID/KISS/YAGNI, doc/functional consistency, coverage + test quality with both suites executed, detection stability, UX/setup, product opportunities) with every critical/high finding re-verified by an independent adversarial agent; verifiers downgraded five of eleven originally-high claims and refuted one absolute, and the severities filed here are the corrected ones. The headline: the capture half of the core promise is genuinely robust (REC1-REC5 verified as described, contracts mostly pinned, suites green, ruff/pyright clean), and the distance to the owner's stated bar (Spark-Mail-grade "it just notices every call") is structural, not tuning: detection is a hand-maintained whitelist single-gated on window-title text with its audio-evidence leg dead in production, the recovery half of the crash story is weaker than the capture half, and several surfaces assert behavior the code no longer has (worst: the mute story). The owner approved filing everything on 2026-07-12. Bands: DET (app-agnostic detection, the owner's #1 priority), REC/END/MIC/ASR (lose-nothing reliability), PIPE/LOCAL (pipeline posture + the platform decision), CI/T/DOC/HYG (truth maintenance + measurability), UX (first-run + shipped-surface floor), and corpus features (STOR4, DV3, AI7, AI8, AI9). Setup items file at P2 because the owner's single install already works; re-band if a fresh install or a second Mac is imminent. The P1 line is the assessment's recommended do-first order, not a contract; re-band freely.

**DET2 (P2): CoreAudio process-tap attribution (DET1 phase 2).** `ProcessAudioSignal` is dead in production: the PID-to-HAL translation never resolves without audio-capture authorization (0 reads in 19.8 days, 13,407 unresolved; `ProcessAudioSignal.swift`, `NativeLifecycleAdapter.swift:8-17`). Adopt a CoreAudio process tap (`AudioHardwareCreateProcessTap` / `CATapDescription`, macOS 14.2+, the repo's existing floor) so per-process input attribution resolves, reviving `processAudioActive` as real start/end evidence. Upgrades DET1's generic prompt to a named high-confidence one, gives native start detection a second evidence class (breaking the DET5 title single-gate for good), and gives end detection an independent leg. Cost: one more TCC consent, so sequence after DET1 proves the tier's value. Acceptance: the signal reports live per-process audio on a real call; the scorer treats it as an independent signal; DET1 prompts name the attributed app.

**END8 (P2): window-gone false-end hardening.** A sustained off-screen native meeting window (minimized, other Space, fullscreen elsewhere) promotes a false end after the debounce: `ShareableContentSignal` enumerates with `onScreenWindowsOnly:true`, `shareableContentWindow` requires no corroboration, and `rescueProvisionalEnd` cannot revert a window-gone-led provisional even when its AX re-walk finds a healthy Leave button. Scope: let a healthy-Leave rescue revert the provisional regardless of the leading signal, or require cross-class corroboration for window-gone-led ends (END6's `EndEvidenceClass` pattern). Acceptance: minimizing the meeting window for longer than the end debounce does not stop the recording; a real leave still confirms within the current latency.

**REC7 (P2): bounded, crash-consistent stop path.** `stop()` bounds the SCStream awaits at 5 s each (the 2026-06-05 lesson) but not the writer drain (`SerialBufferWriter.finish()` is an unbounded `queue.sync`) or the ffmpeg merge await, so a wedged write makes `.stopping` inescapable; the merge also writes the final WAV at its canonical path with no temp+rename (`MeetingRecorder.swift:1557-1589`), so a hard interruption leaves a truncated final that recovery then refuses to touch ("never clobber a finished recording") while intact intermediates sit beside it. Scope: a bounded `finish(timeout:)` falling through to keep-intermediates + recordfail exactly like a merge failure; a generous ceiling on the merge await; merge to `<stem>.merging.wav` and promote on verified success (the MuteRedactor temp+promote pattern); flush FEAT8 markers before the merge outcome branches (they are currently discarded on a failed merge); plus the small failure-path bundle: check for a terminal lifecycle verdict after the async recorder start (an `.ended` arriving in that window is currently swallowed and the recording runs against a dead meeting until the backstop), release the SCStream on the stalled-death path, take the MIC15 coverage snapshot after the tap stops, and `.atomic` on the markers write. Acceptance: a simulated wedged writer exits `.stopping` within the budget leaving intermediates + recordfail; a killed merge leaves no final WAV at the canonical path; suite green.

**REC8 (P2): self-healing capture.** Mid-meeting system-audio death recovery is gated on the owner noticing a HUD banner and clicking Retry (REC4 deliberately chose manual; the plumbing exists: `handleSystemAudioLost`, `retrySystemAudio`, `onSystemAudioRecovered`); the daemon holds no power assertion, so an in-person manual recording with no meeting client can idle-sleep the Mac mid-recording; there is no wake hook, and the tap-liveness watchdog is gated on `engine.isRunning`, so a silently stopped engine is never re-armed. Scope: one automatic bounded retry ~5-10 s after a system-audio loss, then fall back to the banner (preserves REC4's partial-recording rationale); `IOPMAssertionCreateWithName` (prevent idle sleep) across recording, released in `stop()` and the termination flush; observe `NSWorkspace.didWakeNotification` and re-arm or fail cleanly if recording; fix the watchdog gate. Acceptance: a killed SCStream recovers hands-off on the first retry; `pmset -g assertions` shows the assertion during recording and not after; a sleep/wake mid-recording ends with a usable artifact.

**MIC16 (P2): mute-backstop AX poll off main.** The 1 Hz mute-state backstop performs a fresh synchronous cross-process AX tree walk on the main thread for the entire recording (`MeetingAXWindowWatcher.swift:91-96` Timer on main; `poll()` to `readMuteStates` to `MeetingAXHandleBuilder.findMeetingWindowMuteButtons`), which can delay the panic force-stop hotkey exactly when a meeting client wedges. Move the poll's stateResolver onto the existing `axWalkQueue` and hop only the fused result back to main; the injected-resolver test seam makes this cheap to pin. Acceptance: no cross-process AX call on the main thread during a recording; watcher tests green; a deliberately slow resolver in tests does not block a simulated hotkey.

**ASR2 (P2): per-job diarizer reset.** `FluidAudioRunner` caches the ASR manager and diarizer for the daemon session lifetime (deliberate, to avoid CoreML recompilation), but the `DiarizerManager` also accumulates a speaker-clustering database across meetings with no reset; the code acknowledges the side effect (`FluidAudioRunner.swift:145-147`). Re-initialize the diarizer (or call FluidAudio's reset, if exposed) at the start of each transcription job while keeping `DiarizerModels` resident, so clustering state is per-meeting and label assignment cannot drift with daemon uptime. Acceptance: two back-to-back jobs produce the same labels a fresh-launch run produces on the same fixtures; no per-job model recompilation.

**PIPE8 (P2): pipeline posture + honesty bundle.** Five small fixes in one pass. (a) `mp serve-local` calls `Config.load()` then `os.execvp` directly (`summarize_local.py:781-822`): no `entry.prepare`, no `egress_guard.child_env()`, no `HF_HUB_OFFLINE`, no cached-model refusal, unlike the lazy spawn at `:522-544`; fix with `entry.prepare(secrets=False)` + `execvpe(cmd[0], cmd, child_env())` + the armed-and-uncached refusal, and pass a token-stripped env from the daemon's `LocalModelPreloader`. (b) An entry-contract tripwire test: walk every `__main__` dispatch target and assert the module calls `entry.prepare` or sits on a named allowlist, so the next forgotten arm is a red build (SEC13 is enforced by convention today). (c) Atomic writes: `orchestrate` finalize, `_apply_glossary`, and `summarize` rewrite the only copy of the transcript/summary via plain `write_text`; one `_atomic_write_text` helper (tmp in dir, chmod 0600, `os.replace`), matching the embeddings sidecar and every Swift store. (d) `mp digest --publish` calls `publish_router.fanout` without the `EXIT_PUBLISH_FAILED` check every other fanout caller honors (`digest.py:262-263`). (e) FEAT9 follow-up: `merge_meetings` commits the merged audio (`os.replace`, `merge_meetings.py:220`) before writing the transcript that carries `merged_from` (the retry guard's only key, `:195`), leaving a crash window that re-concatenates fragments onto already-merged audio on retry; commit the transcript first or add a pre-replace marker the retry checks, and make those transcript writes atomic per (c). Acceptance: serve-local refuses armed-and-uncached and leaks no tokens to the child; the tripwire is red on a new unarmored subcommand; a kill during finalize leaves a parseable transcript; digest exits 3 when all sinks fail; a kill between the merge's two commits does not double-concatenate on retry.

**PIPE9 (P2): transcript corrections reach the pipeline.** Transcript text corrections (`TranscriptCorrectionStore`, resolved at display time in Swift) have zero Python readers, so `mp summarize` regenerate, `mp ask`, `mp digest`, and the embedding index all run on uncorrected text while the Library shows corrected text; the store's own doc comment says `original_text` exists "so downstream tools can detect drift" and none consume it. Scope: a small `mp.transcript_corrections` reader mirroring the `speaker_overlay` precedent (same segment-index convention), folded into the overlay application and the index's text assembly so both overlays apply on any re-read; stamp `schema_version` on the sidecar; add a CI4-style parity fixture. Acceptance: correcting a transcript then regenerating produces a summary reflecting the correction; ask/digest citations quote corrected text.

**LOCAL11 (P2): warm-server identity verification.** Warm `mlx_lm.server` reuse never verifies what the server serves: `_is_healthy` checks only HTTP 200 on `/v1/models` (`summarize_local.py:572-577`) and `--adapter-path` exists only at spawn, so a `local_model` or `local_adapter_path` config change silently serves stale weights, the run sidecar records the configured (not served) model, and a warm base-model server means the LOCAL9 adapter never actually serves. Scope: compare the `/v1/models` payload (plus an adapter field added to the LOCAL10 ownership marker) against the configured identity and respawn on mismatch when we own spawning; restart the daemon's `LocalModelPreloader` on the relevant `ConfigStore.didPersist`; a doctor probe reporting served-vs-configured mismatch (also a precondition for trusting the LOCAL9 A/B). Acceptance: flipping the model or adapter takes effect on the next run without a manual kill; doctor names a mismatch; the run sidecar records the truly served identity.

**LOCAL12 (P1, spike): MLX-Swift trigger evaluation (the I7 gate).** The platform decision the quarter hangs on, commissioned so I7's amended trigger stops being aspirational: evaluate MLX-Swift LLM inference on the standard 7B against the current mlx-lm path on this exact workload (summary quality via the dogfood harness, latency, peak memory, long-transcript behavior vs the LOCAL3 map-reduce), and write a verdict doc in `docs/spikes/` like DEP1's. Context the decision weighs: about 8% of pipeline LOC (~1.1k lines plus four golden-fixture sets) is Swift-mirror upkeep and the mirror count keeps growing, while the LLM band (~2.9k), publishers (~1.9k), and embeddings/RAG (~1.3k) are real Python behavior with no Swift equivalent; DIST1's daemon plumbing and build tool shipped 2026-07-12 in parallel with this review (small, worth keeping either way), but the endgame choice (maintain the embedded-Python bundle vs an incremental port starting at engine/summarize, per `docs/spikes/dist2-swift-port-of-the-pipeline.md`) hangs on this verdict. Acceptance: a written GO/NO-GO on the I7 trigger with measured quality/latency/memory deltas and a recommended endgame shape.

**CI4 (P2): pin the remaining cross-tree contracts.** Three must-agree Swift-Python surfaces have no shared fixture, unlike the CI2/CI3 set (meta, chunking, json-extract, summary-markdown): `<stem>.publish.json` (the PIPE1 outcome contract; Python tests pin Python-authored expectations, Swift tests pin Swift-authored string literals, so coordinated drift passes both suites), the `mp prefetch-model` JSONL progress stream (`ModelDownloadSupervisor` switches on event strings pinned by neither side), and the speaker-overlay resolution (documented "must stay byte-identical to Swift"); the speaker-labels sidecar is also the one cross-language sidecar with no `schema_version`. Scope: golden fixtures generated from the Python writers and parsed by Swift tests for all three, plus the version stamp (the reader is already fail-open). Acceptance: a deliberate writer change breaks the other tree's test; CI green otherwise.

**CI5 (P2): truth fences.** This review found the same failure class END4(b) fixed once, three more times (a documented, rendered knob consumed by nothing) and the event-category table five categories behind the emitters despite DOC7. Scope: a CI step that extracts keys from `config.example.toml` + the README config reference and diffs them against the actual readers (`Config.swift` / `ConfigStore.swift` / `config.py`), and one that greps `Log.event` / `events.emit` category literals and diffs against the CONVENTIONS table (rejecting non-snake actions like `toolbar.action`). Sequence after HYG2 and DOC9 make the current state clean. Noted but not gated here: an arm64 pipeline CI leg; the Swift-warning fence lands with CONC4 (its own acceptance). Acceptance: reintroducing a dead knob or an undocumented event category fails CI.

**T3 (P2): shipped-untested tests + coverage harness.** The load-bearing untested residue: `SearchIndexer`'s reconcile/signature diff (UX16, shipped with zero test references), `StatusBarController` (731 lines, the always-visible surface, zero references), `Notifier`'s response-identifier-to-delegate routing (a consent surface), and `__main__.py`'s dispatch (the pipeline front door); and no coverage measurement exists in either tree (pytest-cov absent from dev extras, no `--enable-code-coverage` anywhere). Scope: pin reconcile (first-scan build, mtime-change reindex, dead-stem delete, no-change no-bump); extract a pure `StatusBarModel` derivation (title/icon/menu state from AppState + processing count + NDA + permissions + download state) and a pure Notifier routing map per the repo's `decide()` idiom, table-test both; a parametrized dispatch test over every registered command name; add pytest-cov to dev extras + `scripts/coverage.sh` (pytest --cov=mp; swift test --enable-code-coverage + llvm-cov report) and record a baseline here. Optional: `SystemAudioCapture.pcmBuffer(from:)` conversion. Acceptance: new tests green in both suites; `scripts/coverage.sh` prints per-module numbers for both trees; the baseline is written down.

**DOC8 (P1): mute-story truth.** Confirmed high, privacy-class. Three surfaces still describe pre-ADR-0016 live mic gating: the Preferences "Pause mic when muted" toggle copy, the `config.example.toml` `honor_app_mute` comment, and ARCHITECTURE's mute-gating section, sequence-diagram note, and glossary; the code zeroes the mic only under the regulated gate (`MeetingRecorder.swift:670-692`: capture-first modes are "Lossless: never zero the mic"), redaction is a per-workflow opt-in (`flags.redactMutedSpans`, `MeetingSessionController.swift:295-304`), and `honor_app_mute` only gates whether MicGate observes mute for the timeline/backstop. Under the default workflow, muted side-comments are recorded, transcribed, summarized, and published; README's ADR 0016 section is the lone correct surface. Scope: owner decision first, keep capture-first-by-default (then rewrite the three surfaces to the real posture: the toggle becomes something like "Track in-app mute" with a sublabel pointing at per-workflow "Redact muted spans" and regulated mode) or flip the default to redact; either way every surface stops asserting the old model, and the workflow editor hints where redaction actually lives. Acceptance: every surface that mentions mute matches shipped behavior; a search for the old claim across Preferences copy, config comments, and ARCHITECTURE returns nothing.

**DOC9 (P2): docs truth sweep.** The post-DOC7 drift this review verified, fixed in one pass: README's backstop troubleshooting numbers (90 s nudge / 5 min auto-stop vs the shipped 480 s nudge and `mic_only_silence_seconds` default 900, configurable 60-1800); the three-Keychain-items claims in CONVENTIONS and README vs the four managed keys (OPENAI_API_KEY joined via PROV1; the backup manifest names four); `config.example.toml` missing the `[output]`/`[obsidian]`/`[filesystem]`/`[lan]` sections and `detection.default_prompt_action` that README documents, plus two stale comments; "Republish all" claiming Notion-only in README and the BatchActionsPane card title while the code fans out via `mp publish`; the Preferences "Whisper" sublabel (ASR is FluidAudio/Parakeet); the CONVENTIONS event-category table missing five emitted categories (daemon, recording, Swift-side pipeline, detail, prefetch) and the non-snake `toolbar.action`; ARCHITECTURE's stale detail-pane tab list and pre-INSIGHTS rail description plus dead `RawFilesTab.swift` (delete it); the where-files-live table's missing rows (markers, embeddings, voiceprint, roster, glossary, transcript_corrections, empty-marker); pipeline/CLAUDE.md's VCR-cassette claim (the real convention is httpx.MockTransport; drop the dead pytest-vcr/vcrpy dev deps). Acceptance: each listed surface matches code; suites + ruff/pyright green after the dep removal.

**DOC10 (P2): SETUP.md + persona amendment.** Owner priority: easy setup for non-programmers with one exhaustive guide; today no such path exists (README's install is dev-register: git clone, Homebrew, uv, hand-run `security add-generic-password`) and the written persona actively steers sessions away from building it (PRODUCT.md's "read CLI flags faster than landing pages"; the CLAUDE.md out-of-scope list catches onboarding under selling). Scope: write `docs/SETUP.md`, the single exhaustive clean-Mac walkthrough (real prerequisites: Xcode CLT, Homebrew, network + download sizes, about 630 MB ASR models and about 4.3 GB for the local backend; the four TCC grants including the Accessibility relaunch; keys; the Notion integration + database; first recording to first published summary); add the missing prerequisites to README Requirements and a swift/CLT check to install.sh with a friendly die(); amend PRODUCT.md and the project CLAUDE.md so setup ease and the guide are written in scope while licensing/telemetry/landing-page work stays deferred. Coordinate with DIST1: the guide documents the from-source path now and gains a short drag-install section when the notarized bundle lands. Acceptance: a non-programmer can go clean-Mac-to-first-published-meeting from SETUP.md alone; install.sh fails helpfully without CLT; the persona docs no longer contradict the priority.

**HYG2 (P2): dead-knob wire-or-delete.** Three controls render, persist, and drive nothing, the exact class END4(b) fixed for the end-debounce knob. (a) `transcription.language`: ConfigStore round-trips it and Preferences offers the picker, but the sole transcribe call hardcodes `languageHint: nil` (`SinkDispatcher.swift:89`) while `FluidAudioRunner.resolveLanguage` would honor it, and README claims the key was removed; recommendation: wire it (real value for the en/uk owner) and fix the "Whisper" sublabel wording (DOC9 has the copy). (b) `recording.sample_rate`: parsed, round-tripped, offered as 16/24/48 kHz, read by no capture or post-process code (16 kHz is a deliberate constant); recommendation: delete the picker and key. (c) `detection.debounce_start_sec`: live 1-30 s slider, documented, consumed by nothing (`PromotionEngine.handleIdle` promotes instantly; only the end knob is wired); wire it into the promotion start path or delete the key, slider, and glossary sentence. Acceptance: every remaining Preferences control demonstrably changes behavior; config docs match; no key is read into Config/ConfigStore without a consumer.

**UX21 (P2): first-run traps.** (a) Startup fires all four TCC dialogs before the OnboardingGate branch (`App.swift:148` runs `coordinator.start()` first; `Coordinator.swift:367` calls `requestPermissionsAtStartup()` ungated), so a fresh install gets four unframed system dialogs stacked over the onboarding window and the framed permissions step arrives pre-answered; gate the burst on onboarding completion and have completion trigger prewarm + refreshAll. (b) A local/NDA workflow never prefetches the MLX model: `ConfigRefreshCoordinator.swift:30-35` keys only on the global backend, and the onboarding "Client work (NDA)" preset creates backend .local with no cache check, so the first NDA meeting records fine and then summarize fails with a terminal-command remedy (a de-CLI violation in an error path); on workflow save and the preset, when the resolved backend is local and the model is uncached, show an inline "model not downloaded (~4.3 GB), Download now" affordance reusing ModelDownloadSupervisor, and teach doctor's local-stack check to also run when any workflow carries backend=local or nda_mode. (c) Onboarding's Accessibility row gives no relaunch hint even though the product's own model requires one (the Preferences footnote documents it). Acceptance: a fresh install shows zero TCC dialogs before onboarding's permissions step; saving a local-backend workflow with no cached model offers the download inline and the failed-row remedy is in-app; the AX row explains the relaunch.

**UX22 (P2): setup completion surfaces.** There are four setup surfaces (terminal installer, README, Preferences, onboarding) and no in-app answer to "am I done?". Scope: a menu-bar "Finish setup" row, styled like the existing permissions-attention row, listing unmet items (key missing, database_id empty, permission missing, model uncached), each deep-linking to its fix, computed from what doctor already knows; an onboarding publish-target step (Keychain key status + the existing NotionDatabaseList picker + a small test publish), closing the last terminal/TOML dependency between install and first value; reuse that picker in Preferences Integrations (the raw Database ID field stays as the fallback); rewrite install.sh's completion epilogue as a numbered mirror of what first launch actually does, and symlink the mp launcher into `~/.local/bin` so README's bare `mp` examples work as written. Acceptance: clean-Mac first value requires no hand-edited TOML and no terminal beyond install.sh; the checklist row disappears when everything is green; `mp doctor` runs from a fresh shell.

**UX23 (P2): shipped-surface floor.** Rough edges on recently shipped surfaces the owner has not yet e2e-tested, each violating a written floor or the quiet-register promise. (a) The prompt panel is mouse-only: `PromptRecordButton` is a bare NSControl with manual mouseDown tracking, no `acceptsFirstResponder`/keyDown, and the close/chevron controls lack accessibility labels, contradicting PRODUCT.md's explicit keyboard + VoiceOver floor for exactly these buttons; add focus, Space/Return activation, labels, and a sane tab order. (b) Off-the-record (MIC14) is undiscoverable: no Preferences hotkey row (the other three hotkeys have one), no HUD affordance; add the SettingsHotkeyField row and a HUD toggle where the moment happens. (c) Search cannot distinguish "index still building" from "SQLite failed to open" (`SearchIndexer.matchingStems` returns nil for both and silently degrades forever); publish an indexing/degraded state, show a one-line hint in QuickFind and the filter bar, and add a search-index probe to the doctor sheet. (d) DigestsView's Generate now discards its `Result`, so a failed `mp digest` renders nothing; capture into an inline error row (AskView.errorState is the in-file pattern). (e) The status-bar permission warning does not name consequences; per missing permission, say what stops working ("Screen Recording off: browser meetings will not be detected"). Acceptance: prompt panel fully keyboard-operable with labels; the off-record hotkey is visible and editable; QuickFind shows an indexing hint and doctor reports index health; a failed digest shows why; the permission warning names the consequence.

**STOR4 (P2): scheduled automatic backups.** The corpus is irreplaceable (the 2026-07-05 review's own framing) yet `mp backup` runs only when the owner clicks "Back up now" or remembers the CLI, and doctor merely reports staleness. Copy the AI4 launch-agent mechanism exactly: a Preferences Storage toggle (+ day/time and target-directory picker) that installs `~/Library/LaunchAgents/com.meetingpipe.backup.plist` running `mp backup <dir>`, `launchctl bootstrap`/`bootout` on toggle, last-run age surfaced beside the existing button. Acceptance: with the toggle on, a scheduled backup lands unattended and doctor's last-backup age stays fresh; toggling off removes the agent; the manifest lands per STOR2's contract.

**DV3 (P2): People rail.** The DV pivot's re-open condition is now structurally met: FEAT3-ROSTER and FEAT3-MANAGE shipped, so named, stable, cross-meeting speaker identities exist. Scope: a Facts-style INSIGHTS projection ("People"): one row per enrolled person showing their meetings, the open actions they own (the summaries already carry owners), and last-seen; rows link back to meetings (the Quick Find hook), rename flows through the existing roster rename. Read-only projection over existing sidecars, no schema change; local-only per the AI/DV rule. Gate: needs a minimally populated roster, which is owner-owed enrollment volume; build after real usage proves a handful of stable identities. Acceptance: selecting a person lists their meetings and open actions; a roster rename reflects everywhere; the empty-roster state points at how to enroll.

**AI7 (P2): commitments loop.** Open actions resurface only if the owner opens Facts or reads a digest, so commitments rot silently between digests; and recurring meetings restate the same commitments, so Facts and digests accumulate near-duplicates and resolving one leaves its clones open. Scope: an amber overdue count on the Facts rail row reusing the needsYou badge mechanics (AI1's aging already computes overdue from ISO due dates); embedding-cluster dedupe of action items across a workflow's meetings (the on-device embedding index already exists) with resolve-the-cluster semantics so marking one instance done resolves its restatements. Local-only per the AI/DV rule. Acceptance: an overdue action shows an amber count without opening Facts; a restated standup commitment groups with its prior instances and resolves once.

**UX24 (P3): saved smart folders.** PRODUCT.md already names the rail "the Library's smart-folder rail" and UX16 shipped a real FTS5 query engine with filter chips, but scopes are a fixed enum; let a query + chip combination be saved as a named rail scope. Acceptance: a saved search persists across restarts and behaves like a built-in scope.

**UX25 (P3): quick-ask handoff.** Quick Find and Ask are separate stories; detect a question shape in Cmd+K (trailing question mark, or an "Ask about ..." row under the results) and hand off prefilled-and-running via the existing `meetingpipe://ask` plumbing. Acceptance: typing a question in Quick Find reaches a cited answer in two keystrokes.

**AI8 (P3): quiet meeting analytics.** Durations, workflow tags, and per-speaker talk time (the ADR 0009 stereo split gives you-vs-them ground truth) exist per meeting and nothing aggregates them. Hours-per-workflow and per-meeting talk ratio, quiet register (a Library view or a digest section, no streaks, no gamification), fully local. Acceptance: per-workflow hours and talk ratio for a date range, computed from existing sidecars.

**ASR3 (P3): batch re-transcribe ratchet.** Transcripts are frozen at capture-time ASR quality: glossary normalization does not touch old transcripts, roster names learned later never reach them, and Reprocess/Regenerate re-run summarize only; STOR1 chose FLAC over AAC precisely so later reprocessing stays possible. A Library multi-select "Re-transcribe with current stack" (confirmation, progress, overlays and corrections preserved via the existing sidecar patterns, re-summarize offered after). Acceptance: an old meeting re-transcribed picks up the current glossary and roster and its overlays survive.

**AI9 (P3): learned workflow routing.** WF8 reassignments quietly produce labelled correction pairs (detected source + title mapped to the corrected workflow) that nothing consumes, and manual recordings always fall to the default workflow. When enough pairs exist for a source, suggest (never auto-apply) the corrected workflow on the prompt chip. Gated on the AI5 owner verdict and real reassignment volume. Acceptance: after N same-source reassignments, the prompt pre-selects the corrected workflow with a visible, dismissible hint.

**ARCH5 (P3): refactoring band, promote when touching the files.** The unfiled structural debt the principles pass named, for green-to-green extraction whenever a task next touches these files (do not refactor speculatively): `MeetingRecorder.swift` (the repo's largest file: extract a RecordingPostProcessor for the ffmpeg merge / plausibility / recordfail / orphan-recovery half; `stop()` spans ~211 lines); `PipelineLauncher.swift` (hosts the whole in-Swift Apple-Intelligence summarization flow plus a 17-method driver protocol with NSError stub defaults; extract the engine beside AppleIntelligenceSummarizer and consider a typed command enum); `summarize_local.LocalSummaryClient` (~590 lines mixing server supervision, HTTP client, retry policy, and map-reduce; extract an MLXServerSupervisor, but sequence against LOCAL12 since a port deletes the file); and the small duplication list: `_parse_summary` byte-identical across both PROV1 providers, the six-way backend dispatch duplicated between `summarize._select_backend` and `engine.complete_text`, LibraryWindowModel's ~11 identical continuation bridges, the tri-plicated delete/export dialog copy (already drifted), and daemon config defaults written three to four times. Suites green after each extraction; no behavior change.

**Amendments (2026-07-12).** CAL1 promoted P2 to P1 (row updated, moved to the P1 cluster): the GO follow-on is the cheapest independent corroborator for DET1, names the meeting in the prompt and sidecar, is the only plausible in-person nudge, and seeds FEAT3/DV3 attendees; run the owner probe early in the DET band. END5 promoted P3 to P1 and re-scoped (row updated, moved to the P1 cluster): browser end detection is active-tab-title-only (`BrowserMeetingLifecycleAdapter.swift:115-132`; `browserTabTitle` carries `requiresCorroboration=false`), so a >5 s tab switch mid-call chops the recording, materially worse than the filed framing, and the old trigger ("a real browser meeting recorded") was circular with the start-side holes (0 browser meetings in 19.8 days, per MIC7's spike); scope now: cross-class corroboration or a minutes-long debounce for tab-title-led ends, plus a corroborator on the bare Meet-code/brand-token matchers (3-4-3 slugs and doc titles containing "huddle"/"webex"/"microsoft teams" false-prompt today); a build here unblocks MIC7's deferred half (shared extension artifact). MIC8 promoted P3 to P2 (row updated): both P1 mute items are wedged on owner AX dumps, and a UI-independent signal supersedes the whole scrape class. I7 (row + spec updated): LOCAL12 is the commissioned evaluation of the 2026-07-10 trigger, so the decision stops drifting. DIST1 (no row change): its daemon plumbing and build tool shipped 2026-07-12 in parallel with this review; LOCAL12's verdict decides whether the embedded-Python runtime is the end state or a transitional artifact, and the owner-owed notarize + clean-Mac test is worth doing either way.

### Carried from Q4 with a changed scope

**MIC6 (P1): attribute-based futureproof mute read.** Carried from Q4 DONE-partial. The four shipped parts (cross-locale category-precedence fallback, `AXMuteButtonProbe` element re-arm after N `.unknown`, decoupled `MeetingAXWindowWatcher` blind-clear, verified `uk` labels) are in place and tested. The owed remainder is part 1's headline: read the mute button's stable state attribute (AXValue / pressed-state / mark-char) instead of scraping the localized title, so the read survives UI redesigns and locale. Gated on an owner-run live AX dump (`daemon/scripts/ax-dump-meeting.swift`) to pick the per-app attribute + polarity. Overlaps MIC10 (re-lock the live control) and MIC8 (the durable non-UI signal); sequence with whichever live Teams session enables the dump.

**MIC10 (P1, partial): re-lock the Teams mini-window mute control + VAD-contradiction staleness.** Carried from Q4; the full spec is in [`q4-final.md`](./q4-final.md) (search the id). **Part 2 shipped 2026-07-11.** A new pure `VADContradictionTracker` (clock-injected, the `RMSGateProbe` idiom) hosted in `MeetingAXWindowWatcher`'s existing poll loop detects a *confidently-wrong* app-mute: the watcher reads `.muted` while `MicGate.currentVadActive` (a new thread-safe accessor over the fused HAL VAD) reports sustained OS voice for a ~4 s dwell. On the contradiction it suppresses the stale `.muted` so `MicGate.decide` falls through to the live voice, and calls a new `onStaleMuteContradiction`; the host (`MeetingSessionController.engageMicGate`) mode-gates the actual `clearAxMute` to capture-first modes only, never under the regulated/NDA gate, where a wrong un-mute would leak at-rest-free audio on a genuine muted side-conversation (the q4-final scope guard). It re-latches the instant the contradiction ends, so a real muted side-talk is only briefly affected. New `micgate`/`mute_state_cleared_vad_contradiction` event. Verified: daemon 1178 green (5 `VADContradictionTrackerTests`, 3 watcher integration tests, 1 `currentVadActive` test). **Part 1 still owed** on an owner live AX dump (`daemon/scripts/ax-dump-meeting.swift`): re-scope the walk to the live mini-window control so a backgrounded/pre-join button is never read at all; without it, part 2's discredit is the only recovery under capture-first and (deliberately) a no-op under the regulated gate. The ~4 s dwell and whether to allow a `clearAxMute` escalation under the regulated gate are owner-validation items (they want the live-wrong read).

**END6 (P1): device-idle end corroborator.** Carried from Q5 DONE-partial (absorbs the END2 validation residual). The correlated-pair gate shipped: native meetings fuse only window-gone + ax-leave, now grouped into one `EndEvidenceClass` that cannot self-corroborate (a Teams / Webex share re-render invalidates the Leave button and drops the call window together), so neither the fast path nor the debounce tick instant-confirms off that pair; the AX re-walk (`confirmProvisionalEnd`) stays the real native confirmation. The deferred remainder is owner-owed: the `kAudioDevicePropertyDeviceIsRunningSomewhere` device-idle corroborator is device-global and confounded by our own recorder holding the input device (it can never read idle during a recording), so it needs live-Mac validation before it can be a genuinely independent end signal. The AX re-walk already backstops native corroboration, so this leg is not load-bearing; the screen-share dogfood trace the spec flags is also still owner-owed. (AUD-10)

**T2 (P2): portable visual-regression tests, or close.** Carried as not-done. The Q4 image-snapshot approach (swift-snapshot-testing + `__Snapshots__` PNGs) was removed in commit de3bf35 as non-portable across macOS/Xcode on the macos-15 CI runner, and replaced by `RenderedViewSmokeTests` that only assert a view constructs without crashing. Decide one of: (a) close T2 as won't-do and keep the smoke tests as the bar, or (b) adopt a CI-portable visual-regression approach (perceptual tolerance tuned for the runner, or a layout-assertion harness instead of pixels). Pairs with the DSN3/DSN9 visual-verification needs.

**VALID1 (P1, owner-owed): on-device acceptance measurements.** Carried from Q4 DONE-partial. Scaffolding shipped (`scripts/valid1_check.py`, runbook); the five runtime measurements (A15 cold-start, A16, DIAR1 DER + step-ratio, SUM1-APPLE + Little-Snitch zero-egress, UX4 real failed SCStream) are owed by the user on a real Apple-Silicon Mac. Progress from the Q5 engine-comparison run over 26 real meetings: the dogfood local-quality gate (`docs/local-llm-quality.md`) and the MLX-vs-Apple-Intelligence read (`docs/engine-comparison.md`) are produced, and the actionable outcome (standardize the local backend on the 7B) shipped as LOCAL6. Still owner-owed: A15 cold-start, DIAR1 DER, UX4 degraded banner, and the Little-Snitch zero-egress confirmation.

**C6-FINISH (P1, owner-owed): real detection-trace corpus.** Carried from Q4 DONE-partial. The redaction note + `scripts/redact_detection_trace.py` shipped; the 20+ real redacted dogfood traces are owed by the user (the corpus is still 9 synthetic seeds).

### Carried from Q4 unchanged

Full specs in [`q4-final.md`](./q4-final.md) (search the id). Detection and capture: MIC8 (promoted and amended 2026-07-12, see the Assessment review section; base spec stays in q4-final); END5 shipped 2026-07-12, row in [`q6-final.md`](./q6-final.md); MIC13 shipped, row in [`q6-final.md`](./q6-final.md); MIC10 moved to the changed-scope section above now that its part 2 shipped; MIC7 now has a progress note in its own section above). Pipeline and distribution: none remain unchanged (PIPE1 shipped, row in [`q6-final.md`](./q6-final.md); CAL1 and DIST1 now have progress notes in their own sections above). Deferred (promote on a real trigger only): I7 (trigger amended 2026-07-10, see the review section above), I8, G1, D8, CAP1, BRAND1..9. One-line reminders live in the ToC Comment column above; the id-searchable full text stays in q4-final so each carried spec has a single owner.

### Watch list (monitor, promote on a real recurrence)

- **'Today'-filter then Clear crash (2026-06-09):** a generic-runtime use-after-free during a trivial `filter = MeetingFilter()` on a degraded 2-day process mid-storm; most likely a secondary symptom of the MIC11 failed-start storm (fixed in Q5). Re-test on a clean post-MIC11 build; promote to a real fix only if it reproduces on a fresh process.
- **REC3 deferred >3h cap-raising:** REC3 fixed the AUD-7 silent-truncation bug (a >3 h meeting is force-stopped near the 4 GiB RIFF cap rather than truncating). The cap-*raising* (CAF or segment-roll so a >3 h meeting keeps recording) was deferred as an unreproduced enhancement that ripples into REC1's orphan/merge contract. Promote if >3 h meetings become a real need.
- **CAP1** mic/system end-of-call skew (see the ToC row; spec in q3-final.md).
- **Transcription RAM peak (2026-07-12):** FluidAudioRunner reads the whole meeting into RAM (about 2 GB peak for a 3 h recording: a full-file AVAudioPCMBuffer plus a mono copy held across ASR and diarization) inside the always-resident daemon. Fine at current meeting lengths; promote to windowed/chunked reading (FluidAudio's streaming floor, pairs with I8) if a real >2 h meeting shows memory pressure.
